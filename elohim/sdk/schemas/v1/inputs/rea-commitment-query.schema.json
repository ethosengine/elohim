{
  "$id": "epr:schema:input:rea-commitment-query",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ReaCommitmentQuery",
  "description": "Query params for GET /api/v1/commitments. Documents the ReaCommitmentQuery interface hand-declared in elohim/sdk/storage-client-ts/src/api/dataplane.ts — storage-client-ts is not currently wired into schema:codegen:ts (see INTERFACE_FILES in elohim/sdk/schemas/scripts/codegen-ts.mjs), so this schema is the documented contract, not the codegen source, until that wiring is added.",
  "type": "object",
  "properties": {
    "action": { "type": "string", "description": "Filter to commitments with this REA action verb." },
    "limit": { "type": "integer", "minimum": 0, "description": "Maximum number of commitments to return." }
  },
  "additionalProperties": false
}
