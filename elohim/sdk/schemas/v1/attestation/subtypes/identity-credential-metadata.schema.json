{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://elohim.protocol/schemas/v1/attestation/subtypes/identity-credential-metadata.schema.json",
  "$comment": "Source of truth: Holochain DHT (sub-shape of attestation:identity-credential's evidence_json.summary_metric). Category A.",
  "title": "IdentityCredentialAttestationMetadata",
  "description": "Per-subtype metadata for attestation:identity-credential. Replaces imagodei's Attestation entry type (general identity credential with category + type + tier).",
  "type": "object",
  "additionalProperties": false,
  "required": ["category", "credential_type"],
  "properties": {
    "category": {
      "type": "string",
      "enum": ["learning", "stewardship", "governance", "community", "technical", "conduct", "identity", "peer"],
      "description": "High-level category. Mirrors imagodei's ATTESTATION_CATEGORIES constant."
    },
    "credential_type": {
      "type": "string",
      "description": "Specific type within category (free-form string)"
    },
    "tier": {
      "type": ["string", "null"],
      "enum": ["bronze", "silver", "gold", "platinum", null],
      "description": "Optional tier for graduated credentials"
    },
    "display_name": { "type": "string" },
    "icon_url": { "type": ["string", "null"], "format": "uri" }
  }
}
