{
  "$id": "weave-view.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WeaveView",
  "description": "Cluster-scoped operational weave projection: placement/coverage/capacity folded per-shard → per-node → per-cluster. Each lens field is optional — a facing carries only the lenses it selected (the not-selected-field contract). Operational Category C — no DHT entry; projection only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["placementGapCount", "measuredAt"],
  "properties": {
    "placementGapCount": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of open shard placement gaps (under-replicated shards) at the time of measurement."
    },
    "measuredAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp at which this view was assembled."
    },
    "rsCoverage": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Mean RS contract-coverage across open placement gaps. 1.0 when there are no gaps (fully covered). Optional — absent when the lens was not selected."
    },
    "clusterCapacity": {
      "$ref": "compute-triptych.schema.json",
      "description": "Aggregated storage capacity (free/used/stewarded bytes) summed across all custodian nodes in the cluster weave."
    },
    "tierOccupancy": {
      "description": "Risk-tier occupancy distribution (deferred — Slice 5 follow-on). Absent in Slice 4 responses.",
      "type": "object"
    },
    "regionOccupancy": {
      "description": "Viewer-agnostic regional occupancy of distinct stewarding hubs (Slice 4 — shipped by build_weave_view). Absent only when the lens is not selected; an empty holder relation reads Some(all-zero).",
      "type": "object",
      "additionalProperties": false,
      "required": ["local", "regional", "global", "unknown"],
      "properties": {
        "local": { "type": "integer", "minimum": 0 },
        "regional": { "type": "integer", "minimum": 0 },
        "global": { "type": "integer", "minimum": 0 },
        "unknown": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
