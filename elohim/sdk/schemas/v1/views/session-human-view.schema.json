{
  "$id": "epr:schema:view:session-human",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SessionHumanView",
  "description": "Source of truth: derived projection of EconomicEvent stream × HumanProgress for a specific agent (Operational, Category C). Aggregates per-agent lamad activity counts from EconomicEvent entries filtered by lamadEventType. journeyStartedAt joined from HumanProgress. Computed by elohim-storage on Signal::EconomicEventCreated. Reconstructable from underlying EconomicEvent + HumanProgress entries in the elohim DNA content_store zome.",
  "type": "object",
  "required": [
    "agentId",
    "hAppId",
    "nodesViewed",
    "nodesWithAffinity",
    "pathsStarted",
    "pathsCompleted",
    "stepsCompleted",
    "computedAt"
  ],
  "additionalProperties": false,
  "properties": {
    "agentId": {
      "type": "string",
      "description": "AgentPubKey of the human whose session state this projects"
    },
    "hAppId": {
      "type": "string",
      "description": "hApp identifier scoping this projection"
    },
    "nodesViewed": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of EconomicEvent entries with lamadEventType='content-view' for this agent"
    },
    "nodesWithAffinity": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of EconomicEvent entries with lamadEventType='contributor-presence' (affinity signal) for this agent"
    },
    "pathsStarted": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of EconomicEvent entries with lamadEventType='path-started' for this agent"
    },
    "pathsCompleted": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of EconomicEvent entries with lamadEventType='path-completed' for this agent"
    },
    "stepsCompleted": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of EconomicEvent entries with lamadEventType='step-completed' for this agent"
    },
    "journeyStartedAt": {
      "type": ["string", "null"],
      "description": "ISO-8601 timestamp of the agent's first EconomicEvent entry; null if no events recorded yet"
    },
    "computedAt": {
      "type": "string",
      "description": "ISO-8601 timestamp when this projection was last computed"
    }
  }
}
