{
  "$id": "epr:schema:manifest:app-manifest",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AppManifest",
  "description": "Protocol-level schema for app manifests. An app manifest is an EPR (content-addressed, stewarded, governed) that declares an app's content types, formats, renderers, relationships, and signals. The protocol validates the SHAPE of manifests (three-leg coupling, substrate signal mapping); apps validate their own vocabulary.",
  "type": "object",
  "required": ["id", "name", "version", "vocabulary"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Content-addressed identifier for this manifest (CIDv1). Canonical EPR address."
    },
    "name": {
      "type": "string",
      "description": "Human-readable app name. Used in registry listings and governance references.",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "description": "Semantic version of this manifest. Manifests are immutable once published; new versions create new EPRs.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "description": {
      "type": "string",
      "description": "Brief description of the app's purpose and domain."
    },
    "vocabulary": {
      "$ref": "#/$defs/Vocabulary"
    },
    "rendering": {
      "type": "object",
      "description": "Map of renderer name to RendererRegistration. Declares the rendering components this app provides for its content formats.",
      "additionalProperties": {
        "$ref": "#/$defs/RendererRegistration"
      }
    },
    "projections": {
      "type": "array",
      "description": "Declarations of how this app's EPR atoms project into pillar SQL read-models. Each entry binds an EPR (kind, schemaKey) to a target table and column mapping, consumed by the projector controller in elohim-storage. Optional — apps that have no read-model projections (or whose projections are derived elsewhere) omit this field.",
      "items": {
        "$ref": "./pillar-projection.schema.json"
      }
    },
    "writeThrough": {
      "$ref": "#/$defs/WriteThrough"
    },
    "observation_kinds": {
      "type": "array",
      "description": "Optional. Observation kinds this app's substrate emits. See observation-kind.schema.json and observation-event-layer-design.md spec §7.",
      "items": { "$ref": "./observation-kind.schema.json" }
    },
    "signalKinds": {
      "type": "object",
      "description": "Optional. Map from signal_kind name to declaration. Signal kinds extend the FeedbackSignal vocabulary; their integrity-zome whitelist lives in feedback_signal.rs.",
      "additionalProperties": {
        "type": "object",
        "required": ["description", "target_kinds"],
        "properties": {
          "description": { "type": "string", "minLength": 1 },
          "target_kinds": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 1
          },
          "evidence_required": { "type": "boolean" },
          "standing_impact_allowed": {
            "type": "array",
            "items": { "type": "string", "enum": ["advisory", "consequential", "binding"] }
          }
        },
        "additionalProperties": false
      }
    },
    "graph": {
      "type": "object",
      "description": "Graph-native projection extension (per 2026-05-16 graph-native-projection-substrate spec). Declares domain-specific edge types, node augmentations, indexes, and named Datalog rules that extend the core EPR graph schema. Applied at manifest registration time by the Rust graph registry validator.",
      "properties": {
        "edges": {
          "type": "array",
          "description": "Domain edge type declarations. Each entry registers a new rel_type in epr_edge. Core rel_types (SUPERSEDES, etc.) are always available and need not be redeclared.",
          "items": {
            "type": "object",
            "required": ["type", "from", "to"],
            "properties": {
              "type": { "type": "string", "minLength": 1, "description": "Relation type name (e.g. 'PREREQUISITE'). Must be uppercase by convention. Must not collide with rel_types declared by other manifests." },
              "from": { "type": "string", "minLength": 1, "description": "Source node type (e.g. 'EprHead', 'ContributorDID', or a domain-declared node type)." },
              "to": { "type": "string", "minLength": 1, "description": "Target node type." },
              "indexed": { "type": "boolean", "default": false, "description": "Whether to create an epr_edge index for this rel_type at registration time." },
              "directional": { "type": "boolean", "default": true, "description": "Whether the edge is directional. Bidirectional edges are stored as two directed edges." },
              "weighted": { "type": "boolean", "default": false, "description": "Whether this edge carries a numeric weight (future; not applied in current engine)." },
              "temporal": { "type": "boolean", "default": false, "description": "Whether this edge carries bitemporal validity semantics (future)." },
              "description": { "type": "string", "description": "Human-readable description of what this edge type represents." }
            },
            "additionalProperties": false
          }
        },
        "nodes": {
          "type": "array",
          "description": "Domain-specific node augmentation declarations. Creates a CozoDB relation named '{manifest_name}_{type}' holding domain-specific properties for EPR nodes.",
          "items": {
            "type": "object",
            "required": ["type", "properties"],
            "properties": {
              "type": { "type": "string", "minLength": 1, "description": "Node type name (e.g. 'LearningPath'). Combined with manifest name as relation name." },
              "properties": {
                "type": "object",
                "description": "Map of property name to CozoDB type string (e.g. 'String', 'Int', 'Float', 'Bool'). Keys become columns in the created relation.",
                "additionalProperties": { "type": "string" },
                "minProperties": 1
              }
            },
            "additionalProperties": false
          }
        },
        "indexes": {
          "type": "array",
          "description": "Index declarations for domain edge types. Applied via CozoDB '::index create' at registration time. Only column-based indexes are supported (no partial/filtered indexes).",
          "items": {
            "type": "object",
            "required": ["name", "on"],
            "properties": {
              "name": { "type": "string", "minLength": 1, "description": "Index name. Will be created as '{on}:{name}' in CozoDB." },
              "on": { "type": "string", "minLength": 1, "description": "Relation to index (e.g. 'epr_edge')." },
              "where": { "type": "string", "description": "Informational only — CozoDB does not support partial indexes. Stored for documentation." },
              "order_by": { "type": "string", "description": "Informational only — column ordering is declared via the columns array. Stored for documentation." }
            },
            "additionalProperties": false
          }
        },
        "rules": {
          "type": "array",
          "description": "Named Datalog rule declarations. Rules are stored in-memory by the graph registry and inlined into queries that invoke them. Rule names MUST NOT shadow core primitives: neighborhood, path, reach_filtered, version_chain.",
          "items": {
            "type": "object",
            "required": ["name", "datalog"],
            "properties": {
              "name": { "type": "string", "minLength": 1, "description": "Rule name. Used as the Datalog rule head. Must be unique across all registered manifests." },
              "description": { "type": "string", "description": "Human-readable description of what this rule computes." },
              "datalog": { "type": "string", "minLength": 1, "description": "Datalog rule body. Variables in rule bodies use plain identifiers (no ? prefix); ? prefix is only valid in final query heads." }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "$defs": {
    "Vocabulary": {
      "title": "Vocabulary",
      "description": "The app's content vocabulary — the types, formats, relationships, and signals it declares. The protocol validates structural coupling; the app owns the semantic meaning of each entry.",
      "type": "object",
      "required": ["contentTypes", "observations"],
      "properties": {
        "contentTypes": {
          "type": "object",
          "description": "Map of content type name to ContentTypeDeclaration. Each type must declare its three-leg coupling.",
          "additionalProperties": {
            "$ref": "#/$defs/ContentTypeDeclaration"
          },
          "minProperties": 1
        },
        "contentFormats": {
          "type": "object",
          "description": "Map of format name to ContentFormatDeclaration. Declares how content is serialized and which renderer handles it.",
          "additionalProperties": {
            "$ref": "#/$defs/ContentFormatDeclaration"
          }
        },
        "relationships": {
          "type": "object",
          "description": "Map of relationship name to RelationshipDeclaration. Declares the graph edges this app uses between content types.",
          "additionalProperties": {
            "$ref": "#/$defs/RelationshipDeclaration"
          }
        },
        "signals": {
          "type": "object",
          "description": "Map of signal name to SignalDeclaration. Declares the substrate signals this app emits, each mapped to an economic action.",
          "additionalProperties": {
            "$ref": "#/$defs/SignalDeclaration"
          }
        },
        "observations": {
          "type": "object",
          "description": "Map of observation name to ObservationDeclaration. Declares the feedback evidence vocabulary. Must include at least one negative-polarity observation — systems cannot ship positive-only feedback.",
          "additionalProperties": {
            "$ref": "#/$defs/ObservationDeclaration"
          },
          "minProperties": 1
        }
      },
      "additionalProperties": false
    },
    "ContentTypeDeclaration": {
      "title": "ContentTypeDeclaration",
      "description": "Declaration of a content type within the app vocabulary. Every content type must declare its three-leg coupling — how knowledge, value, and governance attach to it.",
      "type": "object",
      "required": ["description", "coupling"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of what this content type represents in the app's domain."
        },
        "coupling": {
          "$ref": "#/$defs/ThreeLegCoupling"
        },
        "metadataSchema": {
          "type": "object",
          "description": "JSON Schema defining the metadata shape for this content type. Enables typed metadata access and codegen. Can be inline or use $ref to a companion schema file. The protocol validates this is a valid schema; the app validates its content."
        }
      },
      "additionalProperties": false
    },
    "ThreeLegCoupling": {
      "title": "ThreeLegCoupling",
      "description": "The three-leg coupling contract for a content type, with required feedback claims. Every EPR carries lamad (knowledge) + shefa (value) + qahal (governance) dimensions, and must declare what outcomes it claims to produce. No value-blind content. No governance-free content. No unobserved claims.",
      "type": "object",
      "required": ["value", "governance", "claims"],
      "properties": {
        "knowledge": {
          "$ref": "#/$defs/KnowledgeLeg"
        },
        "value": {
          "$ref": "#/$defs/ValueLeg"
        },
        "governance": {
          "$ref": "#/$defs/GovernanceLeg"
        },
        "claims": {
          "type": "array",
          "description": "Claims about what outcomes this content type produces. Every content type must assert at least one outcome and declare what would contradict it.",
          "items": {
            "$ref": "#/$defs/ClaimDeclaration"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "KnowledgeLeg": {
      "title": "KnowledgeLeg",
      "description": "Knowledge dimension of three-leg coupling. Declares how this content type relates to other content in the knowledge graph.",
      "type": "object",
      "properties": {
        "relationships": {
          "type": "object",
          "description": "Map of relationship name to an array of target content type names. Declares which graph edges this content type participates in.",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    "ValueLeg": {
      "title": "ValueLeg",
      "description": "Value dimension of three-leg coupling. Declares the economic flows triggered by interactions with this content type. Maps to REA (Resource-Event-Agent) economic actions.",
      "type": "object",
      "properties": {
        "onConsume": {
          "$ref": "#/$defs/ValueFlowEvent",
          "description": "Economic flow triggered when a learner consumes this content (reads, watches, listens)."
        },
        "onComplete": {
          "$ref": "#/$defs/ValueFlowEvent",
          "description": "Economic flow triggered when a learner completes this content (finishes assessment, reaches end)."
        },
        "onContribute": {
          "$ref": "#/$defs/ValueFlowEvent",
          "description": "Economic flow triggered when a steward contributes this content (creates, curates, reviews)."
        }
      },
      "additionalProperties": false
    },
    "ValueFlowEvent": {
      "title": "ValueFlowEvent",
      "description": "A single economic flow event, mapped to a REA economic action. Declares what happens in the value network when this event fires.",
      "type": "object",
      "required": ["action"],
      "properties": {
        "action": {
          "type": "string",
          "description": "REA economic action type (e.g. 'use', 'consume', 'produce', 'transfer', 'cite')."
        },
        "resourceConformsTo": {
          "type": "string",
          "description": "ResourceSpecification that the affected resource conforms to. References a protocol-level resource spec."
        },
        "recognition": {
          "type": "string",
          "description": "How this economic event is recognized in the value network (e.g. 'mastery-credit', 'stewardship-standing', 'contribution-record')."
        }
      },
      "additionalProperties": false
    },
    "GovernanceLeg": {
      "title": "GovernanceLeg",
      "description": "Governance dimension of three-leg coupling. Declares how this content type is governed — its visibility reach, governance model, and the signals that can act on it.",
      "type": "object",
      "required": ["defaultReach", "minimumReach", "governanceModel"],
      "properties": {
        "defaultReach": {
          "type": "string",
          "description": "Default reach level for new content of this type. Must be a valid reach value (private, self, intimate, trusted, familiar, community, public, commons)."
        },
        "minimumReach": {
          "type": "string",
          "description": "Minimum reach level — content of this type cannot be more restrictive than this. Ensures governance-required visibility."
        },
        "governanceModel": {
          "type": "string",
          "description": "Governance model applied to this content type (e.g. 'steward-consent', 'community-vote', 'constitutional')."
        },
        "signalTypes": {
          "type": "array",
          "description": "Signal types that can act on this content type. References signal names declared in vocabulary.signals.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "ClaimDeclaration": {
      "title": "ClaimDeclaration",
      "description": "A claim about what outcome a content type produces. Every claim declares what it asserts, what would contradict it, and how long it's presumed valid. Accumulated positive observations extend the validity horizon; accumulated negative observations shorten it.",
      "type": "object",
      "required": ["asserts", "contradictedBy", "validityHorizon"],
      "properties": {
        "asserts": {
          "type": "string",
          "description": "Observation term this content type claims to produce. References an observation name in vocabulary.observations."
        },
        "contradictedBy": {
          "type": "string",
          "description": "Observation term that would undermine this claim. References an observation name in vocabulary.observations. Should have negative polarity."
        },
        "validityHorizon": {
          "type": "string",
          "description": "ISO 8601 duration. How long the claim is presumed valid without fresh evidence.",
          "pattern": "^P"
        },
        "leg": {
          "type": "string",
          "enum": ["knowledge", "value", "governance"],
          "description": "Which coupling leg this claim relates to. Optional — for documentation and query filtering."
        }
      },
      "additionalProperties": false
    },
    "ObservationDeclaration": {
      "title": "ObservationDeclaration",
      "description": "Declaration of an observation term — something the system watches for. Observations have polarity: positive observations extend claim validity horizons, negative observations shorten them. The protocol requires at least one negative-polarity observation per manifest.",
      "type": "object",
      "required": ["description", "instrument", "polarity"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of what this observation means."
        },
        "instrument": {
          "$ref": "../enums/instrument-archetype.schema.json",
          "description": "Instrument archetype that produces this observation. References a protocol-defined archetype."
        },
        "polarity": {
          "$ref": "../enums/observation-polarity.schema.json",
          "description": "Positive observations extend claim validity. Negative observations shorten it."
        }
      },
      "additionalProperties": false
    },
    "ContentFormatDeclaration": {
      "title": "ContentFormatDeclaration",
      "description": "Declaration of a content format — how content is serialized and rendered. Maps to a renderer and declares MIME types and file extensions.",
      "type": "object",
      "required": ["description"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of this format."
        },
        "renderer": {
          "type": "string",
          "description": "Reference to a renderer name declared in the manifest's rendering section."
        },
        "mimeTypes": {
          "type": "array",
          "description": "MIME types associated with this format.",
          "items": {
            "type": "string"
          }
        },
        "extensions": {
          "type": "array",
          "description": "File extensions associated with this format (without leading dot).",
          "items": {
            "type": "string"
          }
        },
        "aliases": {
          "type": "array",
          "description": "Alternative names for this format, for backward compatibility or convenience.",
          "items": {
            "type": "string"
          }
        },
        "bodySchema": {
          "type": "object",
          "description": "JSON Schema defining the contentBody shape for this format. Enables typed body parsing and codegen. Used to generate body interfaces (e.g. EprCompositeBody for epr-composite format)."
        }
      },
      "additionalProperties": false
    },
    "RelationshipDeclaration": {
      "title": "RelationshipDeclaration",
      "description": "Declaration of a relationship type — a directed edge in the content graph. Declares which content types can participate as source and target.",
      "type": "object",
      "required": ["description", "sourceTypes", "targetTypes"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of what this relationship means."
        },
        "sourceTypes": {
          "type": "array",
          "description": "Content type names that can be the source (from) of this relationship.",
          "items": {
            "type": "string"
          }
        },
        "targetTypes": {
          "type": "array",
          "description": "Content type names that can be the target (to) of this relationship.",
          "items": {
            "type": "string"
          }
        },
        "inverse": {
          "type": "string",
          "description": "Name of the inverse relationship, if declared. E.g. 'contains' has inverse 'belongs_to'."
        }
      },
      "additionalProperties": false
    },
    "SignalDeclaration": {
      "title": "SignalDeclaration",
      "description": "Declaration of a signal type — an event emitted by app interactions that maps to a substrate signal and economic action. Signals are the bridge between user activity and the value/governance network.",
      "type": "object",
      "required": ["description", "substrateSignal"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of what this signal represents."
        },
        "substrateSignal": {
          "$ref": "../enums/substrate-signal.schema.json",
          "description": "The substrate-level signal category this maps to. Determines which resource dimension is affected."
        },
        "economicAction": {
          "type": "string",
          "description": "REA economic action triggered by this signal (e.g. 'use', 'consume', 'produce')."
        },
        "resourceType": {
          "type": "string",
          "description": "ResourceSpecification this signal acts upon. References a protocol-level resource spec."
        }
      },
      "additionalProperties": false
    },
    "RendererRegistration": {
      "title": "RendererRegistration",
      "description": "Registration of a rendering component that can display content in specific formats. Maps content formats to UI components.",
      "type": "object",
      "required": ["component", "formats"],
      "properties": {
        "component": {
          "type": "string",
          "description": "Component identifier — web component tag name or framework component reference."
        },
        "formats": {
          "type": "array",
          "description": "Content format names this renderer can handle. References format names from vocabulary.contentFormats.",
          "items": {
            "type": "string"
          },
          "minItems": 1
        },
        "platform": {
          "type": "string",
          "description": "Target platform for this renderer (e.g. 'web', 'tauri', 'universal'). Defaults to 'universal' if omitted."
        }
      },
      "additionalProperties": false
    },
    "WriteThrough": {
      "title": "WriteThrough",
      "description": "Per-pillar write-through flag declaration (EPR Phase 2B Batch C). When `enabled` is true, signals from this pillar that pass through `/api/v1/signal/emit` are composed into EPR Envelopes, signed, and ingested via EprService. When false (default when absent), signal-emit returns 503 for non-integrity kinds. The 4-layer override stack (manifest -> policy.toml -> env -> admin override) lets operators ramp pillars on/off without redeploys; this manifest field is layer 1 (the pillar's declared default). Integrity kinds (KeyRotation, KeyRevocation, RevocationAttestation, AgentPeerBinding) bypass the flag entirely — they are always written through, hardcoded in storage. Per memory pin project_cadence_archetype_tunable_with_dev_overrides for the 4-layer pattern; per memory pin project_elohim_active_observed_not_flagged the effective-state view reports observed activity, not just configured intent.",
      "type": "object",
      "required": ["enabled"],
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether this pillar's signals are written through to EPR storage by default. Initial recommendation: false. Operators ramp on per pillar via env/CLI/admin override (layers 2-4)."
        },
        "kinds": {
          "type": "array",
          "description": "Optional list of EPR kinds (e.g. 'EconomicEvent', 'Observation') restricting write-through to a subset. When absent or empty, applies to all kinds the pillar emits. Integrity kinds always bypass and ignore this list.",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        }
      }
    }
  }
}
