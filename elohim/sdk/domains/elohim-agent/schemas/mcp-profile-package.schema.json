{
  "$id": "elohim-agent:schema:package:mcp-profile",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "McpProfilePackage",
  "description": "Explicit activation profile that composes reusable MCP server packages into project-scoped runtime registrations.",
  "type": "object",
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "scope",
    "serverRefs",
    "projections"
  ],
  "properties": {
    "apiVersion": { "const": "elohim-agent/v1alpha1" },
    "kind": { "const": "McpProfilePackage" },
    "metadata": {
      "type": "object",
      "required": [
        "id",
        "name",
        "version",
        "description",
        "sourceRuntime",
        "master",
        "runtimeTargets",
        "governance"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$" },
        "name": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$" },
        "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
        "description": { "type": "string", "minLength": 1 },
        "sourceRuntime": { "const": "elohim-agent" },
        "master": { "const": "package" },
        "runtimeTargets": {
          "type": "array",
          "items": { "enum": ["claude", "codex"] },
          "minItems": 1,
          "uniqueItems": true
        },
        "governance": {
          "type": "object",
          "required": ["eprRef"],
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },
    "scope": { "const": "project" },
    "serverRefs": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$" },
      "minItems": 1,
      "uniqueItems": true
    },
    "runtimeSettings": {
      "type": "object",
      "description": "Optional non-MCP runtime configuration this profile also owns in its projected runtime file (top-level TOML keys for codex). Absent means the projection is unchanged.",
      "properties": {
        "codex": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "oneOf": [
              { "type": "string" },
              { "type": "number" },
              { "type": "boolean" },
              { "type": "array", "items": { "type": "string" } }
            ]
          }
        }
      },
      "additionalProperties": false
    },
    "projections": {
      "type": "object",
      "required": ["claude", "codex"],
      "properties": {
        "claude": {
          "type": "object",
          "required": ["path"],
          "properties": { "path": { "const": ".mcp.json" } },
          "additionalProperties": false
        },
        "codex": {
          "type": "object",
          "required": ["path"],
          "properties": { "path": { "const": ".codex/config.toml" } },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
