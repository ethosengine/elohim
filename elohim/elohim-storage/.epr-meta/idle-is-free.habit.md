---
epr-habit-version: 1
id: idle-is-free
invariant: >
  A node at rest is quiet. With nothing being authored, read or repaired, a storage peer asks
  its conductor for almost nothing and the conductors leave the processor alone: background
  work is driven by what changed, remembers what it has already settled, and backs off from
  what keeps failing, so the cost of a quiet hour does not depend on how much history the node
  holds. Work done on behalf of nobody is a defect, whatever it was meant to keep in order.
status: red
active: false
checks:
  - "a2o @concern:idle-is-free (genesis/a2o/features/dataplane/household-at-rest.feature — @act:i, the household lane: `just test mesh features/dataplane/household-at-rest.feature`). Two snapshots 300 s apart on a settled household with nobody acting: each storage peer's conductor calls (admission permits released) at most 6 a minute, no permit refused, and the three conductors together at most 10 CPU seconds a minute. The budgets are the target, not a reading."
refs:
  - "genesis/data/timeline/backlog/conductor-cap-grant-scan-per-zome-call.md — what unbounded per-call cost did to the fleet"
  - "genesis/data/timeline/backlog/conductor-admission-saturated-for-hours-after-restart.md"
  - "genesis/data/timeline/backlog/projection-reconcile-actionable-sawtooth.md — the same ~50 items rediscovered every 20–30 minutes and never healed"
  - "elohim/holochain/.epr-meta/zome-call-cost-bounded.habit.md — the sibling: what ONE call costs; this habit is how MANY calls nobody asked for"
retire-when: >
  when every background loop in the node declares its trigger (an event, a change set or a
  bounded timer) and its settled-state memory by construction — so that a loop which re-asks
  what it already knows cannot be written — and a running node reports its own idle cost
  against a declared ceiling, making a separate household rehearsal redundant.
---
DELTA 2026-09-19 (declared RED with its check): first reading, household of three holding 27 pieces of content, settled 5 min, nobody acting, 10-minute window (genesis/a2o/reports/recovery/serving-edge-20260919/idle-baseline.log): conductor calls a minute — matthew 49.7, jessica 49.3, james 74.0 (budget 6); permits refused 0; conductor CPU 87.6 s/min — about one and a half cores pinned — storage 13.3, doorway 0.9 (budget 10). Nearly all calls are `content_store`. The storage log over the same window names the producers: projection reconcile "head unchanged — refreshed row" ×79, the same STATE-DIVERGENT commitment re-reported ×124, `apply_delta failed — page held for retry` ×63, and "serving local inventory" ×270 — reconciliation that re-derives a settled world each sweep. Born of the alpha stall, where all seven conductors sat at their CPU limit while serving no one.
DELTA 2026-09-20 (RED, the cost decomposed; one producer still unnamed and the reason it is unnamed is itself the finding): a read-only trace from the admission gate outward (genesis/a2o/reports/recovery/serving-edge-20260919/idle-producers.md) accounts for the whole reading. The floor is ~13 calls/min from timers, heartbeat and the sweep's batched resolve; 81 % of the cost is a burst on a ~315 s period that, on matthew, re-authored the same 46 rows six times over (276 distinct action hashes) — every one a device-health attestation left behind by 23 past runs of the deliverability fixture — while the write that would record each success failed 90× with `database is locked`, the same lock behind all 63 `apply_delta failed` lines. "Serving local inventory" costs no conductor call and is working as designed. A live re-measure (writer-samples.log, 17 samples at 30 s) saw a 159-call `content_store` burst with no labelled chain write and no INFO line beside it: the gate counts calls by zome, never by function, so the producer of the largest burst cannot be named from any metric or default log. chain / between `admission gate counts a call` → `the story fails on the count` / missing node: `a resting node's conductor calls are attributable to a named function and caller class` (probe: `elohim_conductor_admission_hold_ms_count` carries `fn`; the story prints the top five on failure) / current state: absent. Also read against the wrong clock: the household sweeps every 30 s where production sweeps every 300 s, and the bridge probe's 12 `app_info` round-trips a minute per node bypass the gate and are invisible to this check.
DELTA 2026-09-20b (RED, and now it names itself): the missing node is minted — `elohim_conductor_calls_total{zome,fn,class}`, counted at the one door every zome call takes (`hc_client::admit`, pinned by a source-scan test), with the ungated `app_info` probes counted as `class="ungated"`; the story prints each over-budget peer's top five callers. First run (genesis/a2o/reports/recovery/serving-edge-20260919/household-at-rest-named.log, 300 s, settled household): matthew 98.6, jessica 94.0, james 135.2 calls/min — and ~80 % of it is ONE function, `content_store::get_rea_commitment`, ×405 / ×387 / ×594, on the INTERACTIVE lane: the REA heal leg re-reading every divergent commitment from its own conductor each 30 s sweep, reaching the same `NoAdvance` / `conductor-behind` verdict, adjudicating it in a per-sweep tracker, and forgetting it. The earlier read-only trace had estimated this loop at ≤4/min and flagged it as inference; the measure overturned the estimate in one run. Second: `app_info` ×65 (the bridge probe, invisible until now). Third: `get_feedback_signal_refs_for_target` ×40. The ~315 s attestation re-author burst did not appear in this window. Next: the REA heal leg remembers a verdict against the evidence that produced it, and moves to the Background lane.
