---
epr-habit-version: 1
id: idle-is-free
invariant: >
  A node at rest is quiet. With nothing being authored, read or repaired, a storage peer asks
  its conductor for almost nothing and the conductors leave the processor alone: background
  work is driven by what changed, remembers what it has already settled, and backs off from
  what keeps failing, so the cost of a quiet hour does not depend on how much history the node
  holds. Work done on behalf of nobody is a defect, whatever it was meant to keep in order.
status: red
active: false
checks:
  - "a2o @concern:idle-is-free (genesis/a2o/features/dataplane/household-at-rest.feature — @act:i, the household lane: `just test mesh features/dataplane/household-at-rest.feature`). Two snapshots 300 s apart on a settled household with nobody acting: each storage peer's conductor calls (admission permits released) at most 6 a minute, no permit refused, and the three conductors together at most 10 CPU seconds a minute. The budgets are the target, not a reading."
  - "store growth, per DNA, read from a STOPPED conductor (`app/elohim-app/scripts/conductor-store-sniff.py <data-root> --json`, folded as `dna-actions-per-day@1`, `wal-main-ratio@1`, `subject-anchor-links@1`) and rendered by `epr flow report --bound <id>` for each of `dna-actions-per-day-ceiling@1`, `wal-main-ratio-ceiling@1` and `subject-anchor-links-ceiling@1`: lamad at most 5,000 actions a day per peer at idle, WAL no larger than its database, at most 100 attestation links on one subject anchor. Fleet: the conductor's integrated-ops series is a proxy for the first; nothing on the fleet reports WAL or anchor size."
refs:
  - "genesis/data/timeline/backlog/conductor-cap-grant-scan-per-zome-call.md — what unbounded per-call cost did to the fleet"
  - "genesis/data/timeline/backlog/conductor-admission-saturated-for-hours-after-restart.md"
  - "genesis/data/timeline/backlog/projection-reconcile-actionable-sawtooth.md — the same ~50 items rediscovered every 20–30 minutes and never healed"
  - "elohim/holochain/.epr-meta/zome-call-cost-bounded.habit.md — the sibling: what ONE call costs; this habit is how MANY calls nobody asked for"
retire-when: >
  when every background loop in the node declares its trigger (an event, a change set or a
  bounded timer) and its settled-state memory by construction — so that a loop which re-asks
  what it already knows cannot be written — and a running node reports its own idle cost
  against a declared ceiling, making a separate household rehearsal redundant.
---
DELTA 2026-09-24 (RED; now felt by a sensor, not by the operator): store growth declared as three teeth-free measures with bounds, all folded red on the stopped household (genesis/docs/content/elohim-protocol/architecture/2026-09-24-conductor-store-growth-report.md). Lamad took 46,656 actions a day at idle (648 Create + 1,296 CreateLink an hour; ceiling 5,000). The matthew WAL is 4.37× its database (1,407 / 322 MB; ceiling 1). The hottest attestation subject anchor has 600+ links (ceiling 100). 27,317 of 30,787 lamad entries are device-health attestations about 179 doorways, 176 of them dead. No status flip: the cure is plan Lane H (H1 roster expiry, H2 samples off the DHT, H3 fork checkpointing), and a reading after it is what can flip this habit.
DELTA 2026-09-20f (RED, live proof refused honestly): the strengthened household check exited 1 before its 300-second window because Jessica's lamad zome-call readiness timed out; no idle reading or green verdict was fabricated (household-at-rest-resource-witness.log). Original 19 apps and 85/5/5 running cells were restored, diagnostic exporters removed, and the mesh lease released at 15:27Z. A separate guarded post-recovery capture measured 542 CPU-seconds in 61.744 seconds, not an idle-certified window; its requested profile size cap invalidated Matthew's truncated stack capture while preserving full-window OS counters. Final owning gate and 18 focused tests pass. Evidence: genesis/a2o/reports/recovery/serving-edge-20260919/CPU-resource-investigation.md. Habit projection was attempted but refused duplicate declarations under other sessions' nested worktrees; the generated register was not hand-edited.
DELTA 2026-09-20e (RED; process cost is now a fail-loud witness): the household check attaches before/after CPU, RSS and Linux I/O counters with exact conductor configuration/executable/PID/start-time identity, clock and reboot validation; missing processes, restarts, missing metrics and counter rollback fail instead of becoming zero. One combined assertion reports both call and CPU budget failures, so a call failure cannot skip processor accounting. Focused resource tests and typecheck passed. The warm hosted household measured 464.54 CPU-s/min over 300 s (not certified quiescent); a separate 60 s userspace CPU profile attributes 38.275% of sampled self time to SQLCipher crypto and 24.041% to SQLite. These are not statement wall times or a causal agent-count law. A reusable bounded, opt-in profiler lives at genesis/a2o/scripts/resource-profile.ts; evidence and unresolved instrumentation boundaries are in genesis/a2o/reports/recovery/serving-edge-20260919/CPU-resource-investigation.md. No budget was raised and no green claim is made. chain / between `CPU budget fails` → `a bounded background path earns its budget` / missing node: `workflow trigger and SQL work are attributable to the initiating cell without unbounded tracing` (probe: trigger/zero-work counts, queue/service time, normalized statement timing and rows visited across two controlled population sizes) / current state: existing workflow exports lack cell/trigger attribution; statement logs are slow-query-only.
DELTA 2026-09-19 (declared RED with its check): first reading, household of three holding 27 pieces of content, settled 5 min, nobody acting, 10-minute window (genesis/a2o/reports/recovery/serving-edge-20260919/idle-baseline.log): conductor calls a minute — matthew 49.7, jessica 49.3, james 74.0 (budget 6); permits refused 0; conductor CPU 87.6 s/min — about one and a half cores pinned — storage 13.3, doorway 0.9 (budget 10). Nearly all calls are `content_store`. The storage log over the same window names the producers: projection reconcile "head unchanged — refreshed row" ×79, the same STATE-DIVERGENT commitment re-reported ×124, `apply_delta failed — page held for retry` ×63, and "serving local inventory" ×270 — reconciliation that re-derives a settled world each sweep. Born of the alpha stall, where all seven conductors sat at their CPU limit while serving no one.
DELTA 2026-09-20 (RED, the cost decomposed; one producer still unnamed and the reason it is unnamed is itself the finding): a read-only trace from the admission gate outward (genesis/a2o/reports/recovery/serving-edge-20260919/idle-producers.md) accounts for the whole reading. The floor is ~13 calls/min from timers, heartbeat and the sweep's batched resolve; 81 % of the cost is a burst on a ~315 s period that, on matthew, re-authored the same 46 rows six times over (276 distinct action hashes) — every one a device-health attestation left behind by 23 past runs of the deliverability fixture — while the write that would record each success failed 90× with `database is locked`, the same lock behind all 63 `apply_delta failed` lines. "Serving local inventory" costs no conductor call and is working as designed. A live re-measure (writer-samples.log, 17 samples at 30 s) saw a 159-call `content_store` burst with no labelled chain write and no INFO line beside it: the gate counts calls by zome, never by function, so the producer of the largest burst cannot be named from any metric or default log. chain / between `admission gate counts a call` → `the story fails on the count` / missing node: `a resting node's conductor calls are attributable to a named function and caller class` (probe: `elohim_conductor_admission_hold_ms_count` carries `fn`; the story prints the top five on failure) / current state: absent. Also read against the wrong clock: the household sweeps every 30 s where production sweeps every 300 s, and the bridge probe's 12 `app_info` round-trips a minute per node bypass the gate and are invisible to this check.
DELTA 2026-09-20b (RED, and now it names itself): the missing node is minted — `elohim_conductor_calls_total{zome,fn,class}`, counted at the one door every zome call takes (`hc_client::admit`, pinned by a source-scan test), with the ungated `app_info` probes counted as `class="ungated"`; the story prints each over-budget peer's top five callers. First run (genesis/a2o/reports/recovery/serving-edge-20260919/household-at-rest-named.log, 300 s, settled household): matthew 98.6, jessica 94.0, james 135.2 calls/min — and ~80 % of it is ONE function, `content_store::get_rea_commitment`, ×405 / ×387 / ×594, on the INTERACTIVE lane: the REA heal leg re-reading every divergent commitment from its own conductor each 30 s sweep, reaching the same `NoAdvance` / `conductor-behind` verdict, adjudicating it in a per-sweep tracker, and forgetting it. The earlier read-only trace had estimated this loop at ≤4/min and flagged it as inference; the measure overturned the estimate in one run. Second: `app_info` ×65 (the bridge probe, invisible until now). Third: `get_feedback_signal_refs_for_target` ×40. The ~315 s attestation re-author burst did not appear in this window. Next: the REA heal leg remembers a verdict against the evidence that produced it, and moves to the Background lane.
DELTA 2026-09-20c (RED; one cure measured, the real producer finally seen whole): the REA heal leg now remembers an unresolvable verdict against its evidence (`services/rea_verdict_backoff.rs`) and reads on the BACKGROUND lane. Measured on the household (household-at-rest-after-rea.log): the lane moved — every sweep read is `class="background"`, a person's lane is no longer borrowed — but the count did not: 401 / 383 / 586 reads in 300 s against 405 / 387 / 594 before, 8 skips. The leg's new per-sweep roll-up shows why: `to_read` is 135, 133, 133, then 0 for twelve sweeps, then 135 again. Only 2 ids ever reach a rememberable verdict; the other 133 are commitments peers advertise and this conductor cannot see (`Ok(None)`, logged at debug — silent), retried `MAX_RETRIES`=3 sweeps, then dormant for `MISS_READMIT_SWEEPS`=12 SWEEPS and re-admitted forever. The dormancy is counted in sweeps, so the household's 30 s tick turns a designed "roughly hourly" into every seven minutes: the ~315 s burst of the first reading and this burst are the same cycle. At the production 300 s tick the same 133 cost ~5 calls/min — still most of the budget, and it scales with history. Next: the miss ledger's dormancy becomes wall-clock and lengthens on repeated exhaustion against unchanged evidence (changed evidence still re-admits at once); the peer-confirmed adjudication the code names as its designed follow-up remains the real answer for ids no conductor here will ever see.
DELTA 2026-09-20d (RED; the cure is in, and its proof needs hours, not minutes): the miss ledger's dormancy is now wall-clock and lengthens — base `MISS_DORMANCY_BASE_SECONDS` 3600 s (the predecessor's documented intent, 12 × 300 s, preserved exactly), doubling per consecutive exhaustion against unchanged evidence, capped at `MISS_DORMANCY_CAP_SECONDS` 86400 s; changed evidence still re-admits at once and resets the ladder, `resolved()` still clears, exhaustion is still never permanent. The household runs the same ladder scaled 10× (360 / 8640), so the harness now measures a system proportional to production rather than one retrying ten times as eagerly. Derived for the 133 stale REA ids: production 5.3 → 0.27 calls/min/node at the cap (reached after ~32 h); household 53 → 2.7 at the cap (~3.2 h). Rung 1 is deliberately identical to the old cadence, and the ledger is in-memory, so THIS story — five minutes after a boot — will read the same until a household has been left up for hours: the check that flips this habit is a long-soak reading, with `elohim_projection_reconcile_miss_readmissions_total{stream,rung}` showing ids climbing to `capped`. Gates: storage 650 s, 0 failed; settings green. Still open beyond the ladder: `app_info` ×65 per 5 min (ungated liveness probes), `get_feedback_signal_refs_for_target` ×40, and the peer-confirmed adjudication for ids no conductor here will ever see.

DELTA 2026-09-25 (RED preserved; the H3 sensor gets its first household reading, and it is far outside the bound): store sizes measured at rest on household-dowell (pin 25dd2d0be, conductors 57 min post-launch, no lane running). Conductor `databases/` per peer: jessica 2939 MB, matthew 2369 MB, james 1594 MB; storage peer directories: matthew 7491 MB, jessica 6774 MB, james 6580 MB. One DHT space dominates every peer, and its write-ahead log dominates the space: `dht-uhC0keuLMYBe0sj…` carries a 1682 MB WAL on jessica, 1407 MB on matthew, 465 MB on james against a 338 MB main db on matthew — a `wal-main-ratio@1` reading of roughly 4:1 where the declared ceiling is 1. The H3 cure (`wal_checkpoint(PASSIVE)` after each integration batch, TRUNCATE at idle, `journal_size_limit`) is therefore no longer argued from the growth report alone; it has a household number to beat. Cost this imposes on everything else, measured the same hour: conductors held 307–370% CPU indefinitely with continuous sqlx `slow statement` warnings over these files, and an unclean container kill at ~21:28 left SQLITE_BUSY "database is locked" during shutdown — a WAL this size is what makes both of those expensive. No before/after pair yet: this is the before.

DELTA 2026-09-25b (RED preserved; the after of the WAL before/after pair, and it did not improve): after 60 min of steady state on conductor fork e0bfc6c7a (sargable arc reads, which removed the DHT-model full-table scans), the same household stores read wal/main james 1.77, matthew 4.17, jessica 3.06, against 1.12 / 4.29 / 3.08 on 25dd2d0be (ceiling 1). james's WAL grew 541 → 873 MB. The main databases were written only in the first ~90 s after boot, plus a single james checkpoint at +52 min, so with the long arc-read transactions gone, checkpoints are still not landing: the H3 cure (an explicit checkpoint cadence and `journal_size_limit`) is still owed and was not made redundant. At rest, three conductors still held 2–4 cores each (228/414/356% at +60), almost all of it per-author publish selection (plus its COUNT(*)) and ScheduledFunction cleanup repeated for every hosted agent: work done on behalf of nobody, for 0–11 rows a call. The same hour's prologue lost two source-chain writes to SQLITE_BUSY `database is locked`. Record: genesis/a2o/reports/recovery/k0-window-e0bfc6c7a-vs-25dd2d0be.md.
