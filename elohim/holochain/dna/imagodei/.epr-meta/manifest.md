---
epr-meta-version: 1
id: imagodei-dna-governance
purpose: >
  The identity DNA: agents, relationships, collectives, and the stewardship entry types
  (`StewardshipGrant`, `StewardshipAppeal`, `DevicePolicy`, `Collective`, `Membership`,
  `CollabAgreement`) where one party's authority over another party's account is modeled. This
  manifest hosts the custodial-authority habit atom. It carries no author-time rule: the
  authoring discipline for this seam lives where the scenarios are written
  (`genesis/a2o/features/stewardship/.epr-meta`), and duplicating it here would be a second home
  for one rule.
---
# imagodei DNA — governance package

The habit declared here (`*.habit.md`) describes what this DNA's stewardship model must reliably
do. Its checks will live in `genesis/a2o/features/stewardship/` once they exist — which is
precisely why the habit is `unwired` today rather than absent.

Projected into `genesis/manifests/habits.yaml` by `.claude/scripts/habits-project.py`.
Edit the atom, never the projection.
