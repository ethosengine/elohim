//! Intentionally empty library target — the real code is in `tests/`.
//!
//! This crate exists solely to host fast integration tests over the DNA
//! manifest YAML files. No runtime, no conductor, no holochain dep — pure
//! YAML parsing so husky can run it in milliseconds.
