{"timestamp":"2026-03-02","area":"doorway,shefa","intent":"Doorway nodes as DePIN CDN for static assets (fonts, icons, sophia UMD, HTML5 app ZIPs). Blobs are already content-addressed and served via /store/{hash}. Instead of build-time bundling, treat static assets as network content. Hybrid: bundle critical icon subset for bootstrap, load full set from doorway post-connection with SW caching. Shefa accounts for blob-serving bandwidth as measurable work. Prompted by font 404 bug in CI where fetch-fonts.sh was skipped.","a2o_feature":"genesis/a2o/features/content/blob-serving.feature"}
{"timestamp":"2026-03-02","area":"doorway-app,imagodei,orchestrator","intent":"Doorway dashboard 'Nodes' tab should always show 'this node' — the hardware the doorway is running on. This is device self-registration (imagodei concern), not orchestrator. The orchestrator is a separate elohim subagent that manages hardware-level concerns — on a cluster it balances compute across nodes (like a P2P k8s operator consuming shefa resource stats), but even on a single box it runs diagnostics, monitors disk/memory/thermal health, and could recommend hardware swaps. Same agent, different action space depending on topology. A doorway should be self-aware of its own node regardless. The health endpoint already has the data (conductor pools, workers). Clarify boundary: self-awareness = imagodei/doorway, hardware stewardship = orchestrator subagent (useful at any scale).","a2o_feature":"genesis/a2o/features/auth/device-registration.feature"}
