---
name: project_brit_next_gen_epr_meta_foundation
title: brit next-gen epr-meta foundation + composition-snapshot
description: "Content-addressed epr-meta in brit supersedes the parent Python cite tooling; parity PROVEN vs the live oracle, integrated to brit main."
metadata: 
  node_type: memory
  type: project
  originSessionId: 0e51e175-a51c-4846-a3cd-f9961dedf905
---

The `brit` submodule (`elohim/brit`, a gitoxide fork = "covenantal VCS") is the greenfield for a **next-generation, content-addressed `epr-meta`** meant to **supersede** the parent's Python discipline (`epr_meta.py`/`cite_graph.py`/`placement-audit`) — "master in brit, bring back to the whole." Branch **`brit-dev`** (foundation + composition-snapshot slice now **INTEGRATED to brit `main` 2026-06-30** — see dated update at end; legacy plan was: operator owns brit-dev→brit main + the monorepo submodule-pointer bump).

**Design** (`elohim/brit/docs/specs/2026-06-29-canonical-epr-meta-git-bridge-design.md`): canonical-first, **git-as-bridge** (git = external format reached via an internal bridge → enables cmd-by-cmd migration); `EprMeta`/`NodeSeed` content-addressed artifacts; two-layer notarization (static CID identity + runtime-supplied blessing); WIT-world-shaped import/export contract; **recomposition bridge** = the clean-sheet completeness test (must re-compose the parent's `.epr-meta`+cites+MEMORY via a script — schema gap if it can't).

**Foundation landed + independently verified green** (isolated worktree at HEAD: build/test/clippy/`--no-default-features`/byte-parity all exit 0): brit consumes **`elohim-epr = "0.1"`** from the `elohim` Nexus registry (anonymous read; resolves via the `[source.crates-io]`→mirror redirect, NOT the vestigial `[registries.elohim]` stanza). `BritCid` is now real **CIDv1·0x71 dag-cbor·0x12 sha2-256** (was blake3-hex), byte-identical to `elohim-epr` (golden-vector conformance test). `EprMeta`/`NodeSeed` use typed `BritCid` **tag-42 IPLD links** (not string FKs). `brit meta seal/verify` CLI works (sealed a live `bafyrei…`). BLAKE3 retained for non-address fingerprints only.

**Composition-snapshot cite-discipline slice LANDED 2026-06-29** (brit-dev `fb3495ce81..a4ac3f43ad`, 12 tasks via subagent-driven-development; phase summary `4185b9a0dd` = `docs/plans/phases/2026-06-29-epr-meta-composition-snapshot-phase-summary.md`; whole-branch review **ready-to-merge, 0 blockers**; INTEGRATED to main 2026-06-30, clean FF). The **parity-supersession thesis is now PROVEN**: brit's `engine::frontmatter::drift_fingerprint` (`sha256:hex16` over the frontmatter-excluded trimmed body) + `engine::verdict::verdict` (`dead>held>stale>ok`, via `InterfaceRef`/`SlugIndex`/`parse_cite_line`) reproduce the parent `_lib.cite_graph` (`fingerprint` + `envelope_verdict`) BYTE/LABEL-exact across **two non-vacuous gates run against the LIVE oracle** (`tests/canonical_body_conformance.rs` 5 vectors + `tests/cite_parity.rs` ok/stale/held/dead, skip-on-absent). `EprMeta`/`NodeSeed` lifted into the generic engine with `imports`/`exports`/`sub_seeds` (Decision-A `#[serde(default)]`, NO `skip_serializing_if`; `--no-default-features` green); `seal` projects cites; `brit epr-meta status` verb + advisory PostToolUse hook are the felt surface. Parity-hardening backlog (latent, non-blocking) in the phase summary: held-matcher-on-relative-path, SlugIndex first-vs-last-wins on dup `id:`, malformed-frontmatter tolerance, hook missing `CARGO_TARGET_DIR`. Layer-2 governance (deterministic floor ↔ elohim ceiling, Dunbar-graduated stewardship, `Mishpat::Commitment` self-executing contracts w/ circuit-breaker tag-off — see [[project_rea_compute_commitment_primitive]]), the git bridge, version-DAG/signed-heads, functional edge kinds — genuinely deferred; the envelope is converged-but-unpopulated so they compose inward without a re-encode.

**Deferred follow-on plans** (each its own spec/plan): `lock` (NodeSeed composition), the git bridge, `notarize`, export/import (CAR-via-packfile + Lexicon fail-open gate), the **recompose** completeness test, and the **whole-corpus CID re-mint** (was gated on "a workable model" — now exists in foundation form).

**Punch-list (non-blocking):** vestigial `[registries.elohim]` stanza; the documented `cargo clippy --workspace` gate should be scoped to first-party crates (upstream `gix-*` carries pre-existing clippy-1.96 debt — do NOT parity-maintain); `meta seal` is immediate-files-only but its help says "subtree."

**Process lesson:** changing a shared type's API (the `BritCid`/`CborError` swap) needs a **workspace-wide consumer sweep**, not a per-crate test scope — the SDD run's per-task reviews + a too-quick "it's all upstream debt" dismissal shipped `brit-graph` broken; caught via rust-analyzer diagnostics + a real build, fixed in `b0eca4bf`. And: a concurrent session's uncommitted `brit-graph` WIP polluted the shared-tree clippy run → **verify your branch's green in an isolated worktree**, never trust a shared dirty tree. Sibling: [[feedback_concurrent_sessions_shared_worktree]].

**Counter-lesson (the composition-snapshot SDD run was clean BECAUSE the controller ground reality before each high-risk brief):** grounding the REAL parent-oracle signatures + the actual constructor sites BEFORE writing each task brief caught 4 bugs the plan would have shipped — A3/C4 oracle-call arity (`envelope_verdict` is 2-arg; `parse_cite` returns a dict; `Frontmatter.get` returns `""` for list values so cites need `.fields.get`), the B3 `None`-drift→`Stale` verdict bug (oracle returns `ok` when no fingerprint), the C1 constructor-breakage (adding non-`Default` fields broke `seal`; the plan's commit list omitted `meta_cmd.rs`), and the C4 corpus `{id}|{ref}` key-collision (ok+stale both citing `target` masked one case). File-handoff briefs (one per task, exact code + grounded facts) + fresh per-task adversarial review + a **consolidated clippy/fmt end-gate** (caught fmt drift + 1 clippy the per-task `cargo test` never lints — the [[feedback_pvc_deferral_hides_gate_debt]] shape) = 0 review blockers across all 12 tasks. The ambient shared-tree WIP was handled by **committing it as a labeled pickup first** (clean base for clean sprint commits), per the operator's "pick up that WIP" call.

**Direction note 2026-07-02 (operator, captured in-flight):** tempted to upgrade the brit epr-meta work into **eprfs** — an elohim-protocol-native FUSE filesystem extension projecting the p2p dataplane over a common filesystem, carrying the context-management/governance enhancements as *projections of the files themselves*. Seam-routing: **bridge seam** (POSIX file I/O is the external protocol — "doorway for the local filesystem"). What it buys: the compose-gate + memory discipline bind for ANY writer (vim/git/small-model agents), making born-governed emission substrate-guaranteed instead of harness-hook-guaranteed. Hard edges named at capture: (1) FUSE mounts in Che/k8s pods need privileged securityContext — operator-gated exactly where it matters most; (2) the `ask` enforcement class has no POSIX errno — likely becomes write-lands-as-*proposed*-atom + escalation signal (REA commitment-state-shaped, a design decision not a port). Design consequence adopted in the parent repo same day: rails stay declarative (`.epr-meta` rules + deterministic projectors + signal ledgers), Python hooks as thin transport, so the layer lifts into brit/eprfs without rework; MEMORY.md became a generated projection of topic-file frontmatter (the [[project_principle_p1_reconciliation_controller]] storage-as-projection principle applied to agent context).

**Update 2026-06-30 (integration + a sibling upstream-sync sprint).** epr-meta is now **integrated**: `brit-dev` fast-forwarded onto `main` (`6e788e59aa..4185b9a0dd`, +27 commits = foundation + composition-snapshot slice; `brit-epr/-build-ref/-graph/-verify/-cli` build green; pushed). Done the SAME session: the **gitoxide-parity side got a full upstream sync** to gitoxide `c31dac2d53` (the SHA-256 object-format epoch + 3 CVE-class fixes — symlink worktree-escape, packetline DoS, ref-transaction TOCTOU — 256 commits over ~2 months). `gix-main` (pristine mirror) fast-forwarded + pushed; **`gix-brit`** (the ralph-wiggum parity loop) merged upstream as a **clean building base** (merge commit `e8ec5252b8`, `cargo check --workspace` green, pushed). Strategy was **clean-base-defer-deep-features**: KEPT+adapted brit's bloom (commitgraph+blame), untracked-cache dirwalk, blocking-server/gix-serve stack, refspec match_push, and the porcelain CLI parity; **DEFERRED** (un-wired from build, code retained in-tree + history) the four deepest features that entangle with upstream's hardest-rewritten internals — **push/send_pack, the `branch` command/branch-write, partial-clone object-filter, fetch `--unshallow`** — with a re-integration checklist living in the `e8ec5252b8` commit message. All four branches (main/brit-dev/gix-main/gix-brit) pushed to ethosengine/brit; the elohim-monorepo submodule-pointer bump remains operator-owned. Merge mechanics lesson: a prior "upstream merge train" commit had bundled brit features INTO shared files, so a bulk take-theirs silently reverted them — caught only by the build (resolution errors for dropped features, then duplicate-def/missing-field cascades); the reliable classifier was "which files are brit-only (additive, keep) vs brit-modified-shared (3-way or defer)".
