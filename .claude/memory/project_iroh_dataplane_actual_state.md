---
name: project_iroh_dataplane_actual_state
title: iroh dataplane actual state — merge-ready, dual-stack gap
description: Dual ENABLED in alpha manifests 2026-08-05 (deploy pending) w/ sovereign never-n0 iroh defaults; proof = "Dual: DualGossipPublisher wired into P2PNode" + irohNodeId, NOT the degraded-wrapper log.
metadata: 
  node_type: memory
  type: project
  originSessionId: cf766280-b0c4-495e-8e66-6e752749f7f2
  modified: 2026-08-04T14:15:59.836Z
---

The iroh parallel stack (`elohim/elohim-storage/src/p2p_iroh/`, feature `p2p-iroh`) is ~85% finished — trust the tree, not the docs. Verified 2026-06-15 by 9-reader synthesis (`/tmp/iroh-synthesis/*.md`) + direct main.rs read.

**What's REAL (contra the README's "What is stubbed" §197-227, which is Phase-10-stale):**
- Crate COMPILES green `--features p2p-iroh --tests`. `IrohNode::start_with_protocols` (node.rs) binds a real Endpoint, opens iroh-blobs FsStore, spawns gossip + Router accept loop, mounts 7 custom ALPNs. Wired on the live path at `main.rs:1506-1729` ("Phase 11 … production backends wired in").
- `ELOHIM_TRANSPORT_BACKEND=iroh` starts a live data plane end-to-end; blob serve + sync + EPR-resolve + view-fed read are COMPLETE.
- Bench harness fully LANDED (9 head-to-head plane benches + 10k stress gate `bench_blob_stress_10k.rs`); perf is MEASURED loopback data: iroh wins p50 4×–290× on chatty small-frame planes, collapsing to ~1.2× once bytes dominate (shard 4MiB). DoD "perf grounded" = RUN the harness, not build it. Recovery e2e (gate #5) COMPLETE (landed d5e29fb67).

**The keystone gap — code is MODE-EXCLUSIVE, spec wants DUAL-STACK:** `TransportBackend{Libp2p,Iroh}` (no Dual). main.rs builds `P2PNode` XOR `IrohNode` at boot (libp2p slot None in iroh mode, `main.rs:1457-1461`). So `main.rs:1741 if let (Some(libp2p_n),Some(iroh_n))` is NEVER true → the built+tested `DualGossipPublisher` is **structurally dead on the live path**. A single node cannot run both swarms today. Per-object cross-plane routing therefore needs a new `Dual` boot mode (both swarms co-resident) — that's the real "finish the dual plane" work, and it lights the dead dual-publish. NOTE the blob plane is special: its "libp2p fallback" (`IrohThenLibp2p`) is a LOCAL legacy SHA256 store read, not a swarm — so per-object blob A/B works in iroh mode WITHOUT dual swarms.

**self_cid gap (lights the resilience card):** `main.rs:400-462` derives self_cid only on the Libp2p branch → Unset on iroh → provide-loop tick (`main.rs:1029-1035`) takes disabled arm → household-resilience card dark. iroh NodeId is in hand at `main.rs:1645` (`iroh_secret.public().to_string()`, key `storage_dir/iroh.key`). Fix needs 3 things: derive the value + hoist the provide-loop out of libp2p-only scope (`main.rs:1008-1144`) + make `/p2p/status .peerId` report the iroh id for the seeder join.

**iroh-mode reductions (the "replace stub backends" cutover work):** real services with bare `new()` instead of manifest-aware ctors → EPR-atom caller=Anonymous, View-fed connected_peers=empty, Identity-handshake peer-label=claimed. EPR Announce = accept-signal no-op (dual-publish gossip works around the missing Kad put_record / n0 chokepoint). Trust = fixed-response stub but parity-identical to libp2p (protocol-wide, NOT iroh-specific). Gossip RECEIVE-side over iroh entirely unwired (biggest parity hole; publish-side is live).

Why master plan held: `genesis/docs/superpowers/held/plans/2026-05-10-iroh-delivery-master.md` doc-level `requires_env:[harbor-registry, alpha-cluster-6peer]`; harbor back, alpha-cluster-6peer still blocked-by-env. Held WHOLE but MIXED: code+toggles+self_cid+bench-on-loopback are household-testable; only Wave-4 SOAK gates (#6/#7/#9) need the 6-peer cluster. Pins frozen: iroh =0.92 / iroh-blobs =0.94 / iroh-gossip =0.92 (highest on stable ed25519-dalek 2.2 / curve25519-dalek 4.1 path; 0.95+ pulls a pre-release crypto path whose published source doesn't compile). Never bump.

**SPRINT 2026-06-15 (both toggles DONE, ready for merge-prep)** — worktree `/projects/elohim/.claude/worktrees/iroh-dataplane`, branch `feat/iroh-dataplane-finish` (base `660d05b14` is already an ancestor of origin/dev → 2 commits are a CLEAN delta). `279ddf8a3` = NodeTransport seam (node-level toggle + self_cid on iroh + /p2p/status iroh peerId; 10 tests). `82d2e2538` = per-object blob `transport_affinity` (enum + additive migration + choose_backend override + /admin setter; 19 tests). libp2p byte-identical (Auto=NULL passthrough, diff-reviewed). `cargo build --features p2p-iroh` GREEN. **MERGE BLOCKER (pre-existing, NOT ours):** `cargo test --features p2p-iroh --tests` won't compile — `BlobInventorySnapshot` `hints` field (added by `8287af29d`, on base/dev) missing at `tests/iroh_gossip_dual_publish_inventory.rs:68` + `tests/iroh_gossip_byte_parity.rs:113`; fix = add `hints: vec![],` (latent because iroh tests don't run in CI). Build env: CARGO_TARGET_DIR=/tmp/iroh-target (NOT pool slot — fingerprint ENOENT), RUSTFLAGS getrandom custom, plain cargo, no codegen, surgical single-test builds (disk ~82%). Full handoff: `/tmp/iroh-synthesis/HANDOFF.md`. Deferred fast-follows: Wave-4 reductions, Tier-2 `TransportBackend::Dual` (the keystone — boot is mode-exclusive so DualGossipPublisher at main.rs:1741 is structurally DEAD).

**MERGE-PREP session 2 (2026-06-15) — adversarial 4-lens review caught 3 MORE blockers the feature-only verification missed; all FIXED.** Sprint-result doc: `genesis/docs/superpowers/sprints/2026-06-15-iroh-dataplane-toggle-sprint-result.md`. The catches: (1) **default-build break** — `handle_set_blob_transport_affinity` + its `/admin/blob-transport-affinity/` route arm were NOT `#[cfg(feature="p2p-iroh")]` but reference the gated `http_blob_router` → `E0433` in the DEFAULT (`cargo build`, no feature) build that CI/Docker ships; gated both. KEY LESSON: feature-gated verification ≠ sufficient — ALWAYS build BOTH the default feature set AND `--features p2p-iroh` before declaring a `#[cfg]`-gated change merge-ready. (2) the `hints` breakage was **5 literals across 3 files** not 2/1 — third file `iroh_gossip_cross_stack_e2e.rs:140,322`; all 5 fixed. (3) `PeerBlobInventoryRow` struct (6 fields) drifted from the 7-col table — compiled only via `.select(as_select())` but couldn't read back the column the setter writes; added `transport_affinity: Option<String>`. Tests green (`--features p2p-iroh`): 7 iroh integration + 27 toggle units + 212 schema_contract. Review-confirmed SOUND: libp2p byte-identical, full 5-variant affinity truth-table, additive migration. Fast-follow (non-blocker): iroh `/p2p/status` minimal `{"peerId"}` body not yet schema_contract-asserted as an intentional subset. **MERGE-READY** as of 5 commits on `feat/iroh-dataplane-finish` (clean delta from `660d05b14`, an ancestor of `origin/dev`): `279ddf8a3` (seam) + `82d2e2538` (affinity) + `3c0fb6b80` (hints fix) + `cc8d6327f` (gating + struct + comment) + `1f086aafd` (sprint doc). Working tree clean. Integrator owns push/merge per commit-only discipline.

**DUAL LANDED (2026-07-10, branch feat/frontend-eyes-sprint, commits ae198e1bd + 3cd8c04ac):** the keystone gap above is CLOSED. `TransportBackend::Dual` boots both swarms co-resident; the shared sync DocStore rides the libp2p node's SyncManager Arc (one sled lock, one Automerge truth); gossip receive is transport-neutral (`p2p/gossip_dispatch.rs` handler called by both the GossipsubEvent arm and the new `p2p_iroh/gossip_receive.rs` loop that keeps the previously-dropped GossipReceiver) with ONE shared DedupLru so dual delivery dedups once. Dockerfile ships `--features p2p-iroh` (cargo default features unchanged); Libp2p stays default — Dual activates per-node via `ELOHIM_TRANSPORT_BACKEND=dual`. `/p2p/status` gained optional `irohNodeId` (schema + codegen). self_cid policy in Dual: canonical=agent_cid, legacy fields keep libp2p PeerId. STILL OPEN: live 2-node dual-delivery check on alpha (receive-loop runtime efficacy unit-tested only); the iroh-mode reductions (§above) are now cross-plane-visible known-asymmetries; reach-scoped EPR announce topics remain libp2p-only.

**PIN CANNOT LIFT until the Holochain-0.7-family move (found 2026-08-04):** iroh ≥1.0 needs `serde ^1.0.228` (iroh-metrics) but everything on `holochain_serialized_bytes 0.0.56` exact-pins `serde =1.0.219`; the shared `elohim/sdk/domains/*/types` crates (`holo_hash =0.6.0`) are consumed by five DNA zome crates on the 0.6 hdk, so the escape window (`holochain_types 0.7.0-dev.25–.30`) is unreachable until zomes leave the 0.6 family. Canonical home: the convergence campaign plan (Global Constraints decision + Evidence §6). Dual-mode enablement does NOT wait — it rides the shipped `=0.92` build.

**RUNTIME VERIFIED 2026-08-04 — iroh is NOT live on alpha.** No repo manifest sets `ELOHIM_TRANSPORT_BACKEND` (the per-human template `genesis/orchestrator/manifests/humans/_edgenode-consolidated.template.yaml` omits it; the superseded `genesis/orchestrator/manifests/edgenode/alpha.yaml:286` sets `TRANSPORT_BACKEND="dual-stack"` — wrong var name AND a value the parser rejects, inert twice over). Default is Libp2p, so all 7 alpha peers run libp2p-only. TRAP: the fleet-wide `"T22: published inventory snapshot via DualGossipPublisher"` log does NOT prove dual mode — when `p2p-iroh` is compiled (always, per Dockerfile) the libp2p-only path still wraps its publisher in `DualGossipPublisher::new(Some(libp2p), None)` (main.rs ~3444, degraded fan-out). The true dual-mode startup sentinel is `"Dual: DualGossipPublisher wired into P2PNode"` (main.rs ~2907) — absent from alpha logs. Enablement is one env var in the consolidated template.

**WAVE-2 E3 ENABLED IN-REPO 2026-08-05 (deploy pending).** Manifests commit `4cb207de3`: `ELOHIM_TRANSPORT_BACKEND: "dual"` in the humans template + adam-firstman (hand-rendered — template edits alone never cover adam) + edgenode/alpha.yaml (dead dual-stack block replaced); staging/prod stay libp2p (alpha-first). Sovereignty prerequisite `f9087cfad`: `IrohConfig` defaults flipped never-n0 (`use_n0_relays=false`, `use_n0_discovery=false`, no default pkarr resolver — the old defaults would have published our endpoint addresses to n0's DNS the moment dual lit), new `relay_url` → `RelayMode::Custom` with hard-fail on bad URL, env opt-ins `ELOHIM_IROH_RELAY_URL` / `ELOHIM_IROH_USE_N0_RELAYS` / `ELOHIM_IROH_USE_N0_DISCOVERY`. Reach without n0: iroh addressing rides the Phase-12 `peer_transport_manifest` plane (LAN/direct now; WAN when `ELOHIM_IROH_RELAY_URL` points at the live self-hosted relay — design doc `2026-08-05-wave2-relay-sovereignty-design.md`, which also proved the iroh-0.92 dataplane shares the 0.95.1 relay instance wire-identically). Post-deploy verification = the true sentinel + `irohNodeId`; that closes the "STILL OPEN: live 2-node dual-delivery check" above.

Related: [[project_per_node_memory_is_conductor_authority_arc]], [[project_resilience_snapshot_humans_junction]], [[project_doorway_wedge_unbounded_mongo_await]] (parked 503 RCA).
