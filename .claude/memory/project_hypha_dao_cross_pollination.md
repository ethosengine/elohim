---
index: false
id: project-hypha-dao-cross-pollination
name: hypha-dao-cross-pollination
title: Hypha DAO cross-pollination (autonomous collectives)
description: "Hypha DHO green/red-team: same holonic shape, opposite trust root (capital- vs care-weighted); borrow mechanics, reject chain+tokens; bridges must be lossy."
metadata:
  node_type: memory
  type: project
  originSessionId: 89f7bc8d-a468-43cf-a34f-99fe587ab209
---

[Hypha DAO](https://github.com/hypha-dao) (the **DHO** = "Decentralised Human Organisation" toolkit; Telos/EOSIO → EVM) is the closest external mirror of the protocol's **autonomous-entity & collective** work (recursive-Qahal `Collective`/`Membership`/`CollabAgreement`). Full survey + green/red-team: `genesis/research/hypha-dao-autonomous-collectives-cross-pollination-2026-06-24.md` (module note: `elohim/elohim-token/research/hypha-dao-governance-token-prior-art.md`; 4 repos pinned in `research-manifest.json`). Read the doc for detail; this is the re-loadable residue.

**⚠ Name-collision triple:** `hypha-dao` (this — DAO/DHO governance tooling) ≠ `hyphacoop` (Hypha Worker Co-op — Distributed Press / fediverse, the 2026-06-23 survey) ≠ `Pointsnode/hypha-network` (unrelated, Base L2). Conflating dao↔coop is live because of our own federation vocabulary.

**The one sentence:** both designs say "the chain layer takes weight as coordination scale grows" — but Hypha's chain is **capital/stake-weighted** (Telos/EVM); the protocol's council chain ([[project_weave_epic_arc]], multi-collective §4) is **care-aggregate-weighted**. Same holonic shape, opposite trust root. Hypha *describes* anti-plutocracy while its token substrate leaks capital-as-voice; the protocol forecloses it structurally. Borrows are pattern-level — the interesting repos (`document-graph`, `voice-token`, `dao-contracts`) are LEGACY C++/EOSIO (2024); live `hypha-web` V3 hides the chain. No Holochain anywhere.

**Borrow / reject, one line per theme:**
- BORROW content-addressed-id + chain-as-projection (validates CID-as-id + [[project_principle_p1_reconciliation_controller]]; lift cursor/namespacing/selective projection); REJECT the schemaless document-graph as primary model.
- BORROW sense-making→dissent→vote lifecycle + Role/Assignment/Period/Claim vocab + "role≠soul"; REJECT token-weighted voting + majority override of constitutional floors.
- BORROW VOICE's decay *intuition* as an Operational-C recognition-decay kernel on reach/standing (`future-distribution-models.md` dir B, **no** `VoiceBalance` entry); REJECT HYPHA-as-vote-multiplier + fungible treasury + SEEDS dependency.
- REJECT global-consensus blockchain as trust root (validation by negative example).

**Two hard guards (the durable residue):**
1. **Lossy-bridge invariant** — a Hypha→Elohim bridge MUST strip transferable-token + voting-multiplier semantics; project *agreements & agents*, never *token-weighted governance*. "Maps cleanly" is wrong — maps *lossily*, and the dropped fields are the dangerous ones.
2. **Cite as governance/anti-plutocracy fellow-traveler, NOT identity-sovereignty corroboration** — Hypha's own "owned by you / human sovereignty" marketing is the exact leak [[feedback-identity-sovereignty-ontology-guard]] catches (governance-power seam ≠ identity-ontology seam).

**Three traps the adversarial pass caught (don't repeat):** an Assignment is `Membership.role` + REA recognition, NOT [[project_mishpat_commitment_cid_is_entry_hash]]'s `delegates-compute` (compute-authority only); share-sum==1.0 is coordinator-side not integrity-notarized (so the REJECT rests on the A/A2/B/B2/C gate, not "all invariants notarized"); the decay half-life is itself a governance knob needing a rent-extraction test on who sets it. Sibling fellow-traveler to [[project_polity_ethelo_outreach_thread]]; contributor-credit convergence with [[who-is-who-networking-skill-contributor-credit]].
