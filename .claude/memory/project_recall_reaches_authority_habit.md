---
name: project_recall_reaches_authority_habit
title: recall-reaches-authority habit — bounded recall graduates from eprfs to p2p
id: project-recall-reaches-authority-habit
description: "UMBRELLA for the 2026-09-11 recall work (folds: bootstrapping-is-a-recall-journey, governed-discovery-ontology-reuse, plan-checkbox-is-commitment-grain). Declared 2026-09-11 (born red→green): one governed recall journey serves ceremony and sprint doors; middot recall-{metered,unmetered}-bytes/mistaken-assertions/screens-to-shape; graduates to p2p when humans discover meaning in communities through the same inspectable artifact."
metadata:
  type: project
---

**Declared 2026-09-11** at `.epr-meta/recall-reaches-authority.habit.md`, born red, by the sprint plan
`genesis/docs/superpowers/plans/2026-09-11-bounded-recall-mastery-sprint.md`. It takes over the
verdict rule the memory-kit replacement had grafted onto `dev-system-equilibrium` (a stock-rate habit
that memory work could never flip): a context-reset agent with a concrete question reaches the
current authoritative source through `epr flow memory recall open --need …` alone, its question is the
intent, `.claude/` is inside the entry's scope, bytes are metered, no mistaken assertion.

**Middot / mishpat:** `recall-metered-bytes@1`, `recall-unmetered-bytes@1`, `recall-mistaken-assertions@1`,
`recall-screens-to-shape@1` and their `-ceiling` rows in `.claude/epr-meta/measures.yaml`;
`recall-unmetered-bytes-ceiling@1` renders into the `recall` SessionStart headline slot (the retired
`memkit` slot's successor). First sample folded: 14 screens, ~100 KB unmetered, 1 mistaken assertion.

**Graduation (operator ruling on the plan, 2026-09-11):** this concept graduates from eprfs to p2p when
humans search, surface and consume content and discover meaning in their communities through the same
kind of governable artifact, its mishpat and middot inspectable and interpretable, judged against human
flourishing for humans and agents of the larger network. Hence the invariants now: the algorithm is a
content-addressed EPRFS artifact (`.epr-meta/elohim/algorithms/recall-contract.json`) with declared
authorship and change authority; bounds are declared rows, never code constants; measures fold as
re-derivable observations; every first screen prints its selection rule and omissions.

Related: [[feedback_bootstrapping_is_a_recall_journey]], [[reference_memory_ceremony_skill]],
[[feedback_private_thought_governed_fruit]].

**Folded members (index: false; open the file):** [[feedback_bootstrapping_is_a_recall_journey]] (your own
bootstrap is the first journey sample; two doors), [[project_governed_discovery_ontology_reuse]] (recipe=ProcessSpec,
lens=AttentionTending, sample=FlowEvent+Verdict — never mint these), [[feedback_plan_checkbox_is_commitment_grain]]
(one `- [ ]` per task in a plan; steps numbered). Spec: `genesis/docs/superpowers/specs/2026-09-11-governed-discovery-journey-lens-graduation-design.md`;
plan: `genesis/docs/superpowers/plans/2026-09-11-governed-discovery-stations-0-3-plan.md`.

**2026-09-12 (governed-discovery stations 0–3 merged to dev):** the habit's check 3 became a rolling-window bound
(`recall-journey-window-ceiling@1`, 91 days, hard 0.2) fed by `recall sample`/`judge` journeys and the weekly
standing-reader workflow, and on its first reading against main's fold history the habit flipped GREEN → RED
(4 of 6 journeys positive). The latest-journey readers were green; the quarter was not. Station 4's work is the
discovery gap the standing reader surfaced (short register vocabulary like "top red", area stemming, a
reader-chosen candidate) — that, not a wider bound, is what greens it.
