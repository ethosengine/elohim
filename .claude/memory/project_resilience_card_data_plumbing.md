---
name: project_resilience_card_data_plumbing
description: "Resilience card data plumbing: snapshot joins need substrate-owned humans (agent_pub_key); tier is content-declared floor not reach; card is data-starved."
metadata: 
  node_type: memory
  title: Resilience-card data plumbing (umbrella)
  type: project
  originSessionId: a2dc3dac-b9f3-429b-89b0-d00f39aa1e9d
  modified: 2026-07-23T13:32:20.185Z
---

# Resilience-card data plumbing (umbrella)

Folds the resilience-card data-plumbing cluster. Members:

- [[project_resilience_snapshot_humans_junction]] — both snapshot() joins pass through substrate-owned humans.agent_pub_key+household_id (no HTTP create surface sets them); commitments-only seeding lights nothing.
- [[project_resilience_tier_content_declared_floor]] — content self-declares its durability FLOOR (tier), orthogonal to reach; deriving tier from reach (reach_to_resilience_tier) is the conflation to correct.
- [[project_resilience_weave_sprint_landed_data_starved]] — all 5 lens routes landed on dev 2026-06-22; the dark resilience card is DATA-STARVED (shard_locations empty, no live writer), not lens/fold-starved.

**2026-07-17 runtime-chain diagnosis (supersedes the SELF_CID-pin framing):** the runtime provide-loop was double-broken on all three M/J/J pods — (1) provider namespace: `resolve_provider` fell back to the libp2p transport id; FIXED at `2eea26671` (resolves [session key → own conductor cell key `hc.agent_key_uhcak()`], skips+metric `PROVIDE_PROVIDER_UNRESOLVED` when unresolvable — **do NOT chase SELF_CID env pins for the provider; cell-key resolution supersedes them**); (2) pin starvation: sole `acquisition_pins` writer is `POST /api/v1/pins` — nothing auto-pins seeded content, so the loop's desired set is empty every tick (a SILENT loop = starved input, not failure). Sanctioned next slice = demand-driven auto-pin on zero-peer/read-miss (self-healing-opportunity-map row 15), NOT blanket backfill. Also refuted: `SALVAGE_CAPACITY_ENABLED` is the WRONG lever for this card — salvage authors `custody-blob`, the card counts `provide` projections (the `replicates-commons`→`provide` bridge in `record_provide_from_content_commitment` handles runtime commitments). Cross-peer coverage (diversityScore, multi-steward) stays gated on mesh convergence ([[project_alpha_topology_bootstrap_pair]] / TURN — TURN went LIVE 2026-07-17).

**2026-07-17 evening — rekey-drift class CONFIRMED FLEET-WIDE and heal landed:** `peers-unavailable` root cause = DNA-reinstall rekey drift (non-prod `ALLOW_DNA_REINSTALL` mints a new agent key each DNA deploy; `humans.agent_pub_key` + session + seeded commitment providers kept fossils; the fossil's peer_statuses row freezes degraded/non-pool-member). The stale-for-self heal (genesis_self_heal, pushed `9356f767d`) fired on ALL FOUR pods at boot — matthew/jessica/james/adam each had fossils (adam's "healed June" key was itself stale). Demand auto-pin also landed (default ON, DoS-budgeted 256/window + 2048-row ceiling). Known limitation: auto-pin fires on MISS only, so already-present content never re-pins — the seeder (seed-provide-rows reading healed keys) remains the re-author for present content after a rekey. Open residues: `provide-reconciler-actual-set-transport-id-mismatch` backlog (transport-id actual-set query); heartbeat zome-call race (unguarded `call_zome`).

**2026-07-21 — identity-coherence sprint landed on dev (`928bbb5ec..44feb0db0`):** membership projection now stamps `agent_pub_key` (the 2026-06-15 stopgap, was still unbuilt); salvage/custody self-identity writes agent_cid (last transport-id provider writer closed); snapshot diversity = real shared fault-domain fold; holder-relation deduped. Details in [[project_dataplane_next_lens_diversity_placement]]. Card lights on alpha once deployed AND humans rows carry keys (membership signals / heal / seeder re-author).

**2026-07-22 — the remaining three stacked defects found + fixed (on integ/dev-merge, commits `9378519cb`+`53f0f5673`+`f3f3a9422`, a2o `2d2406dc8`+`4ca36fb75`):** (1) **non-self fossil keys** — NO writer could correct a stale-but-SET `agent_pub_key` for another human (every writer NULL-only; genesis_self_heal SELF-only); live fossils on matthew's pod were adam (`unY9`→live `Qte6`) AND jessica (`tdyZ`→live `eM3b`). Cure = `membership_identity_reconcile` boot service: forced-bijection pairing per household (exactly 1 orphan fossil + 1 unmatched live membership key, else abstain — membership entries carry only `agent:{pubkey}`, no slug, so household is the only match dimension), CAS supersede + same-transaction cascade to `shard_locations.peer_id`+`rea_commitments.provider`, abstains on INCOMPLETE membership reads (decode-drop + same-boot new-join = the mis-attribution trap). (2) **shard push NEVER worked** — `push_shard` fed the agent_cid (`uhCAk…`) to `libp2p::PeerId::parse()` (bs58) which always fails → `shard_locations` never populated by distribution, on any content, ever; the `#[ignore]`d `distribute_shards_diversity` test admitted it. Cure = `transport_resolve.rs` (manifest-first, `peer_identity_bindings` fallback, send-time only — stored identity STAYS agent_cid) + backfill Part B: measured-but-zero-locations content re-distributes at boot (cap 50). (3) **commitment leg already lights live** — `commitmentBackedCollectives: 2` on `evolution-of-trust` (probed 2026-07-22); earlier zeros were from probing non-commons e2e content (scope-list must contain `content:{reach}`). Post-deploy money probe: `GET doorway-alpha/api/v1/resilience/evolution-of-trust/household` → `stewardingCollectives ≥ 2`. Watch metrics: `elohim_identity_key_supersede_total`, `elohim_shard_push_peer_unresolved_total`. Preconditions that can keep it partial (now observable, not silent): eve's `peer_statuses` row (eden placement), per-peer binding sources for push resolution, adam pod degraded (`projectionReconcile 0/53`, `provideLoop.active:false`).

**2026-07-23 — humans:0 RED HERRING resolved (live-probed):** doorway `/health/startup` `projection.humans/relationships` counts the doorway's Mongo warm-cache, whose cache stream filters humans on the exact literal `profile_reach='public'` (`db/cache_queries.rs` `list_cacheable_humans`; content/relationships use `eq_any(["commons","public"])`) — nearly all fixture humans declare `community`, so `humans:0` there is the filter working, NOT missing data, and it is a DIFFERENT subsystem from the resilience card (which reads storage SQLite via manifest StorageProxy). Storage `/db/humans` live: 14 rows; `household-eden` + `household-dowell` FULLY keyed (matches `commitmentBackedCollectives:2`); `humans.household_id` holds the plain seed slug and that IS the correct join key (fold joins `collectives.id`). Real residues: (a) `stewardingCollectives:0` on fully-keyed households = shard pushes likely still not landing (`peer_transport_manifest`/`peer_identity_bindings` empty in prod → transport_resolve returns None) — verify by re-triggering distribution on one measured content and watching `shard_locations`; the 2026-07-23 placement-gap fix (7d5b2d876) makes this failure VISIBLE as `peers-unavailable` gaps instead of fabricated-green empty gap lists; (b) gertrude anomaly: pod live but key NULL (doesn't fit the deployed+self-heal pattern — needs conductor logs); (c) no signal projector heals a human row from a DHT Person event (only writers: seed-time register with null keys, SELF-only boot heal, and the membership reconcile which correctly abstains absent preconditions — incl. the new withdrawn-member abstain, b684eea71).

**2026-07-23 evening — residue (a) CLOSED, root cause PROVEN by live metric; and the real ceiling found.** Discriminator: `elohim_shard_push_peer_unresolved_total` = **8 on matthew's pod, 0 on all six others** (Prometheus via observability MCP; storage `/metrics` is NOT doorway-proxied, so query Prometheus, not the doorway). That pins it to the **push layer, not selection**: `peers-unavailable` has TWO emitters (`peer_selection.rs:178` = no eligible provider; `p2p/mod.rs:1712` = selection full but `distributed < total`, string reused because the placement-gap schema enum is closed) — the metric is the ONLY way to tell them apart, so never infer the layer from gapKind alone. Chain: no manifest sets `AGENT_PUBKEY`, so `main.rs:1896` always minted a random `uhCAk_<32hex>` per boot (note the UNDERSCORE — a real key never has one); the identity handshake broadcasts that as its `agent_cid`, so `peer_identity_bindings` holds real dialable `peer_id`s keyed to garbage cids; `peer_transport_manifest` is iroh-plane-only and `ELOHIM_TRANSPORT_BACKEND` is set in NO manifest (defaults Libp2p) → structurally empty. Both `transport_resolve` sources dead → every push skipped → `shard_locations` empty → all four card zeros (`onlinePeers` is a CONSEQUENCE: `count_online_peers_in_households` is called with the already-empty steward set, not independent evidence). Safe to fix because `NodeIdentity::load_or_generate` persists ONLY the libp2p keypair — `agent_pubkey` is a fresh per-boot parameter, so correcting it leaves `peer_id` byte-identical. Second live bug: backfill **Part B never runs** — `run_once` early-returns on an empty Part A candidate set (`shard_manifest_backfill.rs:344`) and Part B sits after it, so in alpha's steady state (every blob-backed content already has a manifest) the self-healing redistribution has never executed on any boot. **THE CEILING (new, structural): `peer_statuses` is SELF-ONLY** — one production writer (`signals.rs:564`, self-authored zome call whose `post_commit` signal is author-local; the zome defers cross-agent aggregation to "Phase 2"), no DELETE, no TTL. Proven live: doorway-alpha 4 rows vs elohim.host 2 rows, **zero overlap** — each pod sees only its own key plus its own rekey fossils. Since `peer_selection` draws candidates ONLY from `peer_statuses`, the only selectable steward is the node itself and `stewardingCollectives` **cannot exceed 1** on current code, no matter how much plumbing is repaired. Real multi-household stewardship needs the deferred cross-agent peer-status aggregation — that is the next slice, not more distribution debugging. Also found (independent, survives the shard fix): `db::peer_statuses::list_by_household` INNER JOINs `stewarded_nodes.id` (a pod **hostname**, from `boot_registration.rs:177`) against `peer_statuses.peer_id` (an `agent_cid`) — structurally empty, so `details.onlinePeers.live` stays 0 even with a full `shard_locations`. Byte-exact fossil check (decode base64, compare first 32 bytes — comparing base64 CHARS misleads: a 36-byte stored hash and a 32-byte conductor id legitimately diverge at char 43): only **adam + jessica** are fossils; matthew/james/eve/gertrude/susan are live-keyed, so fossils were NOT this blocker.
