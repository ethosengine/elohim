---
index: false
name: project_doorway_wedge_unbounded_mongo_await
title: Doorway wedge — blocking getaddrinfo parks tokio workers
description: "Doorway-alpha SIGKILL crashloop root = blocking getaddrinfo in holochain_client connect parking tokio workers during DNS flaps; fixed via async DNS + watchdog."
metadata: 
  node_type: memory
  type: project
  originSessionId: 8374a407-e107-43ab-b772-2b91e8417354
---

The doorway-alpha residual under-load wedge (all threads `futex_wait`, zero error logs, pre-fix `cpu:1`) was root-caused 2026-06-14: warm_stream re-streams the WHOLE corpus on every (re)connect/DHT-signal (~14k upserts/2min over ~3.6k docs, `store.rs`); cold-start hot cache is EMPTY so the idempotency guard misses → every doc takes a full `upsert_to_mongo().await`; Mongo ops had NO per-op timeout. **CURE APPLIED & DEPLOYED** (4dc862748, live in image `c6243f34`): `WARMUP_PACE_MS` (8ms/64-entry real sleep) + `DOORWAY_MONGO_OP_TIMEOUT_MS` (2s) + 75s total warm budget. warm_stream is conductor-DECOUPLED — it does `GET {storage_url}/api/v1/cache/stream` over HTTP then Mongo upsert; zero conductor refs (`warm_stream.rs:255-409`); the only kitsune2 client is `subscriber.rs:374` (background signal stream, single-flight-guarded on reconnect).

**POST-CURE RCA (2026-06-15, adversarial 6-agent workflow) — the canon was MIS-MECHANISMED.** The cure WORKS at its job: CPU is flat ~0.05 cores and **CFS throttling is literally ZERO** at cpu=2, even while "Projected streamed entry" runs 24k/hr. **Yet SIGKILLs continue** (exit 137, reason `Error`, NOT OOMKilled; mem flat ~80MB/1Gi). So the **"warm_stream burst → CFS CPU starvation → /health starved" mechanism asserted in `warm_stream.rs:53-60` AND in [[project_per_node_memory_is_conductor_authority_arc]] is FALSIFIED at cpu=2.** The residual is a **non-CPU shared-runtime stall**: `/health` shares the main Tokio runtime (`DOORWAY_HEALTH_PORT` deliberately UNSET, alpha.yaml:206-210) and misses the 150s probe window when a blocking/synchronous op monopolizes the few worker threads WITHOUT burning CPU. **ROOT PINNED** (2026-06-15 root-hunt, 5-agent source-verified): a BARE synchronous `std::net` getaddrinfo (`to_socket_addrs`) inside holochain_client's conductor connect (`admin_websocket.rs:112` / `app_websocket_inner.rs:45`), run on the tokio worker polling the connect future, BEFORE the first `.await`. The per-conductor subscriber reconnect loop (`subscriber.rs:374`) fires it twice per reconnect; during matthew's DNS flap (309 reconnects/10min, "Name or service not known" = failing getaddrinfo, the slowest resolver path) workers park in the resolver syscall — UNCANCELLABLE by the existing `tokio::time::timeout` (a blocking syscall has no yield point) → enough concurrent parks starve /health. adam never flaps → 0 restarts.

**The A/B control is the clincher:** matthew-side doorway = 12 restarts; adam-side `-b` doorway = **0 restarts** — identical image `c6243f34`, identical probe config, same warm_stream code. Only difference: matthew-side runs ~5× the warm_stream replay volume (24,311 vs 5,038 "Projected streamed entry"/hr; matthew's corpus is bigger). More replay → more starvation windows → liveness kill. This rules conductor-saturation OUT as *sufficient* (both conductors equally stressed; only matthew's doorway dies) and localizes the killer to the doorway's own behavior.

**H2 (conductor kitsune2 `Full(..)` fetch-queue overflow as ROOT) — REFUTED.** The `Full(..)` IS present but is **INFO-level** `kitsune2_gossip::timeout` force-initiate backpressure (graceful ~20s-timer skip, upstream crate, NO repo knob), NOT request-dropping op-fetch overflow. Temporally UNCORRELATED (doorway crashed flat 7–8/hr even when `Full(..)` collapsed to 1/hr). The only WARN fetch errors are `tx5 → wss://signal.doorway-alpha` = conductor can't reach peers BECAUSE the doorway is down (arrow inverted). Conductor (matthew) Ready=1 all 48h, 0 crash-restarts; its pod-UID churn was rolling DEPLOYS, not crashes. **Real-but-separate:** matthew logs `Database read connection is saturated. Util 500.00%` (~1.58M/13h) — SQLite read-pool 5× oversubscribed + ~3GB RAM + cpu-limited conductor; this AGGRAVATES H1 (slow storage reads lengthen bursts) but does not deliver the SIGKILL. Own ticket.

**FIXED 2026-06-15** (branch feat/frontend-eyes-sprint; operator deploys — atomic image+manifest via pipeline). **(1) Root cure** (f236416db): `conductor::resolve_host_port` resolves via `tokio::net::lookup_host` (async getaddrinfo on the BLOCKING pool, off the core workers, `.await`-cancellable) and hands holochain_client a pre-resolved `SocketAddr` (std's `ToSocketAddrs for SocketAddr` is a no-op, no syscall) — applied at all 4 connect sites (typed_admin, typed_app, zome_caller admin+app), 5s DNS timeout. **(2) Watchdog safety net** (19fb41974): liveness served from a DEDICATED OS-thread tokio runtime (`spawn_health_listener`), verdict gated on a main-runtime progress heartbeat (`spawn_liveness_heartbeat` AtomicU64; 503 only when stale >`DOORWAY_HEALTH_STALE_MS`=15s) — un-starvable AND truthful (genuine wedge → restart-on-hang preserved); resolves the team's stuck `DOORWAY_HEALTH_PORT` tradeoff. `DOORWAY_HEALTH_PORT=8079`, livenessProbe→8079, readiness/startup stay on :8080. **Band-aids** (be313cfc4): `DOORWAY_WORKER_THREADS` 4→8 (only RAISES the bar — a blocking syscall is unbounded by worker count), `WARMUP_PACE_MS` 8→50. Verified: clippy -D warnings clean, 652 tests pass (+6 new). a2o harvest: peer-conductor-connection-resilience.feature (4fd6c1c75). Full record: `.claude/data/doorway-freeze-incident-2026-06-13/WARM-STREAM-RESIDUAL-DIAGNOSIS.md`. Related: [[feedback_shift_drive_cure_not_stage]], [[project_doorway_ab_edge_islanding]], [[project_dna_hash_blind_to_coordinator_zomes]].
