---
name: project_content_sync_plane
title: Content sync / replication plane (umbrella)
description: "Sync-plane state: inventory gossip is metadata-only, not bytes; Automerge plane lit; iroh dual-stack; ghost declared heads deadlocked batch-3; ingest drain solved twice; local seed never DHT-anchors."
metadata:
  node_type: memory
  type: project
---

# Content sync / replication plane (umbrella)

Folds the content sync / blob-replication dataplane cluster. Members:

- [[project_inventory_exchange_not_byte_replication]] — Inventory gossip is metadata-only ("Received content inventory count=N"); byte replication is separate — check per-peer blob counts before calling sync alive.
- [[project_automerge_content_sync_plane_lit]] — Automerge storage-sync plane LIT (producer + libp2p convergence proof); producer MUST write h_app_id="elohim"; back-fill default-ON w/ fleet-safety invariants.
- [[project_ghost_declaration_deadlock_batch3]] — The "~2000 unanchored rows" were anchored rows with phantom declared heads (dead incarnations); cure = local-get responder + author-over-ghost decay (ELOHIM_GHOST_DECLARATION_DECAY)
- [[project_closed_loop_ingest_drain_prior_art]] — Paced ingest drain solved TWICE; live kernel = drain_publish_queue + wait-for-drain; warm_stream is open-loop pacing — diagnose the hang before a 3rd scheme.
- [[project_local_stack_dht_anchor_gap]] — Local bulk seed never DHT-anchors → provenance gate 404s all reads by design; dev repair = p2p_published_at backfill; real fix = import anchor step
- [[project_iroh_dataplane_actual_state]] — Dual ENABLED in alpha manifests 2026-08-05 (deploy pending) w/ sovereign never-n0 iroh defaults; proof = "Dual: DualGossipPublisher wired into P2PNode" + irohNodeId, NOT the degraded-wrapper log.
- [[project_dataplane_next_lens_diversity_placement]] — Diversity-aware salvage placement (1a+1b landed) is INERT in prod — household_id NULL from identity-coherence gaps, not scope reads; degrades safely to XOR.

**2026-08-23 — iroh leg was an idle listener until 972748a6d.** Every dual-mode node (alpha since Wave-2 E3, the mesh) had all iroh responders mounted and zero iroh traffic: sovereign defaults register no discovery service, so no NodeAddr was ever learned, nothing dialed, no gossip topic joined, no sync round initiated. "dual boots" / "iroh node started" log lines prove nothing — the claim is the `elohim_iroh_*` counters moving (`a2o features/dataplane/transport-dual-plane.feature`). Cure: signed `elohim/transport/manifest` gossip → `IrohPeerBook` → per-topic `join_peers` + iroh sync-round driver. Still open: pure-`iroh` mode has no bootstrap (manifest rides libp2p gossipsub first); blob heal-on-read and custody push remain libp2p-only.

**2026-08-24 — `syncDocuments` is a REPORTING BUG (always 0); iroh sync is working and idle-by-correctness.** `/p2p/status.syncDocuments` calls `count_documents("_all")` → `DocStore::count` scans key prefix `"_all:"`, but every doc lives under `"elohim:"` — so it reads 0 forever; `count_all()` is the right call. Ground truth via `GET /sync/v1/elohim/docs`: 504 docs on each local peer, 5,356 on the fleet. Corpus back-fill ran everywhere (local: scanned ~485, skipped ~484 = docs already exist and match). In dual mode both planes share ONE DocStore; libp2p converged it (`elohim_sync_in_sync_total` ≈ every round), so iroh's `local_heads == remote.heads` fires on every doc and `sync_changes_applied` stays ~1/peer. Control plane live everywhere (peers_known=6, ~78k neighbor events, ~3.9k manifests, sync_rounds 62–313/pod). **Consequence for measurement:** a converged corpus cannot show transport leverage in steady state — leverage is only visible DURING CHURN (peer restart, new content, deploy). Measure time-to-converge per transport pair under injected churn, never steady-state counters. Don't read `syncDocuments` until the count fix lands; read `/sync/v1/elohim/docs` `total` or `elohim_sync_in_sync_total` instead.

**2026-08-29 — half-record contagion + edge-triggered heal; three stations cured, one open.** A HALF row = `blob_cid` set, `blob_hash` NULL (legit for declared-but-unuploaded blobs — matthew has ~1.4k). The pull leg's `bulk_create_content` is insert-only, so a half record served by a half survivor (james restarted 2 min after a seed) lands as a half row on the recovering peer and is never re-pulled (`local_has` = row exists). Three stations cured (`d4fc29f5b`…`e4eb19ec6`): (1) `reverse_project_content_doc` was EDGE-triggered on doc changes and no-op'd when the row was absent — `heal_half_rows_from_docs` is the level-triggered sweep (200 half rows/sync round, keyset cursor, `elohim_sync_half_row_heal_total`); (2) `update_content`'s amber rule kept a NULL blob_hash on green rows — a NULL is not a notarized hash; (3) reverse heal now returns healed iff the row carries the hash. OPEN node: some blob_hash writer doesn't ride `ContentUpdated`, so publisher docs go stale until the cold-start back-fill (`projected 1270` on a matthew restart). Also open: `inventory-refresh-pages-dropped-as-gaps` (77-page refresh, out-of-order pages dropped, SnapshotRequest is a placeholder → ~9 % inventory view). Read `half rows` via `select count(*) from content where blob_cid is not null and blob_hash is null` on a `cp` of `content.db*` (no sqlite3 CLI; python sqlite3).

**2026-08-29 (evening) — inventory refresh: three defects behind the "9 % view", all measured and cured (`4230637f4`, `e4e5d097e`).** (1) Early pages were dropped as gaps → reorder window (`p2p/inventory_reorder`). (2) The tail (pages 63..76 of 77) never left the publisher: `broadcast_inventory_snapshot` try_sent every page into the 64-slot `P2PCommand` channel the swarm loop itself drains → paced 16 pages/s from a backlog. (3) A publisher RESTART resets its sequence allocator; receivers keep the old high cursor, every delta reads `replay`, the identical snapshot page keeps deduplicating → view collapses to one page for ~cursor/78 refreshes (days on a long-lived fleet peer = every deploy) → `apply_snapshot` re-bases on a snapshot > `PUBLISHER_RESTART_GAP` (200) below the cursor. Proper fix is a boot epoch in the page header (deferred: msgpack positional structs). Fleet-readable now: `elohim_inventory_pages_total{kind,outcome}` + `elohim_inventory_pages_published_total`. Also: a failed `apply_delta` (SQLite busy) is held and retried on the next Gap arrival. Cold-start drift class = `headActionHash` (HTTP declare handlers didn't announce) — read `filled=` on the back-fill line.
