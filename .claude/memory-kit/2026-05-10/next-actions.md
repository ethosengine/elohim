# Next-actions menu — 2026-05-10 (PILOT RUN)

Generated by Phase 2 synthesis subagent of `/converge`. Pilot scope: top 3 themes only (`epr`, `doorway`, `iroh`). Other 17 themes not investigated this cycle.

**Read first**: this menu reflects only the pilot's narrow lens. The single most-actionable next step in the corpus may live outside the three themes investigated. Operator should treat this as "best of the top 3 themes," not "best in the corpus."

---

## Top recommendation: Verify and close the iroh cutover (gates #6/#7/#9/#11 soak verification)

- **Plan**: `genesis/docs/superpowers/plans/2026-05-10-iroh-delivery-master.md` (operator-tier, not subagent-tier)
- **Vision-alignment**: 8/10 — Principle 1 (Distributed Architecture, anti-capture by transport diversity) is structurally advanced by closing the cutover; the iroh-libp2p complementarity is the substrate's most important resilience property. Per memory `project_redeploy_the_substrate`, this work IS the protocol's escape valve from single-vendor capture.
- **Readiness**: 7/10 — gates #2/#4/#5/#8/#10 have unambiguous evidence on dev; gates #6/#7/#9/#11 are soak-and-drill artifacts that need an operator presence to verify (CI nightlies green for 7 days; alpha cluster on dual-stack for 7 days; consumer hardware soak; rollback drill executed). NOT subagent-runnable.
- **Pre-authored Objective**: "Walk the iroh delivery master plan's `## Cutover gate closure tracker` table. For each ⏳ entry, verify the closure evidence (Jenkins for #6, alpha cluster `kubectl` for #7, consumer hardware logs for #9, rollback drill report for #11) and update the table. For #2/#4/#5/#8/#10 (already done in code), simply flip the table marker to ✅ with the commit-hash citations from `.claude/memory-kit/2026-05-10/converge/iroh-proposal.md`."
- **Estimated cycles**: 1 cycle (operator-led, ~2 hours of verification + table edit)
- **Recommended skill**: NOT `/shift` (no agentic developer needed) — operator handles directly with Jenkins MCP + kubectl. Possibly `/loop` if waiting for nightly soak completion.
- **Blockers**: none code-side. Soak-time-elapsed is the gate.

---

## Other ready plans

### 2. Mark-done batch on EPR Phase 3.5 Done-definition (16 entries)

- **Plan**: `genesis/docs/superpowers/plans/2026-04-30-epr-phase-3-5-trust-compute-gradient-plan.md`
- **Vision-alignment**: 9/10 (theme); 5/10 (act of marking) — substantively advances Principles 1, 2, 3, 6 but the substrate already shipped; this is corpus-legibility hygiene.
- **Readiness**: 9/10 — every Done-definition checkbox has matching `feat(epr-3.5): T<n>` commits on dev (T1-T20 + follow-up T17-T20 fix commits). Pre-authored proposals at `.claude/memory-kit/2026-05-10/converge/epr-proposal.md` with commit-hash evidence per box.
- **Pre-authored Objective**: "Apply the 16 mark-done edits from `.claude/memory-kit/2026-05-10/converge/epr-proposal.md`. The plan's `## Done definition` section (lines 1062-1079) maps 1:1 to T1-T20 commits. Two are OPERATOR-CALL (quality-gate verification + merge-style verification) — reviewer reads those before flipping."
- **Estimated cycles**: 1 cycle (~30 min operator review + apply script)
- **Recommended skill**: `converge-apply.py` after operator marks Accept boxes
- **Blockers**: none

### 3. Author plan file for `epr-light-up` continuation

- **Plan**: NEW (no plan file exists for this in-flight workstream)
- **Vision-alignment**: 7/10 — same principles as Phase 3.5; lighting-up is where standing/tending start affecting actual reach decisions in the running app
- **Readiness**: 4/10 — workstream is mid-flight (T01-T48 commits visible), but no plan file means convergence can't track it. Operator OR a planning agent needs to author the plan retroactively from the commit chain so the next cycle picks it up.
- **Pre-authored Objective**: "Read the chain of `feat(epr-light-up): T<n>` commits since 2026-05-04 (start with `git log --oneline --grep 'epr-light-up' --reverse`). Author `genesis/docs/superpowers/plans/2026-05-04-epr-light-up-plan.md` (or similar dated filename) with: tasks scoped from each commit's diff, Done definition reflecting current state, Open Questions for any tasks beyond T48 not yet started. Use the `superpowers:writing-plans` skill."
- **Estimated cycles**: 1-2 cycles (substantive plan authoring)
- **Recommended skill**: `/shift` with planning Objective, OR operator-direct with `superpowers:writing-plans`
- **Blockers**: requires reading ~48 commits' diffs; meaningful work

### 4. Doorway SSR Phase 2 followups (concept content rendering, memory monitoring, staging/prod manifest hardening)

- **Plan**: `genesis/docs/superpowers/plans/2026-05-07-doorway-ssr-runtime.md` (needs Open-Questions section added)
- **Vision-alignment**: 6/10 — Principle 1 (Distributed Architecture; SSR makes content readable to external WebFetch / crawlers / AI design tools)
- **Readiness**: 6/10 — substrate shipped, followups well-scoped per sprint-result, but Phase-2 work itself is real engineering (transferState wiring, Prometheus instrumentation, manifest patches)
- **Pre-authored Objective**: "Concept page bodies still render empty after SSR substrate landed (shell renders, `<article>` body and `<title>` don't populate). Wire `provideClientHydration` + sync-aware data resolver + `transferState` so `curl https://alpha.elohim.host/lamad/concept/<id>` returns HTML with the actual concept text. Reference: `.claude/shifts/doorway-ssr-deliver-2026-05-07T23-37/sprint-result.md` followup #1."
- **Estimated cycles**: 2-3 cycles (Angular SSR + transferState debugging)
- **Recommended skill**: `/deliver` (target is operator-visible content rendering, not green CI)
- **Blockers**: requires Angular 19 + @angular/ssr expertise; the deliver shift left this open as the bottom-of-iceberg

### 5. Mark-done bulk on iroh-recovery-e2e step boxes (29 entries)

- **Plan**: `genesis/docs/superpowers/plans/2026-05-10-iroh-recovery-e2e.md`
- **Vision-alignment**: 8/10 (theme) / 4/10 (act) — recovery is principle-tier per memory `project_recovery_grandma_standard`; the marking is hygiene
- **Readiness**: 9/10 — plan frontmatter declares `status: complete`; §Status section enumerates all green gates; 7/7 cargo tests pass
- **Pre-authored Objective**: "Bulk-flip all 29 `- [ ]` checkboxes in `genesis/docs/superpowers/plans/2026-05-10-iroh-recovery-e2e.md` to `- [x]`. The plan's frontmatter and §Status section already declare COMPLETE. Use sed or operator-manual edit; the per-step evidence is the section's `EXIT 0` enumeration."
- **Estimated cycles**: 1 cycle (5 min)
- **Recommended skill**: operator-direct (sed or Edit tool); too small for /shift
- **Blockers**: none

---

## Quiet but load-bearing

These items rank LOW in the convergence-themes file but MAY be foundational. Per safeguard #3, surface them so operator attention isn't bias-anchored to recency.

### A. The trust-compute gradient brainstorm artifact

- **File**: `genesis/docs/superpowers/specs/2026-04-30-trust-compute-gradient-brainstorm.md`
- **Why quiet**: it's a spec, not a plan — falls outside `convergence-themes`'s plan-filename weighting. Search-invisible despite being the source-of-truth for §2.8 constitutional floors and the aunt-and-rage-bait scenario (which T17 + T20 of EPR Phase 3.5 implement).
- **Why load-bearing**: §2.8 names the 10 floor classes (5 standing-immune + 5 tending-immune) that the bootstrap manifests encode. These ARE the constitutional floor of the protocol's reach gradient. Per memory `project_values_forward_preference_guards` and `project_elohim_as_counsel`, these floors are vision-aligned with Principle 2 (Graduated Intimacy) and Principle 3 (Values Alignment).
- **Action**: consider promoting §2.8 + the principles into `genesis/docs/content/elohim-protocol/manifesto.md` Part II (or as a sibling doc under `social_medium/`). Per safeguard #4, this is the moment — once the EPR theme hits DF saturation, the brainstorm will become invisible to convergence and only be findable by direct path.

### B. The iroh-libp2p complementarity architecture decision

- **File**: implicit in commit `4086e454b docs(architecture): iroh ↔ libp2p complementarity — three transport tracks, anti-capture by design`
- **Why quiet**: this is an architecture-decision commit, not a plan or spec file. Search will never surface it; only memory entries `project_iroh_parallel_stack_phases3_7_landed` etc. anchor it. Memory cap warning in MEMORY.md (32.1KB > 24.4KB) means even those entries are partially evicted.
- **Why load-bearing**: this is the substrate-tier "anti-capture by design" decision per memory `project_redeploy_the_substrate`. If iroh cutover completes and gets archived, but the architecture decision isn't promoted to manifesto-tier first, the *reasoning* will be lost.
- **Action**: when gate #11 closes, promote the complementarity decision into either `manifesto.md` (transport-architecture section) or `protocol-specification.md`. Don't let the iroh theme go dark until this is done.

### C. The lamad / mishpat DNA entry-type budgets

- **Files**: implicit in memory entries (`Lamad DNA at ~73/~100`, `Mishpat at 11/~100`)
- **Why quiet**: these are operating constraints embedded in memory, not in any plan. They appear as parenthetical reminders inside individual plan P2P-Design-Gate sections. No single doc tracks the running budget.
- **Why load-bearing**: every new feature plan needs to know the DNA budget before designing. The per-plan reminders are all manually authored; drift accumulates silently.
- **Action**: consider authoring a `genesis/docs/superpowers/specs/dna-entry-type-budget.md` (or tracker) that's the single source of truth, refreshed when each new plan ships an entry type. Hygiene work; not blocking.

### D. The `epr-light-up` workstream itself

- See item 3 above (other-ready menu). Calling it out here also as quiet-but-foundational because **the convergence search literally does not see it** — there's no plan filename matching the keyword. ~48 commits invisible to next-cycle convergence.
- This is the textbook safeguard #4 case in reverse — successful undocumented work goes invisible to corpus tracking.

---

## Summary counts

- **Themes synthesized**: 3 (`epr`, `doorway`, `iroh`)
- **Per-theme proposal files**: 3
- **Edits proposed (across all themes)**:
  - `mark-done`: 16 EPR Done-definition + 1 BULK iroh-recovery-e2e + 5 iroh-master-table-cell (manual) = 22 total
  - `surface-question` / OPERATOR-CALL: 1 doorway Phase-2 + 5 iroh gate-status-Q + 4 EPR/iroh meta-questions = 10 total
  - `add-as-outstanding`: 0 (all candidates ended up as surface-question or manual)
  - `merge-redundant` / `remove-obsolete`: 0 (per safeguards 2 + 7)
- **Empty per-theme proposal slots**: 0 (every theme had at least mark-done evidence)
- **Menu entries**: 5 ready + 4 quiet-but-load-bearing = 9 surfaces
- **Output paths**:
  - `/projects/elohim/.claude/memory-kit/2026-05-10/converge/epr-proposal.md`
  - `/projects/elohim/.claude/memory-kit/2026-05-10/converge/doorway-proposal.md`
  - `/projects/elohim/.claude/memory-kit/2026-05-10/converge/iroh-proposal.md`
  - `/projects/elohim/.claude/memory-kit/2026-05-10/next-actions.md` (this file)
