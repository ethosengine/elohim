# Sprint Digest — 2026-05-10

Generated by `.claude/scripts/memory-kit/sprint-distill.py`. Read-only.

Periodic digest of sprint-result artifacts. Scattered across the repo, they carry real engineering signal — what shipped, what blocked, what was learned — but lose visibility quickly. This file makes them legible in one place. The operator (or a future agent) decides what graduates to memory or specs.

- **Sprint files digested**: 18
- **Date range**: 2026-04-20 to 2026-05-09
- **Output**: `.claude/memory-kit/2026-05-10/sprint-digest.md`

---

## Sprints (newest first)

### 1. Shift Journal — Drive elohim-orchestrator/dev to a clean cascade

- **Path**: `.claude/shifts/2026-05-09T16-30-orchestrator-clean-cascade.sprint-result.md`
- **Date**: 2026-05-09 _(source: filename)_

**Key learnings** _(line 23)_:

- **JSONArray.join() sandbox rejection** — readJSON returns net.sf.json.JSONArray, not Groovy ArrayList. Calling `.join()` directly hits Script Security whitelist denial. Fix: `.collect{it}.join(...)` to materialize a plain ArrayList first. Candidate for ci-observer pattern catalog.
- **Empty PVC mount masking image-resident binary** — mounting an empty cluster PVC at a path the container image populates (e.g. `/nix/store`, `/usr/local/lib`) wipes the image's content on first run. Symptom: `command not found` on a binary whose PATH entry should be valid. ...

**Iteration trajectory** _(line 14)_:

- **Iter 1** (252c611ad): JSONArray.join sandbox fix. #926 dispatched cascade — orchestrator passed cleanly; downstream elohim-holochain failed.
- **Iter 2** (32858ed84): Drop /nix/store PVC mount. Empty PVC was masking image's bundled nix binary on first run. cargo-cache + sweettest-target-cache mounts retained.

---

### 2. Sprint Result — `2026-05-07T00-47-storybook-stage-green`

- **Path**: `.claude/shifts/2026-05-07T00-47-storybook-stage-green.sprint-result.md`
- **Date**: 2026-05-07 _(source: filename)_

**Outcome** _(line 9)_:

**The fix is in. Two fresh-trigger SUCCESS measurements confirm stability.**

| Measurement | Build | Result | Triggered by | Commit | Duration |
|---|---|---|---|---|---|
| 1 | `elohim-storybook/dev#25` | SUCCESS | orchestrator#876 | `489e0fcc` (the fix) | 200s |
| (—) | `elohim-storybook/dev#26` | FAILURE | orchestrator#879 | `866c340b` | 610s — **Checkout-stage infra flake; never ran the fix** |
| 2 | `elohim-storybook/dev#27` | SUCCESS | orchestrator#881 | `a12adc47` | 195s | ...

**What blocked** _(line 104)_:

- **Iteration 4**: An observer pass returned a confidently-wrong claim (storybook SKIP'd) that nearly triggered a premature bail. The measure command (single-purpose, deterministic Bash) was the disagreement detector. Lesson: when an observer's verdict and the measure disagree, trust the measure and re-observe with a more pointed prompt. ...

**Decisions / pivots** _(line 97)_:

- **Iteration 1**: Skipped pre-push verification gate normally — auto-mode + clear diagnosis from user-supplied error log made full-trace investigation overkill. Validated locally before commit; built successfully with 144 entries in dist/storybook.
- **Iteration 2**: Tried empty `[build:storybook]` retrigger as canonical clean path. Wrong: tag isn't aliased. Pivoted to real-source change in Iter 3. ...

**Open questions / followups** _(line 43)_:

The shift surfaced three real bugs in the surrounding orchestrator/CI/k8s stack. Each is its own scoped shift; none block the storybook fix landing.

### F1 — Add `storybook` alias to orchestrator buildTagAliases

**One-line change** at `genesis/orchestrator/Jenkinsfile:907-914`:

```groovy
def buildTagAliases = [
    'edge': 'elohim-edge',
    'dna': 'elohim-holochain',
    'app': 'elohim',
    'genesis': 'elohim-genesis',
    'sophia': 'elohim-sophia',
    'steward': 'elohim-steward',
    'storybook': 'elohim-storybook'   // <-- add this
]
``` ...

---

### 3. Sprint Result — feedback-dialogue-panel

- **Path**: `.claude/shifts/2026-05-07-feedback-dialogue-panel.sprint-result.md`
- **Date**: 2026-05-07 _(source: filename)_
- **Status**: **Delivered.** The Feedback Dialogue Panel renders correctly from any markdown content page, surfaces the four governance feedback choices, and stacks the dialogue modal **above** all page chrome with ...

**Outcome** _(line 11)_:

**Delivered.** The Feedback Dialogue Panel renders correctly from any markdown content page, surfaces the four governance feedback choices, and stacks the dialogue modal **above** all page chrome with a backdrop that dims the underlying content. The "panel behind another element" symptom that motivated this delivery sprint is resolved. ...

**What shipped** _(line 19)_:

Cited from `genesis/a2o/features/qahal/feedback-dialogue-panel.feature` and the design spec `genesis/docs/superpowers/specs/2026-04-19-gate-challenge-and-indemnification-design.md` (P4 — Accountable Peers).

| Scenario (verbatim) | Step-level assertions | Visual evidence | Verdict |
|---|---|---|---|
| `@happy-path @reach` — *Learner reaches the feedback dialogue from a content page* | 6/6 pass | `trigger-bottom-left.png` — "Share Your Feedback" panel rendered in `.content-actions`, four icon-bearing category buttons visible | ✅ delivered | ...

**What blocked** _(line 80)_:

**Strict console-error `After` hook masks otherwise-passing scenarios.**

In `cucumber-report-browser.json` for #993, all `@feedback-gate` scenarios show this pattern:

```
[scenario] Trigger reveals the four governance feedback choices
  step statuses: p,p,p,p,p,p,p,p,p,f
  ❌ step #9: After
     err: Error: Scenario passed but had 5 browser error(s):
       [Timothy] console.error: 503 () https://alpha.elohim.host/resource/manifesto
       [Timothy] console.error: 400 () https://alpha.elohim.host/resource/manifesto ...

**Open questions / followups** _(line 249)_:

- Alpha SSR/ingress routing: `/lamad/index.html` returning nginx welcome — separate ops concern
- Cucumber strict console-error checker still masks @feedback-gate scenarios on alpha-doorway flake (iter-3 known gap, unchanged)
- Genesis pipeline didn't get dispatched this round because elohim-edge failed at Level 0; once edge is fixed, genesis will run automatically on next push

---

**Iteration trajectory** _(line 126)_:

| Iter | Refresh | Render source | Tier-1 | Tier-2 | Tier-3 | Notes |
|---|---|---|---|---|---|---|
| 3 (this iteration) | P4 spec re-read (`2026-04-19-gate-challenge-and-indemnification-design.md`) | CI builds #993 + #994 (already-completed artifacts) | ci-observer hit downsampling limit on 22 MiB full-page PNG; orchestrator cropped via Pillow and inspected directly | skipped — orchestrator-direct read of cropped images sufficient | **delivered** with documented framework gap | No code edits this iteration; verdict was a read-only stewardship judgment over existing artifacts. | ...

---

### 4. Sprint Result — `/deliver light-up-the-topology` (third pass)

- **Path**: `.claude/shifts/light-up-the-topology-deliver-2026-05-07T04-20/sprint-result.md`
- **Date**: 2026-05-07 _(source: filename)_

**Outcome** _(line 9)_:

The light-up-the-topology sprint's user-visible surfaces now render end-to-end with real data shape. The user's complaint ("still not seeing the visual surfaces working") has been substantially addressed:

| # | Deliverable (verbatim) | Iter-0 | Iter-1 | Evidence |
|---|---|---|---|---|
| D1 | "Per-content distribution badge — visible to visitors with no auth" | error_state | `error_state` | content-viewer template at lines 78-82 has `<elohim-distribution-badge>` conditional `@if (node.distribution)`. ...

**Decisions / pivots** _(line 103)_:

- **Iter-1: Local Playwright vs CI for fresh trigger.** The deliver skill specifies "fresh CI render or fresh local trigger". Genesis #989/#990 broke at browser launch. Building two #989-equivalent failures into a "stability" verdict would be cargo-cult; instead, ran direct Playwright locally against alpha as the fresh-trigger. Same chromium build (1208), same auth path (Matthew → JWT → X-Agent-Cid), same endpoints. **Stronger evidence than CI** because not gated on the sidecar version drift. ...

**Open questions / followups** _(line 80)_:

**Sprint: "Wire content-seeders to alpha pipeline"** (estimated 1-2 days)

1. **seed-agent-bindings**: wire into `genesis/Jenkinsfile` after seed-accounts. Build CONDUCTOR_URLS from deployments.json (filter !suspended), passing `ws://elohim-${name}-alpha:4445` for each non-suspended human. Matthew gets `[desktop, node, steward]` per the script's plan.

2. **seed-commitments**: wire into `genesis/Jenkinsfile`. REST-only via DOORWAY_URL (already proven works from local). Alone, this populates D4 with shape but not Matthew-specific inflow/outflow. ...

---

### 5. Sprint Result — doorway-ssr-deliver-2026-05-07T23-37

- **Path**: `.claude/shifts/doorway-ssr-deliver-2026-05-07T23-37/sprint-result.md`
- **Date**: 2026-05-07 _(source: filename)_

**Objective** _(line 9)_:

`curl https://alpha.elohim.host/lamad/concept/<id>` returns:
- HTTP 200 with rendered HTML body
- Angular hydration markers (`ngh="..."`)
- `x-render-cache` header (MISS first, HIT subsequent)
- `<title>` populated, `<article>` body present

**Outcome** _(line 17)_:

**Substrate (delivered, verified once on alpha):**

```
$ curl -sD- https://alpha.elohim.host/lamad/concept/fct-bible-micah-6-8

HTTP/2 200
content-type: text/html; charset=utf-8
content-length: 3666
cache-control: no-store
x-render-cache: MISS                    ← header present, MISS on first call

<app-root ng-version="19.2.19" ngh="0" ng-server-context="other">
  <router-outlet></router-outlet>
</app-root>
<script id="ng-state" type="application/json">{"__nghData__":...}</script>
```

Three consecutive GETs after first render confirmed cache: MISS → HIT → HIT. ...

**What shipped** _(line 63)_:

Pipeline went from "alpha serves nginx default Welcome page (896 bytes)" to "doorway returns SSR'd HTML with hydration markers (3666 bytes)" via 13 targeted commits:

| Commit       | Subject                                                              | Layer |
|--------------|----------------------------------------------------------------------|-------|
| `8e56d928`   | elohim-site Angular 19 `index.csr.html` handling                     | image |
| `3e8545f2`   | orchestrator dedup (remove redundant githubPush() trigger)           | CI    | ...

**Key learnings** _(line 86)_:

- **The cascade-halt pattern** struck multiple times: builds 943-948 all failed at "Build Doorway", masking a downstream "Build Storage" elohim-render dep regression that only surfaced once Build Doorway finally went green on 949. Reinforces `feedback_cascade_halt_masks_failures`.

- **pnpm `--filter "elohim-app..."` does NOT walk into tsconfig-path-aliased workspaces.** `@elohim/service` is referenced via `tsconfig.json` paths, not `package.json` workspace-deps, so its peerDeps (Angular, rxjs) weren't installed by the elohim-app filter. Required adding `--filter "@elohim/service..."`. ...

**Open questions / followups** _(line 98)_:

1. **Concept content rendering** — Angular route's data resolver doesn't synchronize with SSR. The shell renders but `<article>` body and `<title>` aren't populated. Likely needs `transferState` + `provideClientHydration` + sync-aware data resolver. **This is now the largest remaining gap** — the substrate is solid, but the route's bootstrap path doesn't await its data fetcher before render returns. Separate sprint. ...

---

### 6. Sprint Result — `2026-05-06T02-44-rca-genesis-browser-failure-classes`

- **Path**: `.claude/shifts/2026-05-06T02-44-rca-genesis-browser-failure-classes.sprint-result.md`
- **Date**: 2026-05-06 _(source: filename)_

**Outcome** _(line 9)_:

### What landed

- **Final measurement:** 17 (Δ -1 from baseline 18). Target was ≤ 8.
- **Genuine win, masked by cucumber count:** the seed phase went from **9 failed registrations to 0**. All 9 previously-broken humans (Gertrude, James, Janice, Levi, Maria, Meriadoc, Pam, Susan, Tiffany) seed cleanly. Cucumber count moved 1 because the unblocked Susan/etc. scenarios now advance past login and hit cluster A's `[data-testid="assessment-start"]` content-not-found cascade — recorded as `feedback_cascade_hidden_test_surface.md` pattern. ...

**What blocked** _(line 77)_:

- **Iter-3 cucumber count is trustworthy at face value** but the user must read the seed-phase result alongside it. The 18→17 delta vastly understates the fix's reach. The proposed pipeline-legibility improvement (surface seed X-markers in sprint-report.json) would make this visible in the standard measure path and prevent future shifts from looking like they "barely moved" when they actually solved cascade-root issues.

**Decisions / pivots** _(line 71)_:

- **Iter-1**: bail-with-proposal at first iteration despite having a clear in-scope fix path? No clean in-scope fix existed; bail surfaced the scope question to operator early rather than burning iterations on shallow work. Outcome: operator authorized Option 3, fix landed iter-2.
- **Iter-3**: declared `progress` despite 17-score being far from ≤8 target? Yes — the measure understates the win (cascade-hidden test surface), and the seed-phase-clean evidence is the load-bearing verification. Not chasing the ≤8 target by shallow in-scope edits is the disciplined call. ...

---

### 7. Sprint Result — `/deliver doorway-conductor-stale-mapping`

- **Path**: `.claude/shifts/doorway-conductor-stale-mapping-2026-05-06T17-40/sprint-result.md`
- **Date**: 2026-05-06 _(source: filename)_

**Decisions / pivots** _(line 76)_:

- **Server-side fix over operator-side cleanup.** Two paths: (a) drop the 12 stale Mongo rows once via admin tooling, (b) make the chaperone self-heal so each user clears their own row on next login. Chose (b) — operator never has to touch this again on future deploys, and the fix doubles as protection for any user who happens to be offline during a future pool composition change. ...

**Open questions / followups** _(line 55)_:

1. **Mastery 405 noise** (~13 × POST `/api/v1/mastery` and `/api/v1/mastery/engagement` errors per page load on the lamad landing page after login).
   - Root cause: alpha nginx ingress lacks `/api/v1` proxy_pass → doorway. Same root-cause class as the topology cluster issue from `light-up-the-topology-deliver-cont-2026-05-06T08-30` shift's handoff brief Path A.
   - GET on alpha returns 200 with `content-type: text/html` (SPA fallthrough). POST returns 405 because nginx doesn't allow POST on the static-file location. ...

---

### 8. Sprint Result — `/deliver light-up-the-topology continuation`

- **Path**: `.claude/shifts/light-up-the-topology-deliver-cont-2026-05-06T08-30/sprint-result.md`
- **Date**: 2026-05-06 _(source: filename)_

**Outcome** _(line 9)_:

The orchestrator `[build:*]` force-include regression (Blocker 1 named in operator brief) was diagnosed, fixed, committed, and pushed to dev. Verification gated on a fresh orchestrator build — and four consecutive orchestrator builds (#860, #861, #862, #864) all failed with `Maximum checkout retry attempts reached, aborting` during `git fetch --depth=200`. The git-fetch infrastructure is intermittently broken; same fetch command succeeded on build #858 ~7h earlier.

This is the 7th cascade layer per the operator's brief — surface for direction. ...

**Decisions / pivots** _(line 113)_:

- **Iter-1 fix at Jenkinsfile level instead of graph-walker.mjs.** Operator brief proposed fixing graph-walker.mjs to accept forcePipelines list. Chose orchestrator-level mirror at the integration point because: (a) graph-walker's "no source files changed" is a CORRECT analysis; force-include is an ORCHESTRATOR concern; (b) graph-walker.mjs is also called from pre-push hooks — changing its API ripples; (c) build-graph.groovy is the actually-invoked walker in CI (not graph-walker.mjs); (d) the orchestrator already has a legacy FORCE_BUILD injection at line 1050 — mirror that pattern. ...

**Open questions / followups** _(line 74)_:

### Path A — fast verify (estimated 30 sec on operator side, ~10-30 min for genesis browser stage to render)

1. From cluster-admin context: `kubectl apply -f genesis/orchestrator/manifests/elohim-app/alpha/ingress.yaml`
2. Confirm: `curl -sf -w '%{http_code}\n' https://alpha.elohim.host/api/v1/cluster` should now return 401 (auth required) instead of 200 with HTML
3. Push empty commit `[build:genesis]` to trigger genesis browser stage
4. Genesis #986 (or whatever) will render the topology screenshots — should now show data, not error ...

---

### 9. Sprint Result — `/deliver light-up-the-topology`

- **Path**: `.claude/shifts/light-up-the-topology-deliver-2026-05-06T04-57/sprint-result.md`
- **Date**: 2026-05-06 _(source: filename)_

**Outcome** _(line 9)_:

For the first time ever, three of the topology sprint's five user-visible surfaces have CAPTURED SCREENSHOTS proving they render in CI:

| Surface | Verdict | Screenshot artifact (build #984) |
|---|---|---|
| `/shefa/cluster` (My cluster topology) | `partial` — chrome + heading + nav render; body shows red "unknown" error | [operator-can-see-their-household-device-cluster--Matthew.png](https://jenkins.ethosengine.com/job/elohim-genesis/job/dev/984/artifact/genesis/a2o/reports/screenshots/resilience-observable-distribution/operator-can-see-their-household-device-cluster--Matthew.png) | ...

**Decisions / pivots** _(line 112)_:

- **Iter-2/3/5: HUSKY=0 bypass.** Pre-push hook hung indefinitely on Jenkinsfile/.groovy diffs. Three foreground push attempts each ate 30-60s with empty stdout. Bypassed rather than burn iteration budget on hook-debug. Surfaced explicitly. Auto mode covered it; would re-prompt operator if the change had higher blast radius. ...

**Open questions / followups** _(line 62)_:

These are the blockers between current `partial` and full `delivered`:

### 1. Orchestrator graph-walker ignores `[build:*]` commit tags
**Symptom:** Push with `[build:app]` does not trigger the elohim pipeline if graph-walker says no app source files changed. Affects every developer's ability to manually force a pipeline retrigger. High blast radius.

**Evidence:** Orchestrator #858 console log "force-including: elohim" but graph-walker matrix shows elohim SKIP and graph-walker wins. iter-5's e8ecdd00 ingress fix didn't deploy because of this. ...

---

### 10. Shift Journal — `2026-05-05T20-30-verify-and-finish-genesis-e2e-verification-quality`

- **Path**: `.claude/shifts/2026-05-05T20-30-verify-and-finish-genesis-e2e-verification-quality.sprint-result.md`
- **Date**: 2026-05-05 _(source: filename)_

**Iteration trajectory** _(line 20)_:

Pre-iteration baseline measurement:
- `./genesis/agentic/scripts/jenkins-measure-genesis-findings.sh` → **90** (build #971 at SHA `3123906a`, BEFORE iter-5/6/7 commits landed)
- Predicted final measure once #971+stack chain completes: **35-45**

---

### 11. Sprint Result — `2026-05-04T22-51-alpha-pipelines-green-no-shem`

- **Path**: `.claude/shifts/2026-05-04T22-51-alpha-pipelines-green-no-shem.sprint-result.md`
- **Date**: 2026-05-04 _(source: filename)_

**Outcome** _(line 8)_:

### Bail summary

**Cascade reached UNSTABLE end-to-end on the fresh trigger pushed this shift.** Every load-bearing requirement from the Objective draft is met verbatim in the build logs of orchestrator/dev #822 (changeset `768e7786`). The shift's measure command is `jenkins-measure.sh` (SUCCESS-only), which returns 0 for UNSTABLE. The 4 UNSTABLE-causing issues are all Class 5 pre-existing (verified identical failure pattern in Edge #921 from 2026-04-28, four iterations of the same pipeline before the shem outage). ...

**What blocked** _(line 133)_:

- All measurements were authoritative — direct Jenkins API queries returned consistent results across multiple calls.
- The Cachix flake (Class 5) on #821 caused a single false-low measurement; iter 2's retrigger cleared it, validating the read.
- The chosen measure (`jenkins-measure.sh`) is technically correct (returns 1 for SUCCESS) but semantically over-strict given the chronic Class 5 noise in the alpha cascade. Future shifts of this shape should default to `jenkins-measure-or-unstable.sh` unless Class 5 has been swept first.

**Decisions / pivots** _(line 126)_:

- **Iter 1: chose to push a real fix instead of bail or wait.** The Cachix flake itself was Class 5 transient, but pushing required a meaningful change. The orchestrator-strategy.mjs drift was a real bug (drift since Apr 2026), in scope, and useful. Outcome: Push triggered the cascade I needed; the drift fix is genuine value beyond the trigger purpose. Validated.
- **Iter 1: did not dispatch Sonnet to verify Haiku's read.** Curl corroborated build numbers, results, and timestamps exactly. Outcome: correct call. ...

**Open questions / followups** _(line 41)_:

**Two paths forward; which do you prefer?**

**(A) Re-scope measure to `jenkins-measure-or-unstable.sh` (UNSTABLE-or-better) and retrigger.** This is what the prior shift used. The 4 UNSTABLE-causing issues are all Class 5 pre-existing per the Objective draft's classification ("log in shift result, do not let it gate completion if it's not related to the suspension story"). The suspension story IS validated in the cascade. Adopt UNSTABLE-or-better as a new shift Objective, retrigger orchestrator/dev to confirm 2x consecutive UNSTABLE-or-better with fresh trigger. ...

---

### 12. Sprint Result — `2026-05-03T18-19-orchestrator-805-pipelines-unstable`

- **Path**: `.claude/shifts/2026-05-03T18-19-orchestrator-805-pipelines-unstable.sprint-result.md`
- **Date**: 2026-05-03 _(source: filename)_

**Outcome** _(line 8)_:

### Bail summary

**Primary stated focus achieved:** DNA Integration tests/results are cleared. holochain went from FAILURE (#1189) to SUCCESS twice (#1191, #1192) — two consecutive passes including a fresh-trigger run, satisfying the stability criterion for the DNA layer.

**Cascade reached deploy stages for the first time in 4+ days.** With the storage compile error fixed, edge built all artifacts and pushed to Harbor for the first time since 2026-05-01 (when commit 333fa635 added the broken `include_str!`). ...

**What blocked** _(line 106)_:

- Iterations 2, 4, 5, 7, 8: measurements were `0` because orchestrator was in-progress, not because predicate truly failed. Per `jenkins-measure-or-unstable.sh`, an in-progress build returns `0` since `lastCompletedBuild.result` doesn't include the in-flight build. This is acceptable behavior (the predicate measures the last *completed* build); just be aware that `0` measurements during a long cascade don't all mean "regression." ...

**Decisions / pivots** _(line 100)_:

- Iteration 1: skipped Sonnet verification — reading 6 source files + 1 test file directly was faster than dispatching a verifier. Outcome: iteration 1's diagnosis was correct (4 failures classified into 1 fix + 3 quarantines worked exactly as predicted; iter 5 confirmed the only remaining failure was the same await_consistency root cause on a different test).
- Iteration 6: chose to expand scope (storage Dockerfile) rather than bail. Outcome: fix worked end-to-end; storage now builds in CI. Operator post-hoc has the option to disagree. ...

**Open questions / followups** _(line 32)_:

**Is the K8s rollout failure (`elohim-adam-alpha` StatefulSet pod won't become Ready after `kubectl rollout restart` + `--timeout=600s`) something you can investigate from the cluster side, or do you want me to expand scope to include diagnosing it via Jenkins-side workarounds (e.g., adding a `kubectl logs` capture step to the deploy stage so the next failed deploy surfaces the pod's startup error in the Jenkins console)?** ...

---

### 13. Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`

- **Path**: `.claude/shifts/2026-04-30T22-30-orchestrator-781-recover.sprint-result.md`
- **Date**: 2026-04-30 _(source: filename)_

**Outcome** _(line 8)_:

### Done

- **Final measurement:** 0 (last orchestrator build #790 = NOT_BUILT, treated as pass per cmd; #791 in flight cleared Checkout and dispatched cascade)
- **Stability evidence:** orchestrator's own Checkout success now proven across two distinct fresh-trigger builds (#789, #791). Holochain DNA Checkout proven on #1181 fresh trigger.
- **Landing commits:**
  - `0cee090c ci(checkout): skip default + shallow + 30min timeout to fix repeated SCM fails` — orchestrator + holochain (edge) Jenkinsfiles ...

**What blocked** _(line 96)_:

- **The cmd treats NOT_BUILT as 0 (pass).** This was tolerated for this Objective but is worth tightening for next shifts: NOT_BUILT means "nothing dispatched," not "system healthy." If a build is superseded (mid-shift webhook double-fire), the next poll reads the superseded build's NOT_BUILT and falsely passes. Recommend in next shift: cmd should also check that the lastBuild's commit matches HEAD (or a known-fresh commit), and require a non-NOT_BUILT result. ...

**Decisions / pivots** _(line 87)_:

- **Iter 2 (`26de3f7f` empty retrigger):** chose to test the transient hypothesis with a low-cost retry before paying the iteration cost of a code change. Burned ~12min for a clearer diagnosis (it was NOT transient). Right call given the 1st-iteration evidence pointed at a one-off network blip.
- **Iter 3 → push pre-push gate failed twice:** disk filled to 100% from prior cargo builds; `cargo clean` on `elohim-storage` + `doorway-service` freed 21G but left other corruption; second push failure was rustc 1.95 strictness on `tokio-tungstenite 0.27.0` (unrelated to my Jenkinsfile change, same cl ...

**Open questions / followups** _(line 104)_:

1. **Validate the cascade completes.** With the structural fix in place, holochain DNA #1181 should run ~50 min, then orchestrator #791 dispatches Level 1 (elohim, elohim-edge) ~30min each, then Level 2-3. Operator can monitor — if any fails, those failures need separate diagnosis.
2. **Tackle the `tokio-tungstenite 0.27.0` rustc 1.95 issue.** Surfaced in iter-3 pre-push hook. Same class as `b8dea7a2 fix(steward-node): clippy 1.95.0 lint drift`. Update tokio-tungstenite or pin Rust toolchain. ...

---

### 14. Sprint Result — `2026-04-29T00-15-alpha-blob-deploy-as-expected`

- **Path**: `.claude/shifts/2026-04-29T00-15-alpha-blob-deploy-as-expected.sprint-result.md`
- **Date**: 2026-04-29 _(source: filename)_

**Outcome** _(line 8)_:

### Bailed

**Bail reason:**

The four planned shift changes — seeder data-integrity fix (`e9c4cdd2`), ingress `proxy-body-size: 100m` annotation across alpha/staging/prod (`cb89d36f` + `6e44c64c`), build-graph `deploy-manifests` step in `elohim-edge` (`ee4c3517`), and `schema-dna` source narrowing to `elohim/holochain/dna/**` (`bf753b2c`) — are all on `dev`, picked up by orchestrator 779, deployed by elohim-edge #926 (`/version` advanced from `9a178c5` → `6e44c64c`), and the cascaded elohim-genesis #964 re-seed completed in 3.4 min with **bulk content POST clearing the 413** (annotation works ...

**What blocked** _(line 67)_:

- **Iterations 1–5:** measurement (blob 404) trusted at face value; no oracle-skepticism. Correct call — the 404 was real, just not for the reason the shift's planned scope addressed.
- **Iteration 6:** /storage/blob and /admin/seed/blob returned `200 + 9986 bytes text/html` initially looking like 200s, but the size + content-type matched the SPA `index.html` fallback (the "Connecting" landing page) — same pattern emitted for any unmatched GET when `root_app_slug` is configured. ...

**Decisions / pivots** _(line 61)_:

- **Iteration 1:** observed pre-deploy state (`/version=9a178c5`, blob 404). Skipped Sonnet verification — direct GET on public endpoint, high confidence. Outcome: correct read; deploy was indeed pre-shift-changes.
- **Iteration 2:** distrusted "orchestrator picked up the change" assumption; pulled `/consoleText` on builds 774 + 776 unauth. Found stranded changes: cb89d36f's manifest yamls had been absorbed into `__global__` baseline `f738aa66` so subsequent diffs didn't include them. ...

---

### 15. Sprint Result — `2026-04-28T03-13-orchestrator-clean-deploy`

- **Path**: `.claude/shifts/2026-04-28T03-13-orchestrator-clean-deploy.sprint-result.md`
- **Date**: 2026-04-28 _(source: filename)_

**Outcome** _(line 8)_:

### Bailed — partial-done

**Bail reason:** Three of four done criteria met. The fourth (genesis ≥UNSTABLE) is blocked by a **pre-existing NetworkPolicy regression that started ~84 hours ago** (genesis #946 was the last green at 157h; #947–#953 all FAILURE). The regression is independent of the original DEPLOY_ONLY/CPS bugs this shift was scoped against, and unblocking it requires either a NetworkPolicy change (security-sensitive, needs design review) or a broader genesis pipeline refactor across multiple call sites (lines 528, 573, 578, 592, 606, 610, 619, 650, 672, 683 in `genesis/Jenkinsfil ...

**What blocked** _(line 82)_:

- Iteration 7: Initial Bash check showed `#757 dur=3357s` while the previous read had been `dur=3297s` (+60s) when ScheduleWakeup said it had slept ~25min. Either Jenkins API caching or a /loop manual re-invocation; either way the data was consistent and not misleading.
- Iteration 7 (bail rationale): Used external curl through doorway-alpha proxy as ground-truth oracle for "is /db/stats actually working" since direct cluster-internal probing isn't available from Eclipse Che. ...

**Decisions / pivots** _(line 74)_:

- Iteration 1: Acted on hypothesis without Sonnet verification — bug shape was mechanical (env-file overwrite) and CLAUDE.md context made the fix path obvious. Worked.
- Iteration 2: Ran observe-only on #753 + side-fix Staging/Prod for consistency. Held the side-fix push to avoid `abortPrevious: true` aborting the in-flight matrix.
- Iteration 3: Promoted change-detection validation to a standing skill principle (Principle 7) and added the `[deploy-only]` autodetect feature in the same iteration. Two changes in one iter; clean separation by file. ...

---

### 16. Sprint Result — `2026-04-28T13-41-genesis-seeder-unstable`

- **Path**: `.claude/shifts/2026-04-28T13-41-genesis-seeder-unstable.sprint-result.md`
- **Date**: 2026-04-28 _(source: filename)_

**Outcome** _(line 8)_:

### Done

- **Final measurement:** 0 (target `== 0`, baseline was 1)
- **Stability evidence:**
  - Genesis #956 (UNSTABLE, 0 seeder errors) on fresh trigger from push `6de06bb2` + retrigger `bd66fdee` → orchestrator #759
  - Genesis #957 (UNSTABLE, 0 seeder errors) on fresh trigger from push `644497ab` + `25d4794d` → orchestrator #763
  - Both UNSTABLE results came from an unrelated Cucumber expression parsing error in E2E verify ("the portal host responds to /healthz with 200" — Cucumber interpreted `/` as alternation operator). Not seeder, not in shift scope.
- **Landing commits:** ...

**What blocked** _(line 79)_:

- **`/api/json` lag vs `lastBuild/api/json`**: at one point `result: "RUNNING"` was reported on the orchestrator while `wfapi/describe` showed all stages SUCCESS. The wfapi reflects current execution state instantly; the result field on `/api/json` lags by minutes. Trust wfapi for stage-level state; trust the per-build `/api/json` for final result.
- **`/builds[]` array vs `lastBuild`**: `/builds[]{0,3}` returned `955, 954, 953` even after `lastBuild` had advanced to `956`. The `builds[]` snapshot is cached more aggressively than `lastBuild`. Use `lastBuild` for "what just happened." ...

**Decisions / pivots** _(line 71)_:

- **Iteration 1**: acted on hypothesis without dispatching Sonnet for verification. Code-level evidence (read of `bulk_create_content`, `db/mod.rs::SqlitePragmas`, signal subscriber background tasks) was direct and unambiguous; verify-pass would have cost a cache window for low marginal value. Worked.
- **Iteration 1.5**: Harbor outage during build; chose "wait + diagnose Harbor health" over "bail immediately." Health-endpoint probe correctly diagnosed `registry`/`registryctl` connection-refused (cluster-internal, not transient). Surfaced via PushNotification; operator recovered. ...

---

### 17. Sprint Result — `2026-04-27T03-56-all-pipelines-green-or-unstable`

- **Path**: `.claude/shifts/2026-04-27T03-56-all-pipelines-green-or-unstable.sprint-result.md`
- **Date**: 2026-04-27 _(source: filename)_

**Outcome** _(line 8)_:

### Bailed

**Bail reason:** Edge pipeline's `Deploy Edge Node - Alpha/Staging/Prod` stages fail with kubectl `Forbidden: system:serviceaccount:jenkins:jenkins-deployer cannot get persistentvolumeclaims/deployments/services in namespace elohim-alpha`. The repository contains zero RBAC manifests (`grep -rn "jenkins-deployer\|RoleBinding" genesis/ elohim/holochain/` returns nothing) and `git log --diff-filter=D` shows no deletion of an RBAC file in history. ...

**What blocked** _(line 75)_:

*(none — measurements consistently aligned with WebFetch'd Jenkins state across all 5 iterations)*

**Decisions / pivots** _(line 67)_:

- **Iteration 1:** skipped Haiku for the post-push observation — direct WebFetch returned authoritative state (`BUILDING`, prior `FAILURE`) in 2 lines, summary cost > value.
- **Iteration 2:** chose wire-type change over test-body edit. Wire types had zero downstream consumers in doorway/, app/, elohim-storage/ (verified by grep) so the change is safe; test bodies are judge-protected. ...

---

### 18. Sprint Result — 2026-04-20T02-30-elohim-edge-docker-green

- **Path**: `.claude/shifts/2026-04-20T02-30-elohim-edge-docker-green.sprint-result.md`
- **Date**: 2026-04-20 _(source: filename)_

**Key learnings** _(line 66)_:

- **`feedback_shift_measure_jenkins.md`** (saved earlier this session) — Shift measures on Eclipse Che MUST use Jenkins MCP / API, never local `docker` / `kubectl` / full-stack commands. Lesson paid immediate dividends on iteration 2 of this shift.
- (To be added in a future session if these patterns recur) — *Stage-scoped measures for stage-scoped Objectives.*

**Open questions / followups** _(line 52)_:

1. `doorway-app-quality-gate-green` — scope: `doorway/doorway-app/**`. Measure: same helper against a fresh `elohim-edge/dev` build, but accepting UNSTABLE on "P2P Simulation Test" while requiring SUCCESS on "Quality Gate: Doorway App". Likely 1-3 iteration shift against ESLint/prettier debt.
2. `elohim-edge-p2p-sim-green` — scope: `elohim/holochain/Jenkinsfile` + the test harness invoked by that stage. Measure: stage-level check on "P2P Simulation Test". Likely 1-3 iterations depending on whether it's a test harness bug or real P2P instability. ...

**Iteration trajectory** _(line 11)_:

| # | Type | Build | Result | Duration | Stage outcome relative to root cause |
|---|------|-------|--------|----------|---|
| 1 | observe + act | #893 | FAILURE | 50s | first fix landed (COPY gate-client); exposed second failure (workspace inheritance) |
| 2 | observe + bail | #894 | UNSTABLE | 33m42s | all doorway docker stages SUCCESS; 2 pre-existing unrelated stages UNSTABLE |

---

## Cross-cutting themes

Words appearing in 3+ distinct sprints' titles/objective/status. Surface-level word-frequency only; not semantic analysis. Use as a hint about what's recurring — confirm by reading the cited sprints.

| Word | Sprints |
|------|---------|
| `orchestrator` | 6 |
| `dev` | 5 |
| `cascade` | 4 |
| `four` | 4 |
| `holochain` | 4 |
| `two` | 3 |
| `fresh-trigger` | 3 |
| `surfaces` | 3 |
| `light-up-the-topology` | 3 |
| `evidence` | 3 |
| `returns` | 3 |
| `alpha` | 3 |
| `elohim` | 3 |
| `first` | 3 |
| `final` | 3 |
| `reached` | 3 |
| `dna` | 3 |
| `deploy` | 3 |
| `reason` | 3 |
| `genesis` | 3 |

## Open questions (aggregated across sprints)

Every individual item from each sprint's open-questions / followups section. Gold for the operator — these are what the shifts wanted the next iteration to address.

- **Sprint Result — `2026-05-07T00-47-storybook-stage-green`**: Configure shallow clone (`shallow: true, depth: 1`) on the storybook (and likely all) edge-node Jenkins agents.
- **Sprint Result — `2026-05-07T00-47-storybook-stage-green`**: Add a `gitcache` reference repo on the edge node to skip the full clone cost.
- **Sprint Result — `2026-05-07T00-47-storybook-stage-green`**: Investigate kubelet eviction policy / network-policy throttling on `node-type: edge`.
- **Sprint Result — feedback-dialogue-panel**: Alpha SSR/ingress routing: `/lamad/index.html` returning nginx welcome — separate ops concern
- **Sprint Result — feedback-dialogue-panel**: Cucumber strict console-error checker still masks @feedback-gate scenarios on alpha-doorway flake (iter-3 known gap, unchanged)
- **Sprint Result — feedback-dialogue-panel**: Genesis pipeline didn't get dispatched this round because elohim-edge failed at Level 0; once edge is fixed, genesis will run automatically on next push
- **Sprint Result — `/deliver light-up-the-topology` (third pass)**: **seed-agent-bindings**: wire into `genesis/Jenkinsfile` after seed-accounts. Build CONDUCTOR_URLS from deployments.json (filter !suspended), passing `ws://elohim-${name}-alpha:4445` for each non-suspended human. Matthew gets `[desktop, node, steward]` per the script's plan.
- **Sprint Result — `/deliver light-up-the-topology` (third pass)**: **seed-commitments**: wire into `genesis/Jenkinsfile`. REST-only via DOORWAY_URL (already proven works from local). Alone, this populates D4 with shape but not Matthew-specific inflow/outflow.
- **Sprint Result — `/deliver light-up-the-topology` (third pass)**: **NEW: peer-pair commitments**: extend seed-commitments.ts to also create cross-household commitments (Matthew→Adam, Adam→Matthew, Matthew→Pete, Pete→Matthew, etc.) — not just `receiver: "network"`. This populates D3 peer-household-cards AND D4's inflow/outflow rows.
- **Sprint Result — `/deliver light-up-the-topology` (third pass)**: **NEW: blob-backed seed content**: at least one content item with a real blob (e.g., one of the PDF/markdown chapters as a blob-backed resource). Required for D1 (visitor distribution badge) and D6 (side-by-side coherence).
- **Sprint Result — `/deliver light-up-the-topology` (third pass)**: **conductor-identities pre-flight**: confirm seed-conductor-identities is needed on alpha (some humans may not have Agent EPR yet) — verify via `imagodei.list_agents` zome query on each pod.
- **Sprint Result — doorway-ssr-deliver-2026-05-07T23-37**: **Concept content rendering** — Angular route's data resolver doesn't synchronize with SSR. The shell renders but `<article>` body and `<title>` aren't populated. Likely needs `transferState` + `provideClientHydration` + sync-aware data resolver. ...
- **Sprint Result — doorway-ssr-deliver-2026-05-07T23-37**: **Memory monitoring on the SSR pod** — limit is now 1Gi but observed working-set during V8 parse is ~200MB. Worth instrumenting actual usage (Prometheus/Grafana) to see if there's room to tune back down without re-introducing flake.
- **Sprint Result — doorway-ssr-deliver-2026-05-07T23-37**: **Staging + prod manifests have the same low-memory problem** — `staging.yaml` and `prod.yaml` carry their own resource sections. When SSR rolls to those environments, they'll hit the same OOM. Apply the same memory bump + startupProbe pattern. ...
- **Sprint Result — doorway-ssr-deliver-2026-05-07T23-37**: **The `feedback_cascade_halt_masks_failures` memory** is reinforced by this shift — log it.
- **Sprint Result — doorway-ssr-deliver-2026-05-07T23-37**: **Operator's earlier framing — doorway as full facilitator** — sprint candidate (`project_doorway_full_facilitator_sprint`) to absorb SPA hosting into doorway and retire elohim-site sidecar. ...
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: **Mastery 405 noise** (~13 × POST `/api/v1/mastery` and `/api/v1/mastery/engagement` errors per page load on the lamad landing page after login).
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: Root cause: alpha nginx ingress lacks `/api/v1` proxy_pass → doorway. Same root-cause class as the topology cluster issue from `light-up-the-topology-deliver-cont-2026-05-06T08-30` shift's handoff brief Path A.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: GET on alpha returns 200 with `content-type: text/html` (SPA fallthrough). POST returns 405 because nginx doesn't allow POST on the static-file location.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: Fix candidates: (a) `kubectl apply -f genesis/orchestrator/manifests/elohim-app/alpha/ingress.yaml` if the prior fix landed in source but not deployed; (b) extend ingress.yaml's `/api/v1` rule if the path scope is too narrow; (c) point the elohim-app's mastery client at `doorway-alpha.elohim.host` i ...
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: 1-2 iterations to land. Say so and I'll pick it up.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: **Orphan count drift** (12 → 0 over time). Each affected user's next login self-heals one orphan. No active push needed. Monitor: re-probe `/admin/conductors` over the next few days; orphans should monotonically decrease as users retry.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: **discover_existing_agents boot-time reconciliation** (`main.rs:933` `is_none()` guard). Currently doesn't replace stale entries proactively. Chaperone fix covers reactively, which is sufficient — but a boot-time sweep would pre-empt the 502 entirely. Backlog candidate.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: **Conductor ID format fragility** — positional `format!("conductor-{i}")` makes the orphan situation possible at all. A stable derivation (e.g., from headless service hostname) would prevent recurrence on CONDUCTOR_URLS reorder. Backlog candidate.
- **Sprint Result — `/deliver doorway-conductor-stale-mapping`**: **`wasm/elohim-cache-core/elohim_cache_core.js 404`** — pre-existing 404 on the cache-core wasm bundle (was in original failure log too). Likely a missing static-asset deploy step. Out of scope; hasn't changed.
- **Sprint Result — `/deliver light-up-the-topology continuation`**: From cluster-admin context: `kubectl apply -f genesis/orchestrator/manifests/elohim-app/alpha/ingress.yaml`
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Confirm: `curl -sf -w '%{http_code}\n' https://alpha.elohim.host/api/v1/cluster` should now return 401 (auth required) instead of 200 with HTML
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Push empty commit `[build:genesis]` to trigger genesis browser stage
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Genesis #986 (or whatever) will render the topology screenshots — should now show data, not error
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Read `[build:app]` from commit message
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Set `env.FORCE_BUILD_PIPELINES = 'elohim'`
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Apply force-include to graph-walker result via the new logic at Jenkinsfile:1130-1148
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Dispatch elohim pipeline → applies alpha ingress with /api/v1 route
- **Sprint Result — `/deliver light-up-the-topology continuation`**: GitHub SSH/HTTPS infrastructure slow on the cluster's egress IP (cluster network issue)
- **Sprint Result — `/deliver light-up-the-topology continuation`**: 159 dependabot vulnerabilities (per push warnings) may be slowing GitHub API responses on the repo metadata
- **Sprint Result — `/deliver light-up-the-topology continuation`**: Pod-side: agent-pool exhaustion, slow node, OOM on the JNLP container
- **Sprint Result — `/deliver light-up-the-topology`**: **(a) Auth-cid propagation through doorway proxy** — Spec pre-flight T01 result identified this as PREREQUISITE T28a, never landed. Doorway's `storage_proxy::forward_to_storage` does not decode the JWT and inject `X-Agent-Id` (or `X-Agent-Cid`) header. ...
- **Sprint Result — `/deliver light-up-the-topology`**: **(b) Alpha ingress `/api/v1` route** — iter-5 source-of-truth update landed (commit `e8ecdd00`); cluster reconciliation requires elohim pipeline build (gated on blocker #1) OR manual `kubectl apply -f genesis/orchestrator/manifests/elohim-app/alpha/ingress.yaml`.
- **Sprint Result — `/deliver light-up-the-topology`**: **(c) CORS on doorway** — observed cross-origin error on `https://doorway-alpha.elohim.host/health` from `https://alpha.elohim.host`. May or may not affect topology endpoints depending on whether (b) lands (which makes them same-origin via ingress).
- **Sprint Result — `2026-05-04T22-51-alpha-pipelines-green-no-shem`**: **B1 (operator):** Restore RBAC for `apps/statefulsets/scale` so `jenkins-deployer` ServiceAccount can actually scale orphaned StatefulSets to 0 (cluster-side `Role`/`RoleBinding` patch in `elohim-alpha`, `elohim-staging`, `elohim-prod` namespaces). ...
- **Sprint Result — `2026-05-04T22-51-alpha-pipelines-green-no-shem`**: **B2 (Jenkinsfile, in-scope):** Install `pnpm` in the Quality Gate: Doorway App stage container (or skip the stage with a tag, or move it to a container that has pnpm). Currently UNSTABLE because container doesn't have pnpm.
- **Sprint Result — `2026-05-04T22-51-alpha-pipelines-green-no-shem`**: **B3 (Jenkinsfile, in-scope):** Install `docker-compose` in the P2P Simulation Test stage container (or skip the stage with a tag). Currently UNSTABLE because container doesn't have docker-compose.
- **Sprint Result — `2026-05-04T22-51-alpha-pipelines-green-no-shem`**: **B4 (Genesis Seeder, possibly in-scope):** Either retry-on-conductor-websocket-flake or quarantine Nancy seed. Likely transient; retrigger may pass.
- **Sprint Result — `2026-05-03T18-19-orchestrator-805-pipelines-unstable`**: Bug in the `40e29950` image (e.g., elohim-storage panicking on startup, libsqlite mismatch, missing env var)
- **Sprint Result — `2026-05-03T18-19-orchestrator-805-pipelines-unstable`**: Image pull failure (Harbor throttle, registry auth)
- **Sprint Result — `2026-05-03T18-19-orchestrator-805-pipelines-unstable`**: K8s probe misconfiguration (readiness/liveness firing too early)
- **Sprint Result — `2026-05-03T18-19-orchestrator-805-pipelines-unstable`**: Cluster resource issue (CPU/memory/PVC)
- **Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`**: **Validate the cascade completes.** With the structural fix in place, holochain DNA #1181 should run ~50 min, then orchestrator #791 dispatches Level 1 (elohim, elohim-edge) ~30min each, then Level 2-3. Operator can monitor — if any fails, those failures need separate diagnosis.
- **Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`**: **Tackle the `tokio-tungstenite 0.27.0` rustc 1.95 issue.** Surfaced in iter-3 pre-push hook. Same class as `b8dea7a2 fix(steward-node): clippy 1.95.0 lint drift`. Update tokio-tungstenite or pin Rust toolchain.
- **Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`**: **Reduce duplicate-webhook noise.** Each push triggers 2 builds; the first is always superseded. Likely a GitHub webhook config or orchestrator entrypoint dedup gap.
- **Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`**: **Add `elohim-holochain` job alias note to CLAUDE.md** to prevent the iter-4→5 mistake (job name → Jenkinsfile path).
- **Sprint Result — `2026-04-30T22-30-orchestrator-781-recover`**: **Consider depth:50 for orchestrator + DNA**, since `depth:200` is fairly conservative. orchestrator changeset diffs typically reach baselines within 50 commits unless a pipeline has been failing for many days. Smaller depth = faster fetch.
- **Sprint Result — 2026-04-20T02-30-elohim-edge-docker-green**: `doorway-app-quality-gate-green` — scope: `doorway/doorway-app/**`. Measure: same helper against a fresh `elohim-edge/dev` build, but accepting UNSTABLE on "P2P Simulation Test" while requiring SUCCESS on "Quality Gate: Doorway App". Likely 1-3 iteration shift against ESLint/prettier debt.
- **Sprint Result — 2026-04-20T02-30-elohim-edge-docker-green**: `elohim-edge-p2p-sim-green` — scope: `elohim/holochain/Jenkinsfile` + the test harness invoked by that stage. Measure: stage-level check on "P2P Simulation Test". Likely 1-3 iterations depending on whether it's a test harness bug or real P2P instability.
- **Sprint Result — 2026-04-20T02-30-elohim-edge-docker-green**: `elohim-edge-pipeline-stable` — confirmation shift once (1) + (2) land. Measure as originally authored here. Two consecutive SUCCESS across fresh triggers → done.
