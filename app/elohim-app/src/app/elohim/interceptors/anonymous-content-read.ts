import { HttpContextToken } from '@angular/common/http';

/** Explicit public-reader consent to anonymous requests to discovered sibling doorways. */
export const ANONYMOUS_CONTENT_READ = new HttpContextToken<boolean>(() => false);
