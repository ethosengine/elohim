---
kind: chronicle
status: noted
date: 2026-06-11
ceremony: substrate-currency
surfaces_rewritten:
  - elohim/sdk/domains/avodah/CLAUDE.md
  - app/elohim-app/src/app/avodah/CLAUDE.md
coherence_verdict: GREEN
next_topic_sampled: rendering the avodah service-coordination layer in the work-board app
---

## What changed

First **per-locus** ceremony (pilot of the subject-routing locus graph). Both avodah gospels rewritten.
The decisive driver was the **operator's lens articulation**: domains are not partitions — ONE shared core
(EPR + REA + reach), each domain a cohesive **lens** reinterpreting it (avodah = *the process as canon*,
the Cybersyn control room). That resolved the four-lens deep-read's central confusion: the 40-struct
service/flow/insurance types crate is not "misfiled economics" but the process lens's fuller vocabulary;
the work-board is its rendered demonstrator slice. Cartographer surfaced the manifest's R-in-REA resources
and claims machinery (all verified against `manifest.json`); historian/storyteller drove the
demonstrator-vocabulary convergence; the librarian's de-rating of relative-path noise held. Lens model
canonized in `2026-06-11-subject-routing-locus-graph-design.md` §2b.1.

## Coherence-check sampling

Topic: rendering the service-coordination layer. Verdict GREEN after adjudication: the coherence agent's
YELLOW rested on 4 "stale fingerprints" computed with the wrong convention (full-file sha256 vs the tool's
body-only 16-hex) — disproven byte-for-byte by `cite-gen --verify`. Adversarial refuters: domain gospel
REFUTED-0; pillar gospel one real refutation — the drift-watch claimed `'work'` is not a valid REA action,
but `REA_ACTIONS` (integrity zome, 25 actions) includes it; corrected to the true reconciliation (code emits
`'work'` where the manifest coupling declares `'produce'`; the wire-schema's 5-action description string is
incomplete, not an enum). Three dead memory anchors (pair-off casualties) retired from the pillar.

## Wisdom worth carrying forward

Two ceremony rules earned by this pilot: (1) **category/notarization claims require a substrate citation**
(manifest/schema/zome wiring) — the cycle's only fictions were confident uncited assertions ("Category C",
two invented memory slugs); (2) **verifier agents must use the citation tooling** (`cite-gen --verify`),
never ad-hoc hashing — the convention (body-only, frontmatter-excluded, 16-hex) is not guessable. Drift
propagation was exercised live twice: domain edit → pillar cite STALE in `locus-drift` → verified
edge-granular `--refresh` → stasis.
