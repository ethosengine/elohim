---
id: "chronicle-substrate-currency-pollution-regulation"
kind: chronicle
status: noted
date: 2026-06-28
ceremony: substrate-currency
surfaces_rewritten:
  - .claude/agents/rust-architect.md
  - doorway/doorway-service/CLAUDE.md
  - .claude/agents/librarian.md
  - .claude/scripts/memory-kit/CLAUDE.md
  - .claude/skills/memory-kit/SKILL.md
  - .claude/skills/elohim-epr-metafile/SKILL.md
coherence_verdict: YELLOW
next_topic_sampled: author an .epr-meta rule over a code directory · add a doorway endpoint backed by a storage view
---

## What changed

Six gospel surfaces rewritten across two clusters. **Backend spine** — `rust-architect.md` (cartographer/historian drove it: absorbed the Automerge content-sync plane + the `h_app_id="elohim"` mismatch class, `DiversityAwarePlacementStrategy`, two-level transport selection, the frozen iroh pin-ceiling, four new anti-pattern guards) and `doorway/doorway-service/CLAUDE.md` (Key-Files 20→25 rows, the `is_service_path` two-gate rule, edge-bake COPY trap). **Memory-kit / pollution-regulation** — the operator elevated a new first-class concern mid-ceremony: *"Pollution regulation for agentic-developer context"* (source regulation at the emitter — `.epr-meta` gates + PreToolUse/PostToolUse hooks as prevention, the sweep as remediation), now canon across `librarian.md`, `memory-kit/SKILL.md`, and `memory-kit/CLAUDE.md`. Then a second operator turn (ponytail review) added a code-directory governance section to the `elohim-epr-metafile` skill (the YAGNI ladder) and drove the SKILL section from 44 lines down to 12 — *the rule that asks for minimal code must itself be minimal*.

## Coherence-check sampling

Two fresh Explore agents (not the memory team). Backend cluster ("add a doorway endpoint + storage view"): **GREEN** — 44 tool-uses of code verification confirmed sync ALPNs, the 5-variant `TransportAffinity`, iroh `=0.92`/`=0.94`, `is_service_path` consistency. Pollution-regulation cluster ("author an `.epr-meta` over a code dir"): **YELLOW** — division of labor clean; one dead cite (`feedback_managed_surface_edit_discipline.md` → `[[project-memory-in-repo-two-tier]]`) fixed inline; one cosmetic naming-surface note deliberately left (the librarian heading is action-scoped; the body carries the umbrella name — adding more would re-bloat against the minimalism steer).

## Wisdom worth carrying forward

- **Pollution regulation for agentic-developer context** is now a named first-class memory-kit concern (prevention-at-the-emitter), peer to the hygiene sweep.
- `.epr-meta` governance extended from doc-shaped to **code-shaped** (over-engineering / YAGNI) in the `elohim-epr-metafile` skill — but that skill file is **UNTRACKED in git** (`??`, not ignored); needs `git add` to be captured.
- Minimalism is self-referential: the heavy v2 pollution-regulation section was itself the anti-pattern it warned against; the lean v3 is the fix.
