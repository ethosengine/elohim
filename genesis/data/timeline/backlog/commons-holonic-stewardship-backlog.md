---
id: "backlog-commons-holonic-stewardship-backlog"
kind: "backlog"
contentType: "backlog-item"
contentFormat: "markdown"
title: "Commons + holonic stewardship backlog — custody vs ownership, nested elohim ceilings, and credential-as-lens"
slug: "commons-holonic-stewardship-backlog"
written: "2026-08-05"
author: "claude (operator design session, 2026-08-05)"
status: "backlog"
priority: "medium"
stewardship-frame: bounded
tags: [commons, stewardship, custody, holonic, sociocracy, governance, credentials, lamad, qahal, rea]
cites:
  - genesis/research/playnet-free-association-cross-pollination-2026-08-05.md
  - genesis/research/meadows-systems-dynamics-cross-pollination-2026-08-11.md
  - genesis/data/timeline/backlog/measure-family-borrows-backlog.md
  - genesis/docs/superpowers/specs/2026-06-27-plural-mishpat-lenses-over-epr-design.md
  - genesis/docs/content/elohim-protocol/global-orchestra.md
  - genesis/docs/content/elohim-protocol/architecture/governance-layers-architecture.md
  - genesis/docs/content/elohim-protocol/architecture/2026-05-23-multi-collective-collaboration-epr-design.md
  - genesis/docs/superpowers/specs/2026-06-09-wisdom-layer-floor-ceiling-judgment-culminating-design.md
  - genesis/docs/superpowers/specs/2026-08-04-middot-measure-primitive-design.md
---

# Commons + holonic stewardship backlog (operator design session, 2026-08-05)

**How a holon holds standing** — over resources it stewards, and over the credentials it issues.
Emerged from the [Playnet survey](epr:playnet-free-association-cross-pollination-2026-08-05) mint
pass when the operator pushed the credential question past measurement into governance. Rows 1–2 are
Playnet-derived borrows; the rest are our own design frontier, captured here before they evaporate.

Sibling of [measure-family-borrows](epr:measure-family-borrows-backlog) — that cluster decides *what
is measured*, this one decides *who has standing to judge it and to hold what it is about*.

## The two steering notes that govern this cluster (operator, 2026-08-05)

**1. Ownership is the enclosure-flavoured sibling of sovereignty, and needs the same guard.** The
protocol subordinates *ownership* to *stewardship + custody* exactly as it subordinates *sovereignty*
to community-backstopped identity. A guard now exists —
`ownership-ontology-guard@1` in `.claude/epr-meta/policies.yaml`, realized by
`epr:validator-ownership-ontology-guard`, bound on the canon tree, escape hatch
`stewardship-frame:` — mirroring the sovereignty guard's shape (net-new-only, `ask`, affirm-or-reframe).

**2. The elohim ceiling is itself holonic/sociocratic — not one global cap.** This corrected a
flattened reading during the session. **Every holon has its own elohim and its own ceiling**: Eagle
Scout Trevor has an individual ceiling, unit 210 has its own, Scouting America has its own. And the
elohim **move** — carrying payload, context, signals, and measures — **informing policy up and down
the network**, bidirectionally. Authority is not a cap imposed from above; it is each holon's own
policy surface, negotiating with its neighbours. This is the VSM recursion (each viable system
contains and is contained by viable systems, each with its own identity/policy tier) and it is the
same shape the Value Scanner epic already narrates when personal ↔ family ↔ community ↔ global
elohim negotiate an olive-oil purchase in milliseconds.

| # | Item | What + why (grounded) | Gate/blocker | Owner shape |
|---|------|----------------------|--------------|-------------|
| 1 | **Rights vs custody split — `primaryAccountable` ≠ `custodianScope`** | **We have ZERO hits** for `primaryAccountable`, `custodianScope`, or `transferCustody` across `elohim/` and `bridges/` (verified 2026-08-05). ValueFlows already specifies the split and Playnet's planner implements it verbatim. Without it we can only model ownership — and collapsing custody into ownership **is** the enclosure failure the Georgist common-inheritance framing exists to prevent. The load-bearing case: *the commons holds the rights, unit 210 holds custody of the woodland*, and custody must be transferable **without the rights ever moving**. A holon that "owns" the commons it stewards has enclosed it | p2p-design-gate; needs an `EconomicResource` writer, which we lack | rust-architect |
| 2 | **`transferCustody` and `transferAllRights` as distinct actions** | The action pair that makes row 1 operable — handing over stewardship is a different economic event from handing over rights, and conflating them is how a commons quietly becomes property. Two of ValueFlows' 19 canonical actions; Playnet carries the full table with a behavioural effects matrix (`accountingEffect`, `onhandEffect`, `custodyEffect`) worth reading before we design ours. **⚠ CARVE-OUT (red-team 2026-08-05): freely-transferable-custody is correct for a woodland and is the ATTACK for an inalienable subject.** Non-consensual sharing of an intimate image *is precisely* "transfer custody without moving rights." For subjects under row 2a it must be **prohibited, not gated** — a consent gate cannot help, because a coerced approval is indistinguishable at the substrate from a free one | Follows row 1; blocked on row 2a's subject class | rust-architect |
| 2a | **`subjectStanding` — the missing third relation (who this is *about*)** | ValueFlows gives rights (`primaryAccountable`) and custody (`custodianScope`) and **has no relation for the person the data depicts**. A photo of Alice on Bob's device: Alice appears in neither field, so the whole rights/custody apparatus can be perfectly satisfied while she has no standing at all. Proposed `subjectStanding` — **plural** (group photos), **either-veto-suffices**, and **inalienable** (cannot be transferred, sold, or out-voted). This is the relation that makes intimate data expressible without importing the property frame | p2p-design-gate; **operator decision** on the inalienable class | rust-architect + operator |
| 3 | **A steward is a PATH, not an entity** | `Scouting America / unit 210`. A credential or a custody claim names both the **issuing holon** (the one that actually witnessed) and its **lineage** (where its standing derives). Subsidiarity: the most local holon that can witness, does; authority derives upward, witnessing happens downward. Flat `steward_id` fields cannot express this and will have to be migrated | p2p-design-gate; identity-lineage (C9) is the concern to answer | rust-architect + qahal |
| 4 | **Recursive lens conformance — the same topology as skills** | Unit 210's lens must conform to Scouting America's, which conforms up to its ceiling. This is Playnet's `satisfied-by` disjunction ([measure-family](epr:measure-family-borrows-backlog) row 9) applied one level up: not "which skills satisfy this requirement" but "which local standards satisfy this parent standard." **One conformance mechanism doing triple duty** — skills, credentials, governance nesting. Lets a local unit be stricter or differently-shaped without forking the badge's meaning | Needs lens contract halves + row 5 | rust-architect |
| 5 | **Per-holon elohim ceiling (steering note 2)** | Model the ceiling as a **per-holon policy surface**, never a single global constant. Trevor, unit 210, and Scouting America each hold one. A local holon sets its own standard *within* bounds it cannot breach — which is the answer to "why doesn't holonic nesting just produce 1,000 incompatible badges": they are incompatible in **shape**, bounded in **kind**. Canon home for the axis: [floor/ceiling judgment](epr:wisdom-layer-floor-ceiling-judgment-culminating-design) | Design work; touches the constitutional layer | rust-architect + operator |
| 6 | **Elohim as travelling context/measure carriers** | The elohim move *with* payload, context, signals, and measures, informing policy **up and down** the network. So a measure observed at Trevor's level can inform unit 210's policy, which can inform Scouting America's — and constraints flow back down. Signal-upward-influence is as real as authority-downward. This is the story+value+governance coupling in motion, and it is what makes the ceilings negotiate rather than merely cap | Needs the measure substrate ([measure-family](epr:measure-family-borrows-backlog) row 1) | rust-architect |
| 7 | **Sociocratic double-linking as the legitimation of ratification** | A child holon holds a voice in its parent's circle and vice versa, so the parent's acceptance of unit 210's lens is **consent flowing both directions**, not imposition. Without double-linking, row 4's conformance check is just a superior approving an inferior. Canon: `governance-layers-architecture.md`, `2026-05-23-multi-collective-collaboration-epr-design.md` | Needs an agent-relationship shape | qahal |
| 8 | **Credential-as-lens (the Eagle Badge shape)** | A credential is a **bespoke lens positioned in the social context of the graph**, whose predicate reads a heterogeneous **measure vector** (5 h · 1500 energy · 14 story points · 200 W compute · 1 paper @ 20% synthesis · $1000 materials · 1 prototype · 3 verified deliverables) and whose authority comes from its steward's standing — never from the measures themselves. Middot's law does the separation: *measures never carry teeth; governance lenses compose them*. Gate output recorded below | Blocked on [measure-family](epr:measure-family-borrows-backlog) row 1 | lamad + rust-architect |
| 9 | **World3 as a *lens over* an aggregated commons dataset — the Global Orchestra's deliberation surface** | [Meadows](epr:meadows-systems-dynamics-cross-pollination-2026-08-11) §7 — the survey's largest take, and a **correction to an earlier draft that wrongly filed global modelling under LEAVE**. The no-global-clock constraint bounds *synchronization*, not *aggregation*; a rollup needs only honest attribution and an order-independent fold. The machinery already exists: [plural Mishpat lenses](epr:plural-mishpat-lenses-over-epr-design) executing in parallel per EPR (context-rich selection decides which lens *governs* locally, plurality lets a global fan-out be answered under each node's own valid lens), its **two-layer law** putting rollups on the aggregate-projection layer rather than the T1 notary floor, and [select→fold→aggregate](epr:resilience-facings-select-fold-aggregate-design) as the fold. **The inversion:** World3 was one model with one parameterization computed centrally — here the *dataset* is primary and World3 is one lens over it, live alongside the Donut and planetary boundaries, swappable when a parameterization ages out. That is the plural-lens anti-regime-drift argument applied to global models, where it bites hardest (a 1972-right / 2020s-wrong prescription is the canonical case). Measure half: [measure-family](epr:measure-family-borrows-backlog) row 15 | **Slice-1 dependency landed 2026-08-11** ([measure-ontology-slice1-epr-local-first](epr:measure-ontology-slice1-epr-local-first) Tasks 1–6): measure-family rows 12–13 (kind + rate/period vocabulary, honest-absence interval, propagating fold) are proven locally by [measure-dynamics-confidence-ontology-design](epr:measure-dynamics-confidence-ontology-design) L1/L3/L5/L6. That spec's §4 "What slice 2 inherits" is now the authoritative gate for this row — it names 5 items row 9 still needs before its network rung (per-fold anonymity/row 10, correlation-aware interval arithmetic, cross-peer determinism at a band edge, statistical-method application at the ceiling, and this row's index-lens work itself needing all four) plus a still-missing typed `Quantity → canonical bytes` entry point. Design position, not a build — still needs an orchestra-facing scope pass | rust-architect design pass + operator |
| 10 | **Per-fold anonymity budget — k-anonymity floor / DP noise as a *depleting stock*** | The unsolved half of row 9, and the place these systems actually fail: a carbon rollup over a sparse holon re-identifies its members, and household attribution is near-automatic for exactly the measures the orchestra most wants. Needs a floor or noise budget **per fold level**, and the noise budget is itself a stock that depletes under repeated querying — so it wants the same `kind: level` treatment as everything else ([measure-family](epr:measure-family-borrows-backlog) row 12). Named as an open question in the survey rather than solved | **Blocks row 9's network rung**; red-team pass wanted before any design | red-team → rust-architect |
| 11 | **Power as a published numeraire — stock, flow, and distribution** | [Meadows](epr:meadows-systems-dynamics-cross-pollination-2026-08-11) §3.8: *"The 'forbidden numeraire,' whose stocks, flows, and distribution could lend itself to indicators, is **power**"* (Indicators p.61) — her observation being that we happily denominate in money, time, and information, and leave unmeasured the thing actually contested. We have a great deal of **governor** and no **gauge**: friction-gradient limitarianism, `validate_ratifies_limit_gradient`, and the per-substrate limitarian governor all *act on* concentration; none *publishes* it as a readable measure a member of the commons could form a judgment about before any mechanism fires. Bears directly on ratification-as-peer-acceptance and on the starved-collective read — both require the commons can **see** the distribution. A governor acting on a quantity nobody can read is a mechanism asking to be trusted, which is what [elohim-ceiling](epr:elohim-ceiling-design) Principle 8 rejects | Needs [measure-family](epr:measure-family-borrows-backlog) row 12; governance-sensitive, so operator-gated | rust-architect + operator |
| 12 | **Erodible carrying capacity — capacity as a state variable, not a parameter** (STUDY) | [Meadows](epr:meadows-systems-dynamics-cross-pollination-2026-08-11) §3.4: overshoot degrades the limit itself, so a commons' capacity is a stock with its own inflow and outflow rather than an exogenous constant. Today `CarryingCapacity` (`spatial.rs:165-181`) is a static JSON estimate with provenance (`data_quality`, `source`, `measured_at` — the good part) that **nothing ever decrements**; only a human re-measurement can move it. Held at STUDY deliberately: declaring that a commons' capacity has been *permanently* reduced is a judgment `values-forward` reserves for the human sortition floor, so the arithmetic must not front-run the governance | Design + governance question; do not build ahead of the floor | study-tier |

| 13 | **Index lenses — World3, GNP, GDP, Gini, HDI, Donut, planetary boundaries — as a named projected-lens deliverable of the Global Orchestra epic** | Operator dispatch 2026-08-11, extending row 9. The classic macro indices become **lenses over the aggregated dataset**, not competing datasets: each a named, versioned, deterministic procedure whose reading is recomputable from the signed log. That is what makes *"there are lies, damn lies, and statistics"* tractable — a GDP figure whose lens CID, inputs, and fold are all walkable is arguable in a way a published national statistic is not. Naming GNP/GDP explicitly alongside World3 matters: the orchestra must be able to render **the incumbent measures on its own substrate**, both so their construction becomes inspectable and so a council can compare them against the Donut or a care-weighted lens *over the same underlying observations*. Meadows' GNP critique is the standing argument for why the comparison is the point. Gate output for the lens + computed-value pair is recorded in [measure-family](epr:measure-family-borrows-backlog) | Rides row 9; dozens of lenses, trivially inside the T1 budget | rust-architect + operator (epic deliverable naming) |
| 14 | **Statistical-method application as an elohim-**ceiling** deliverable, not a substrate feature** | Operator dispatch 2026-08-11. Choosing and applying a statistical method to reconcile estimates is a **judgment act with consequences**, so it belongs at the ceiling under Principle 8 (*audit the guardian; do not trust the guardian*) — declared method, declared inputs, reproducible, contestable. **Compose, don't fork:** this is already [graduated-rigor](epr:computation-attestation-graduated-rigor-design)'s exact shape, and that spec's own worked example is a statistical method (Polis-style PCA clustering) contested by a participant and escalated to *Audit* with Merkle-rooted inputs and a pinned algorithm version. Wire the confidence ontology to it: a **contested narrow interval escalates a rigor tier**, the same way a contested clustering does. No new entity | Needs [measure-family](epr:measure-family-borrows-backlog) rows 16–17; ceiling-spec follow-on | rust-architect + operator |
| 15 | **The "fruits" thesis — record why this cluster exists** (operator, 2026-08-11) | Not a build item; the standing rationale rows 9–14 serve, kept where the work is so it is not re-derived. *Elohim — being the nature of AI — give us a way of making what was opaque before intimately more **interpretable, inspectable, explorable, and honest**.* Statistics were the canonical opaque authority ("lies, damn lies, and statistics") precisely because the cost of inspecting a method exceeded almost everyone's budget; the marginal cost of surfaced, patient explanation now trends toward zero. **Those fruits are the point of the whole protocol**, and this cluster is where the ecological/economic aggregate half of them lands. The design test that follows: *could an ordinary member of this commons walk from a published number back to the observations that made it, and disagree with the method?* If not, the lens has failed regardless of its arithmetic | — | — |
| 16 | **Agent-capability attestation — fit-for-purpose model/agent selection as a policy lens over imported benchmark measures** | Operator directive 2026-08-15. Concrete trigger: the repo's `blind-reader` and `scribe` agents are pinned to model ID `claude-opus-4-6` for prose accessibility — Opus 5 reasons better about complexity but writes less accessibly — and a hardcoded model ID is the degenerate form of this row: a fit-for-purpose preference with no attestation, no lineage, no way for policy to select differently. The generalization: "which model for this capability" is a standing question, not an ops-config question — who may fill a role, on what witnessed evidence, under whose policy — which is this cluster's subject. A role declares a needed capability; candidate fillers — AI models and humans alike — hold capability attestations. Selection is relative preference, illustratively: accessible-writing → Opus 4.6 @ .86, Gemini 3.2 @ .80, Opus 5 @ .75, GPT 5.2 @ .70. Scores are **imported measurements**, not self-produced — externally-witnessed benchmark results from socially-relevant benchmarking communities, accepted and re-attested onto the DHT as durable attestations. Which benchmarks count and how they rank is chosen as policy per elohim council — a Mishpat policy lane, never a global constant. Row 8's law exactly: *measures never carry teeth; governance lenses compose them* — a benchmark score is a measure; "preferred filler for role X" is a lens reading it. Structurally a `CredentialLens` (row 8's Eagle-badge shape — the P2P design gate recorded at the bottom of this file already covers the entity classes, so no new gate pass is claimed); attester lineage is row 3's steward-as-path; per-council divergence is row 5's per-holon ceiling. Any filler holds a DID-like portable identifier; attestations couple to the DID, never to the substrate artifact beneath it. As AI agents become evolutionarily dynamic — dynamic or composed models — witnessed capability proofs accruing per unique DID are what make identity composable: a composed agent carries its own proofs even though its substrate changed. A human's identity already works this way (no weights to benchmark; the DID accrues witnessed proofs over time), so this is one pattern for any agent — and that symmetry is part of why row 16a's inclusion requirement holds structurally, not just as policy. Work under a selection carries auditable lineage to the performer — shape deliberately unfixed, functional description only. Horizon: once preference is a policy lens rather than a hardcoded ID, a role becomes a bounty — an REA offer/commitment matched against attested capability | Needs [measure-family](epr:measure-family-borrows-backlog) row 1 (fold substrate); import path for external benchmark attestations is new design; operator-gated on identity/DID shape; bounded by row 16a | design-only — rust-architect + qahal + operator |
| 16a | **Inclusion constraint on capability lenses — earnable paths and plural authorship, or the lens is enclosure** | Steering constraint on row 16 (operator, 2026-08-15). The temptation named plainly: models and weights are deterministic artifacts with benchmarkable similarities, so an attestation regime naturally drifts toward an exclusionary system — roles locked to whoever already benchmarks well. That drift is enclosure of capability, the same failure shape row 1 names for resources. **First:** lamad and avodah exist to make any capability carry an earnable path for humans to grow into it. A capability lens must never define a closed roster; standing to fill a role must remain reachable through the learning path (lamad) and the contribution path (avodah). If a lens has no earnable route into it, the lens has failed regardless of its scores. **Second:** humans co-author with agents. Multi-agent authorship carrying the human's DID — transferable to co-authorship with collective DID(s) — with lineage preserved is a design **requirement**, not an option: it is what lets attributions flow to the fractal affinity network across the whole EPR lifecycle. A selection regime that can only attribute work to a single filler cannot express the network's actual production shape. Rows 16/16a were themselves produced under this pattern — operator (directive and steering), Fable 5 (spec and coherence review), Opus 4.6 (scribe), ethosengine (the collective the attribution flows to, presently one person — the pattern must hold at n=1) — with lineage recorded in the commit's co-author trailers, which stand to plural-DID authorship exactly as row 16's hardcoded model pin stands to attestation: the degenerate present-tense form of the designed thing | Binds row 16 at design time — not a follow-on; operator-gated | lamad + avodah + qahal + operator |
| 17 | **Liability-absorption ledger — an outward-only projection of absorbed public obligation** | Minted from [the succession evidence bridge](epr:succession-without-conquest-mutualist-lineage-2026-08-23). The network already records the witnessed events constituting absorbed public function (care hours, restorative resolutions, shared-asset maintenance); this aggregates them and denominates them **for a named external institutional reader at a moment of presentation**. The invariant that keeps it honest: **outward only — never written back into the substrate's record as valuation**, preserving *record facts, defer valuation*. Composes with the custody≠ownership split (rows 1–2). | **p2p-design-gate: zero new DHT entry types** — binding-local `Precedent` + `Commitment` + `EconomicEvent`. Un-schedulable until an economy habit exists (row 21). | design + spec |
| 18 | **Credit-limit invariant under the existing reach consequence** | Minted from [the succession evidence bridge](epr:succession-without-conquest-mutualist-lineage-2026-08-23). Stance IV.2 already makes the consequence of default the narrowing of one's own reach; the Law Merchant literature supplies why that works (reputation enforcement holds while information cost is near zero) and where it fails (the one-shot defector). The invariant: **drawable credit ≲ a member's expected future participation, unless a legal person has taken collateral.** | Explicitly **not** standing-as-collateral — standing is a graph-derived view with no stored score and cannot be pledged. Composes with trap A7. | design |
| 19 | **Negotiated personal equilibrium — a discernment surface, never a computed figure** | Minted from [the succession evidence bridge](epr:succession-without-conquest-mutualist-lineage-2026-08-23) (operator steering, 2026-08-23). The counterfactual benchmark — *what would this person be owed by the common inheritance, honestly apportioned, in the settled end-state?* — reached in high-context counsel rather than by rule. This is the protocol's answer to George's single tax and Gesell's stamp, which were blunt substitutes for a per-person judgment nobody could afford. Composes with the per-holon elohim-ceiling rows. | **Gate: the row must carry `never a computed payout at the ceiling, never judgment at the floor`, and NO default allocation may be published** (a fallback nobody chose is harder to contest than one somebody did). Sibling of the limitarian-governor work; **not** a new numéraire. | design (council-gated) |
| 20 | **The derivable-projection invariant** | Minted from [the succession evidence bridge](epr:succession-without-conquest-mutualist-lineage-2026-08-23) §2.2. **A projection is legitimate only while it is derivable; the moment it is authoritative for anything, it *is* the ledger.** The test that decides whether elohim-storage's SQLite `token_balances` is the bank-like ledger the Hypha survey refused (`do NOT mint a VoiceBalance/StandingToken entry`). Sibling of row 17's outward-only invariant. | Today's balances carry `dht_anchor_hash: None` at every write site, so no peer could check them regardless — the argument for treating them as scratch, not ledger. | design invariant |
| 21 | **`binds-policy` — the arm that lets an authored lens actually govern** | Minted from [the monetary posture](epr:monetary-posture-internal-currencies-external-fiat-2026-08-23). A Mishpat policy ships as an `author-lens` Commitment (six fields, `role` ∈ lens/floor/ceiling), projected to a `lenses` table and served at `GET /api/v1/epr/:scope/lens-market`. But the coordinator dispatch has **no `binds-policy` arm** (`commitments.rs:189-206`), so a commitment declaring *which* lens governs an EPR is hard-rejected as `unhandled action`. **A currency you can propose but not enact is not yet a currency.** | **DNA-hash-neutral** (integrity's per-action table ends `_ => None`; coordinator hot-swap). Designed in the lens-version-DAG spec. **Blocks every other monetary row, and is the cheapest first red.** | coordinator + a2o scenario |
| 22 | **Currency as Mishpat lens — the eight never-rules as `role: floor`/`role: ceiling`** | Minted from [the monetary posture](epr:monetary-posture-internal-currencies-external-fiat-2026-08-23). A community's currency circuit is an `author-lens` Commitment whose `rule` carries the five knobs (issuance trigger, backing, carry cost, scope boundary, clearing + credit limit) and whose `telos` says what the circuit is for. Constitutional bounds arrive in the same market as floor/ceiling lenses: no protocol unit · no private seigniorage (issuance gain is common inheritance, Stance I.4) · reach never purchasable · standing never collateral · **issuance a function of settled reciprocity, never account creation** (trap A5, the Trueque signature) · no convertibility guarantee · the care layer never converted · no published default allocation. | **p2p-design-gate: zero new DHT entry types, zero new commitment actions.** Depends on row 21. | design + spec |
| 23 | **Three-function decomposition — MoE, SoV and unit-of-account as separate lenses** | Minted from [the monetary posture](epr:monetary-posture-internal-currencies-external-fiat-2026-08-23) §2.2 (operator steering, 2026-08-23). Medium-of-exchange and store-of-value pull against each other structurally; a central bank holds the tension at the apex with one instrument, in a register that forbids naming whose interest is served. Decompose rather than resolve: one lens per function, each with its own `telos` and declared limit. The sharpest: **preservation is legitimate; accretion is not** — George's unearned-increment line applied to time rather than place. | Gate: the accretion limit must be a `role: ceiling` lens, and the prudence-vs-accumulation judgment must be **excluded from the mechanical floor** (it is a discernment — row 19). | design |

## P2P design gate — credential-as-lens (recorded 2026-08-05, pre-implementation)

| Entity | Class | Address | Source of truth | Note |
|---|---|---|---|---|
| `Measure` (procedure) | **A** on reach; EPR atom before | Content-derived CID (`bafyrei…`) | DHT / EPR atom | version is a declared dependency, never recency |
| `MeasureFold` (result) | **C** operational | keyed `(measure_cid, subject_cid)` | recompute from the signed log | legitimately cacheable *because* definitionally reconstructable |
| `CredentialLens` (what unit 210 declares an Eagle to be) | **A** notarized, Mishpat-shaped | Content-derived CID | DHT | the steward binding is the social positioning; Mishpat has headroom (11/~100) |
| `CredentialAttestation` (the issued badge) | **B2** agent-scoped + attestation | Composite `(AgentPubKey, lens_cid, type)` | private chain + DHT attestation | **granular evidence stays private**; only the signed credential is notarized — this is what protects the ~3000-entry budget |

**Two concern-canon answers are load-bearing and must not be skipped:** **C4 honest absence** — the
credential must render *"this measure was never taken"* differently from *"zero"*, or an unmeasured
scout and a failing scout look identical. **C10 contract-evolution honesty** — an Eagle earned under
the 2026 lens stays valid when the 2030 lens tightens; version-pinned, never recency.

**Two properties worth protecting through implementation.** *You trust the steward's judgment, not
their arithmetic* — because folds are recompute-verifiable over a signed log, anyone can re-derive
whether the contract was met, so a credential needs no registry of authorities to be checkable; what
the collective is trusted for is what *should* count. And *plurality is the feature* — the same
evidence vector read by a different collective's lens yields a different verdict, and neither is
wrong.

**Identity-ontology note:** collective-stewarded credentials are the *correct* apex shape —
community-conferred standing, not self-assertion. A **self-issued** credential is the drift the
identity guard exists to catch.
