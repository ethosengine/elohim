# ⚠ STATE GATE — NEEDS-TRIAGE (no state)   (generated by state-machine-gen.py; do not hand-edit)

> **PAUSE before you spend tokens in this directory.**
>
> Everything here has NO machine-readable status — write-only, un-triaged. **Don't extend or design from these blind.** They are the dumping-ground signal. Classify first: give a status + a place in the graph (or distill to history if dead).

**EQUILIBRIUM = this directory is empty of docs** (only this `CLAUDE.md` remains). If you see entries
below the gate, each one is an open pressure point.

**How a doc leaves this state:** librarian/cartographer assign `status:` + links → the doc moves to its real state. `python3 .claude/scripts/memory-kit/placement-audit.py` counts this pile; it must trend to 0.

— A doc's *directory is its state*. Moves are `git mv`, link-rewritten by the operator-gated
`path-update-apply.py`. The deterministic state is computed by `placement-audit.py`; the planner's
in-scope surface by `python3 .claude/scripts/memory-kit/placement-audit.py --focus`.
