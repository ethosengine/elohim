# ⚠ STATE GATE — BLOCKED-BY-ENV   (generated by state-machine-gen.py; do not hand-edit)

> **PAUSE before you spend tokens in this directory.**
>
> Everything here is OUT OF SCOPE right now because a required environment resource is unavailable (a node/cluster/registry is down). This is **NOT a regression** — the work isn't broken, it just can't be validated until the env returns. **Do not 'fix' these. Do not spend tokens here** unless you are restoring the env itself.

**EQUILIBRIUM = this directory is empty of docs** (only this `CLAUDE.md` remains). If you see entries
below the gate, each one is an open pressure point.

**How a doc leaves this state:** Each doc's inline `<!-- STATE-TAG -->` names the missing resource and the line in `genesis/manifests/cluster-state.yaml` that re-enables it. Restore the resource, set it `available: true`, run `python3 .claude/scripts/memory-kit/placement-audit.py --focus`, then `git mv <doc> <destination-dir>` and re-run `python3 .claude/scripts/memory-kit/placement-audit.py --ledger` to lift the doc out.

— A doc's *directory is its state*. Moves are `git mv`, link-rewritten by the operator-gated
`path-update-apply.py`. The deterministic state is computed by `placement-audit.py`; the planner's
in-scope surface by `python3 .claude/scripts/memory-kit/placement-audit.py --focus`.
