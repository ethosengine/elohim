# ⚠ STATE GATE — REGRESSION   (generated by state-machine-gen.py; do not hand-edit)

> **PAUSE before you spend tokens in this directory.**
>
> Everything here was VERIFIED-STABLE and then BROKE on an available env. **This IS the rework queue** — the one pressure dir you SHOULD work in. Pick the oldest, find the cause, fix, re-verify.

**EQUILIBRIUM = this directory is empty of docs** (only this `CLAUDE.md` remains). If you see entries
below the gate, each one is an open pressure point.

**How a doc leaves this state:** Each doc's inline tag links the failing test / CI run. Fix → re-verify (CI green) → `git mv <doc> <destination-dir>` (back to active or promoted to verified), then re-run `python3 .claude/scripts/memory-kit/placement-audit.py --ledger`.

— A doc's *directory is its state*. Moves are `git mv`, link-rewritten by the operator-gated
`path-update-apply.py`. The deterministic state is computed by `placement-audit.py`; the planner's
in-scope surface by `python3 .claude/scripts/memory-kit/placement-audit.py --focus`.
