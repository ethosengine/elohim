# ⚠ STATE GATE — CLAIMED-ONLY   (generated by state-machine-gen.py; do not hand-edit)

> **PAUSE before you spend tokens in this directory.**
>
> Everything here CLAIMS done but has NO verification evidence. **Do not trust these as done. Do not build on them** as if shipped. A 'landed' claim is as untrustworthy as a checkbox (the iroh gates proved it).

**EQUILIBRIUM = this directory is empty of docs** (only this `CLAUDE.md` remains). If you see entries
below the gate, each one is an open pressure point.

**How a doc leaves this state:** Grade it: run ci-investigator / `/verify` (or wire the missing test into CI). Record `verification:`/`landed_commit:`. Verified → promote; failed → move to regression.

— A doc's *directory is its state*. Moves are `git mv`, link-rewritten by the operator-gated
`path-update-apply.py`. The deterministic state is computed by `placement-audit.py`; the planner's
in-scope surface by `python3 .claude/scripts/memory-kit/placement-audit.py --focus`.
