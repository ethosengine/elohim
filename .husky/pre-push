#!/usr/bin/env sh
# Husky v9 runs hooks through `sh -e`, which ignores a Bash shebang in this
# file. Keep this entrypoint POSIX and explicitly hand off to the Bash hook.
exec bash "$(dirname "$0")/pre-push.bash" "$@"
