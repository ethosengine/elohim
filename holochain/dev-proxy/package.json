{
  "name": "elohim-dev-proxy",
  "version": "1.0.0",
  "deprecated": "Use admin-proxy with DEV_MODE=true instead. Run: cd ../admin-proxy && DEV_MODE=true npm start",
  "description": "[DEPRECATED] Path-based WebSocket proxy for Holochain development - use admin-proxy with DEV_MODE=true instead",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "echo '⚠️  DEPRECATED: Use admin-proxy with DEV_MODE=true instead' && echo '   Run: cd ../admin-proxy && DEV_MODE=true npm start' && echo '' && node dist/index.js",
    "start:local": "npm run start",
    "start:remote": "CONDUCTOR_URL=wss://holochain-dev.elohim.host?apiKey=dev-elohim-auth-2024 npm run start",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "~5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
