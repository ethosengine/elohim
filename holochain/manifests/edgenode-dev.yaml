# Edge Node Development Deployment
#
# Provides stable Holochain conductor for Eclipse Che workspaces.
#
# Architecture:
#   Browser -> Doorway Gateway (auth + whitelist + worker pool) -> Conductor (localhost)
#
# Ports:
#   - 4444: Conductor Admin WebSocket (localhost only)
#   - 8444: socat proxy (pod-internal, for legacy/direct access)
#   - 8080: Doorway Gateway (public, authenticated)
---
# Secret for Doorway Gateway API keys
apiVersion: v1
kind: Secret
metadata:
  name: elohim-doorway-secrets
  namespace: ethosengine
type: Opaque
stringData:
  # IMPORTANT: Change these in production!
  api-key-authenticated: "dev-elohim-auth-2024"
  api-key-admin: "dev-elohim-admin-2024"
  jwt-secret: "dev-jwt-secret-change-in-production-2024"
---
# ConfigMap for conductor configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: elohim-edgenode-dev-config
  namespace: ethosengine
data:
  conductor-config.yaml: |
    # Holochain Conductor Configuration for Elohim Protocol
    admin_interfaces:
      - driver:
          type: websocket
          port: 4444
          # Allow any origin - conductor only binds to localhost anyway
          # Doorway gateway handles external authentication
          allowed_origins: "*"
    network:
      # Bootstrap served by Doorway gateway
      bootstrap_url: "https://doorway-dev.elohim.host/bootstrap"
      # Signal served on dedicated subdomain (tx5/SBD protocol requires path = pubkey)
      signal_url: "wss://signal.doorway-dev.elohim.host"
      enable_mdns: false
      enable_relaying: true
      webrtc_config:
        ice_servers:
          - urls: ["stun:stun.cloudflare.com:3478"]
          - urls: ["stun:stun.l.google.com:19302"]
    data_root_path: "/var/local/lib/holochain"
    keystore:
      type: lair_server_in_proc
      lair_root: "/var/local/lib/holochain/ks"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: elohim-edgenode-dev
  namespace: ethosengine
  labels:
    app: elohim-edgenode
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elohim-edgenode
      environment: dev
  template:
    metadata:
      labels:
        app: elohim-edgenode
        environment: dev
    spec:
      nodeSelector:
        node-type: operations
      # Init container fetches hApp from DNA pipeline artifacts
      initContainers:
        - name: happ-fetcher
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - |
              set -e
              echo "Fetching hApp from elohim-holochain pipeline..."

              # Try branch-specific build first, then dev, then main
              HAPP_URLS="
                https://jenkins.ethosengine.com/job/elohim-holochain/job/dev/lastSuccessfulBuild/artifact/elohim.happ
                https://jenkins.ethosengine.com/job/elohim-holochain/job/main/lastSuccessfulBuild/artifact/elohim.happ
              "

              for url in $HAPP_URLS; do
                echo "Trying: $url"
                if curl -sfL -o /happ/elohim.happ "$url"; then
                  echo "✅ hApp downloaded successfully"
                  ls -lh /happ/elohim.happ
                  exit 0
                fi
                echo "Failed, trying next..."
              done

              echo "❌ Could not download hApp from any source"
              exit 1
          volumeMounts:
            - name: happ-volume
              mountPath: /happ
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
      containers:
        # Main Holochain conductor
        - name: edgenode
          image: harbor.ethosengine.com/ethosengine/elohim-edgenode:BUILD_NUMBER_PLACEHOLDER
          env:
            - name: CONDUCTOR_MODE
              value: "true"
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          volumeMounts:
            - name: holochain-data
              mountPath: /data
            # Mount to template path - entrypoint.sh copies this to /etc/holochain/
            - name: conductor-config
              mountPath: /usr/local/share/holochain/conductor-config.template.yaml
              subPath: conductor-config.yaml
            # hApp volume - conductor needs access to install hApp
            - name: happ-volume
              mountPath: /opt/holochain
          # Use exec probe since conductor binds to localhost
          # bash supports /dev/tcp, ash/sh does not
          livenessProbe:
            exec:
              command:
                - bash
                - -c
                - "echo > /dev/tcp/127.0.0.1/4444"
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 5
          readinessProbe:
            exec:
              command:
                - bash
                - -c
                - "echo > /dev/tcp/127.0.0.1/4444"
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
        # Socat proxy for direct pod-internal access (legacy, not publicly exposed)
        - name: ws-proxy
          image: alpine/socat:latest
          args:
            - "TCP-LISTEN:8444,fork,reuseaddr"
            - "TCP:127.0.0.1:4444"
          ports:
            - name: admin-ws-int
              containerPort: 8444
              protocol: TCP
          resources:
            requests:
              memory: "16Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          readinessProbe:
            tcpSocket:
              port: 8444
            initialDelaySeconds: 5
            periodSeconds: 5
        # Doorway Gateway - Rust WebSocket gateway with auth, worker pool
        # Uses dev-latest tag for development
        - name: doorway
          image: harbor.ethosengine.com/ethosengine/elohim-doorway:dev-latest
          imagePullPolicy: Always
          args:
            - "--listen"
            - "0.0.0.0:8080"
            - "--conductor-url"
            - "ws://localhost:4444"
          ports:
            - name: gateway-ws
              containerPort: 8080
              protocol: TCP
          env:
            - name: DEV_MODE
              value: "true"
            - name: RUST_LOG
              value: "info,doorway=debug"
            - name: STORAGE_URL
              value: "http://localhost:8090"
            - name: API_KEY_AUTHENTICATED
              valueFrom:
                secretKeyRef:
                  name: elohim-doorway-secrets
                  key: api-key-authenticated
            - name: API_KEY_ADMIN
              valueFrom:
                secretKeyRef:
                  name: elohim-doorway-secrets
                  key: api-key-admin
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: elohim-doorway-secrets
                  key: jwt-secret
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
        # elohim-storage - Blob storage and import processing sidecar
        # Handles heavy R/W operations, forwards to conductor
        - name: elohim-storage
          image: harbor.ethosengine.com/ethosengine/elohim-storage:dev-latest
          imagePullPolicy: Always
          env:
            - name: RUST_LOG
              value: "info,elohim_storage=debug"
            - name: HOLOCHAIN_ADMIN_URL
              value: "ws://localhost:4444"
            - name: HOLOCHAIN_APP_URL
              value: "ws://localhost:4445"
            - name: HOLOCHAIN_APP_ID
              value: "elohim"
            - name: ENABLE_IMPORT_API
              value: "true"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          volumeMounts:
            - name: storage-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 30
        # hApp Installer - installs the hApp on conductor startup
        # Uses dev-latest tag - rebuilds when installer code changes
        # hApp is fetched by init container, not baked into image
        - name: happ-installer
          image: harbor.ethosengine.com/ethosengine/elohim-happ-installer:dev-latest
          imagePullPolicy: Always
          env:
            - name: CONDUCTOR_URL
              value: "ws://localhost:4444"
            - name: APP_ID
              value: "elohim"
            - name: MAX_RETRIES
              value: "60"
            - name: RETRY_DELAY_MS
              value: "2000"
            - name: HAPP_PATH
              value: "/opt/holochain/elohim.happ"
          volumeMounts:
            - name: happ-volume
              mountPath: /opt/holochain
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        # Shared volume for hApp (fetched by init container)
        - name: happ-volume
          emptyDir: {}
        # Storage data for elohim-storage blobs
        - name: storage-data
          emptyDir:
            sizeLimit: 2Gi
        - name: holochain-data
          emptyDir:
            sizeLimit: 5Gi
        - name: conductor-config
          configMap:
            name: elohim-edgenode-dev-config
---
apiVersion: v1
kind: Service
metadata:
  name: elohim-edgenode-dev
  namespace: ethosengine
  labels:
    app: elohim-edgenode
    environment: dev
spec:
  type: ClusterIP
  ports:
    # Internal socat proxy (for kubectl port-forward or pod-to-pod)
    - name: admin-ws-int
      port: 4444
      targetPort: 8444
      protocol: TCP
    # Doorway Gateway (for public authenticated access)
    - name: gateway-ws
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: elohim-edgenode
    environment: dev
---
# Public Ingress - routes all traffic through authenticated Doorway Gateway
# Supports: /admin, /app/*, /bootstrap/*, /api/v1/*
# Signal subdomain: signal.doorway-dev.elohim.host (tx5/SBD protocol)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elohim-edgenode-dev
  namespace: ethosengine
  annotations:
    # WebSocket support for /admin, /app/*, signal subdomain
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/upstream-hash-by: "$binary_remote_addr"
    nginx.ingress.kubernetes.io/websocket-services: "elohim-edgenode-dev"
    # Required for WebSocket upgrade headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: public
  tls:
    - hosts:
        - doorway-dev.elohim.host
        - signal.doorway-dev.elohim.host
      secretName: doorway-dev-tls
  rules:
    - host: doorway-dev.elohim.host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: elohim-edgenode-dev
                port:
                  number: 8080
    - host: signal.doorway-dev.elohim.host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: elohim-edgenode-dev
                port:
                  number: 8080
