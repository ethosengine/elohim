apiVersion: apps/v1
kind: Deployment
metadata:
  name: elohim-site-alpha
  namespace: elohim-alpha
  labels:
    app: elohim-site-alpha
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elohim-site-alpha
  template:
    metadata:
      labels:
        app: elohim-site-alpha
    spec:
      containers:
      - name: elohim-site-alpha
        image: harbor.ethosengine.com/ethosengine/elohim-site:BUILD_NUMBER_PLACEHOLDER
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config
          mountPath: /usr/share/nginx/html/assets/config.json
          subPath: config.json
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      volumes:
      - name: config
        configMap:
          name: elohim-config-alpha
